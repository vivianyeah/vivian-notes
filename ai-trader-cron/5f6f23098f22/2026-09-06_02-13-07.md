# Cron Job: Daily Hermes Backup

**Job ID:** 5f6f23098f22
**Run Time:** 2026-09-06 02:13:07
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

#!/bin/bash
# Daily Hermes backup to GitHub

REPO_DIR="/tmp/hermes-backup"
SOURCE_DIR="/home/vivian/.hermes"
GIT_TOKEN=$(cat $HOME/.config/hermes/backup_pat)

# Ensure repo exists
if [ ! -d "$REPO_DIR/.git" ]; then
    git clone "https://${GIT_TOKEN}@github.com/vivianyeah/hermes.git" "$REPO_DIR"
fi

cd "$REPO_DIR"
git pull origin main

# Sync files (exclude cache/logs)
rsync -av --exclude='.git' --exclude='cache/' --exclude='audio_cache/' --exclude='*.log' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='state.db' --exclude='state.db-shm' --exclude='state.db-wal' "$SOURCE_DIR/" "$REPO_DIR/"

# Commit and push
git add -A
git commit -m "Auto backup $(date '+%Y-%m-%d %H:%M')"
git push origin main

## Response

Backup successfully completed and pushed to GitHub.

**Daily Hermes Backup — 2026-09-06 02:00 ✅**

- Cloned `vivianyeah/hermes.git` to `/tmp/hermes-backup` (previous local repo had a corrupted `.git/HEAD`)
- rsynced 2.3G of `~/.hermes/` (cache/logs/locks excluded per script)
- Diff: 1 file changed, 10 insertions, 10 deletions — only `cron/jobs.json` (counter + timestamp bumps for 3 cron jobs, expected)
- Commit: `a35eca3` — *Auto backup 2026-09-06 02:12*
- Pushed to `origin/main` ✅ (remote SHA matches local: `a35eca3`)

Remote confirmed:
```
a35eca3 Auto backup 2026-09-06 02:12   ← this run
c299e36 Auto backup 2026-09-05 02:00
7883161 Auto backup 2026-09-04 02:00
```

**Note for future runs:** The `rm -rf /tmp/hermes-backup` + re-clone path was needed because yesterday's run apparently left `.git/HEAD` missing, causing `git status` to fail. The current backup is now clean, so tomorrow's run will use the normal `git pull` path (no re-clone).
