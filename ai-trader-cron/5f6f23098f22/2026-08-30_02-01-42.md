# Cron Job: Daily Hermes Backup

**Job ID:** 5f6f23098f22
**Run Time:** 2026-08-30 02:01:42
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

#!/bin/bash
# Daily Hermes backup to GitHub

REPO_DIR="/tmp/hermes-backup"
SOURCE_DIR="/home/vivian/.hermes"
GIT_TOKEN=$(cat $HOME/.config/hermes/backup_pat)

# Ensure repo exists
if [ ! -d "$REPO_DIR/.git" ]; then
    git clone "https://${GIT_TOKEN}@github.com/vivianyeah/hermes.git" "$REPO_DIR"
fi

cd "$REPO_DIR"
git pull origin main

# Sync files (exclude cache/logs)
rsync -av --exclude='.git' --exclude='cache/' --exclude='audio_cache/' --exclude='*.log' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='state.db' --exclude='state.db-shm' --exclude='state.db-wal' "$SOURCE_DIR/" "$REPO_DIR/"

# Commit and push
git add -A
git commit -m "Auto backup $(date '+%Y-%m-%d %H:%M')"
git push origin main

## Response

Backup completed successfully.

**Status:** ✅ Synced `/home/vivian/.hermes` → `vivianyeah/hermes` on `main` (commit `c7b28d6`, pushed).

**Notable observations from this run:**
- ⚠️ **Logs are being committed despite the `--exclude='*.log'` rule** — the `--exclude` pattern uses a basename glob, but rsync's `--exclude='*.log'` matches against the full path and only catches files literally named `*.log`. The rotated logs (`logs/agent.log.1`, `errors.log.1`, etc.) slip through because they're not named `*.log`. Consider changing the exclude pattern to `--exclude='*.log*'` or `--exclude='logs/'` to actually suppress them.
- ⚠️ **Cron `.tick.lock` is being committed** — a runtime lock file has no business in git. Add `--exclude='*.lock'` (and probably `--exclude='*.pid'` if any other lockfiles exist).
- ⚠️ **Cron `output/` directories are being committed** — depending on intent, this could bloat the repo with cron run artifacts. Worth deciding whether these belong in backup.
- **43 MB sent, ~1.87 GB total in repo** — repo is healthy but trending large; if the `logs/` and `cron/output/` keep growing unchecked, prune policies may be needed.
- Commit message and timestamp formatting work as designed.
