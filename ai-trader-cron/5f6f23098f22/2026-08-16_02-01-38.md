# Cron Job: Daily Hermes Backup

**Job ID:** 5f6f23098f22
**Run Time:** 2026-08-16 02:01:37
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

#!/bin/bash
# Daily Hermes backup to GitHub

REPO_DIR="/tmp/hermes-backup"
SOURCE_DIR="/home/vivian/.hermes"
GIT_TOKEN="ghp_<REDACTED>"

# Ensure repo exists
if [ ! -d "$REPO_DIR/.git" ]; then
    git clone "https://${GIT_TOKEN}@github.com/vivianyeah/hermes.git" "$REPO_DIR"
fi

cd "$REPO_DIR"
git pull origin main

# Sync files (exclude cache/logs)
rsync -av --exclude='.git' --exclude='cache/' --exclude='audio_cache/' --exclude='*.log' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='state.db' --exclude='state.db-shm' --exclude='state.db-wal' "$SOURCE_DIR/" "$REPO_DIR/"

# Commit and push
git add -A
git commit -m "Auto backup $(date '+%Y-%m-%d %H:%M')"
git push origin main

## Response

Backup pushed successfully (commit `25b01bc`).

---

# Daily Hermes Backup —2026-08-16 02:01 ✅

**Status:** Completed successfully

**What changed this run:**
- 6 files modified/added, 365 insertions, 41 deletions
- Commit: `25b01bc` — "Auto backup 2026-08-16 02:01"
- Pushed to `vivianyeah/hermes` on `main`

**Changes breakdown:**
| Change | File |
|---|---|
| Modified | `cron/jobs.json` |
| Modified | `skills/.usage.json` |
| Modified | `skills/data-science/stock-analysis/SKILL.md` |
| Added | `skills/data-science/stock-analysis/references/2026-08-14-0300-cron-pitfall-246-247-write-file-corrupt-display-bytes.md` |
| Added | `skills/data-science/stock-analysis/references/2026-08-14-2200-cron-pitfall-246-250-3cap-2cash-neutral-drift-dual-commit-sandbox-loss.md` |
| Added | `skills/data-science/stock-analysis/references/2026-08-14-2302-cron-pitfall-246-247-hybrid-ab-3cap-2cash-pmr245-second-validation.md` |

Rsync transferred46 MB / ~1.8 GB total repo size at ~4 MB/s.

---

## 🚨 Two Security Issues Found (Urgent)

While running this backup I uncovered two real problems. Please address both — I did NOT touch them because they require human decisions.

### 1. GitHub PAT hardcoded in the cron script (CRITICAL)
The script (and any shell history, process listings, error logs containing the script text) contains your full GitHub Personal Access Token:
```
ghp_<REDACTED>
```
This is a write-capable token (it pushed today's commit). Anyone who can read the script file, the script's git history, or any system logs that captured the script body has full write access to `vivianyeah/hermes`.

**Recommended actions:**
1. **Revoke that PAT now** at https://github.com/settings/tokens → delete it. Generate a new one.
2. Move the PAT to a non-checked-in file (e.g. `~/.config/hermes/backup.env`) and source it: `source ~/.config/hermes/backup.env` instead of inlining it.
3. Add `*.env`, `backup.env`, and the script filename itself to `.gitignore` so future backups don't re-leak it.

### 2. Old secrets are in git history
The current `.gitignore` correctly excludes `.env` and `auth.json`, so they're not being re-added — good. **But** they were committed in earlier runs and still exist in git history:

| File | Last commit in history | Date |
|---|---|---|
| `.env` | `91f5137` "Auto backup 2026-06-03 02:31" | 2026-06-03 |
| `auth.json` | `91f5137` "Auto backup 2026-06-03 02:31" | 2026-06-03 |

The repo currently returns 404 on unauthenticated GitHub requests, which strongly suggests it's **private** (otherwise we'd see the repo page), so these secrets are not currently publicly exposed. But:
- If the repo is ever made public, both files become visible immediately- The PAT in #1 above, if still active, has push access to rewrite history — someone who steals it can rewrite commits but **cannot erase already-pulled clones**
- If anyone forked the repo before the files were last touched (June 3), they still have those credentials

**Recommended actions:**
1. Rotate every credential that was in `.env` and `auth.json` as of June 3 (API keys, OAuth tokens, etc.) — assume they're burned.
2. After revoking the old PAT (#1 above) and rotating secrets, optionally rewrite git history to purge the old commits (e.g. `git filter-repo` or BFG), then force-push.
3. For a small personal backup repo, consider using **git-crypt** or **blackbox** to encrypt sensitive files in the repo, or move secrets entirely out of `~/.hermes` into a system keyring.

---

**Cron job itself:** functional and reliable. No issues with the rsync, commit, or push flow — just the two security concerns above.
