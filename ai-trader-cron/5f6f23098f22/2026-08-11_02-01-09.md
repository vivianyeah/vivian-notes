# Cron Job: Daily Hermes Backup

**Job ID:** 5f6f23098f22
**Run Time:** 2026-08-11 02:01:08
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

#!/bin/bash
# Daily Hermes backup to GitHub

REPO_DIR="/tmp/hermes-backup"
SOURCE_DIR="/home/vivian/.hermes"
GIT_TOKEN="ghp_<REDACTED>"

# Ensure repo exists
if [ ! -d "$REPO_DIR/.git" ]; then
    git clone "https://${GIT_TOKEN}@github.com/vivianyeah/hermes.git" "$REPO_DIR"
fi

cd "$REPO_DIR"
git pull origin main

# Sync files (exclude cache/logs)
rsync -av --exclude='.git' --exclude='cache/' --exclude='audio_cache/' --exclude='*.log' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='state.db' --exclude='state.db-shm' --exclude='state.db-wal' "$SOURCE_DIR/" "$REPO_DIR/"

# Commit and push
git add -A
git commit -m "Auto backup $(date '+%Y-%m-%d %H:%M')"
git push origin main

## Response

Backup completed and pushed: `ac6220e..3ca2b6b` on `vivianyeah/hermes` main.

**Changed this run (8 files):**
- `cron/jobs.json`, `skills/.usage.json` (modified)
- `skills/github/github-auth/SKILL.md` + new `references/token-revoked-2026-08-10.md`
- 5 new stock-analysis cron-pitfall reference notes (2026-08-10/11)

**One thing to flag:** the script hardcodes a GitHub PAT (`ghp_<REDACTED>`), but the push actually succeeded using the credentials already stored in `/tmp/hermes-backup/.git/config` — and your own backed-up notes include `skills/github/github-auth/references/token-revoked-2026-08-10.md`. If that token is indeed revoked, the `git clone` fallback in this script will fail the next time `/tmp/hermes-backup` is wiped (it's in `/tmp`, so a reboot does that). Worth moving the credential to a git credential helper or env var and updating the script — I didn't change the script or touch the token myself.
