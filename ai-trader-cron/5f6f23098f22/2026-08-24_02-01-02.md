# Cron Job: Daily Hermes Backup

**Job ID:** 5f6f23098f22
**Run Time:** 2026-08-24 02:01:01
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

#!/bin/bash
# Daily Hermes backup to GitHub

REPO_DIR="/tmp/hermes-backup"
SOURCE_DIR="/home/vivian/.hermes"
GIT_TOKEN="ghp_<REDACTED>"

# Ensure repo exists
if [ ! -d "$REPO_DIR/.git" ]; then
    git clone "https://${GIT_TOKEN}@github.com/vivianyeah/hermes.git" "$REPO_DIR"
fi

cd "$REPO_DIR"
git pull origin main

# Sync files (exclude cache/logs)
rsync -av --exclude='.git' --exclude='cache/' --exclude='audio_cache/' --exclude='*.log' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='state.db' --exclude='state.db-shm' --exclude='state.db-wal' "$SOURCE_DIR/" "$REPO_DIR/"

# Commit and push
git add -A
git commit -m "Auto backup $(date '+%Y-%m-%d %H:%M')"
git push origin main

## Response

Daily Hermes backup completed successfully.

**Status:** ✅ Pushed to `vivianyeah/hermes` main

**Commit:** `0851810` — Auto backup 2026-08-24 02:00 (12 files changed, +1207 / −37)

**Sync summary:**
- `rsync` transferred ~49 MB of metadata into the local clone; repo total size ~1.76 GB
- 4 modified: `cron/jobs.json`, `skills/.curator_state`, `skills/.usage.json`, `skills/web-app-development/SKILL.md`
- 8 new files added (untracked from prior runs that hadn't been committed):
  - `skills/.curator_backups/2026-08-22T21-43-28Z/` — curator run archive (manifest, cron-jobs snapshot, skills.tar.gz)
  - `skills/ai-platform/minimax/scripts/` — 4 MiniMax platform scripts (image / music / tts / video)
  - `skills/data-science/stock-analysis/references/2026-08-24-0100-cron-pitfall-258-260-stale-meta-subagent-telegram-long-gap-reset.md`

**Verification:** Local HEAD `0851810…` matches remote `main` ref on GitHub. Push confirmed (`aabe901..0851810` advanced on origin).

No action required.
