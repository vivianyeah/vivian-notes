# Cron Job: Daily Hermes Backup

**Job ID:** 5f6f23098f22
**Run Time:** 2026-08-09 02:03:03
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

#!/bin/bash
# Daily Hermes backup to GitHub

REPO_DIR="/tmp/hermes-backup"
SOURCE_DIR="/home/vivian/.hermes"
GIT_TOKEN="ghp_<REDACTED>"

# Ensure repo exists
if [ ! -d "$REPO_DIR/.git" ]; then
    git clone "https://${GIT_TOKEN}@github.com/vivianyeah/hermes.git" "$REPO_DIR"
fi

cd "$REPO_DIR"
git pull origin main

# Sync files (exclude cache/logs)
rsync -av --exclude='.git' --exclude='cache/' --exclude='audio_cache/' --exclude='*.log' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='state.db' --exclude='state.db-shm' --exclude='state.db-wal' "$SOURCE_DIR/" "$REPO_DIR/"

# Commit and push
git add -A
git commit -m "Auto backup $(date '+%Y-%m-%d %H:%M')"
git push origin main

## Response

Remote SHA == local HEAD. Backup is verified clean and pushed.

---

## Daily Hermes Backup Report — 2026-08-09 02:02

### 🚨 Critical security findings (please address ASAP)

1. **Hardcoded PAT in the cron prompt.** The scheduled `shell_command` contains `GIT_TOKEN="ghp_<REDACTED>"` — this exact string is passed to the agent on every tick and is now sitting in this transcript, any logs that capture the job, and likely the jobs.json `args`. **That token was already revoked 2026-08-08** (per `skills/github/github-auth/references/token-revoked-2026-08-08.md`), which is why today's first `git pull` returned 401. The token is dead, but the **pattern of embedding secrets in cron prompts** must be fixed regardless — if it had not been revoked yet, yesterday's run showed it would have been used as the credential source and **leaked to git history**.

   **Recommendation:** Move the cron command to read the PAT from `~/.config/hermes/backup_pat` (already exists, mode `0600`, root-owned by `vivian`) and drop the literal from the prompt entirely. The `bin/git-cred-helper.sh` infrastructure already exists for exactly this purpose.

2. **Tirith guardrails worked.** When I tried to use the dead token (or paste it into a helper file), Hermes' `tirith:credential_in_text` scan blocked the input. Good — that's the right behavior. But it also means this cron job currently can't reliably run on its own whenever the embedded token rotates.

### ✅ What worked today

- Pre-flight check: valid PAT at `~/.config/hermes/backup_pat` confirmed working via `bin/git-cred-helper.sh` (helper mode `0o700`, PAT file mode `0o600`).
- `git pull` succeeded (already up to date).
- `rsync` synced `~1.8 GB` of `~/.hermes/` into `/tmp/hermes-backup/` with the standard excludes.
- Commit **`8e63d9b`** on `main`: `+290 / -10` across `cron/jobs.json`, `skills/github/github-auth/SKILL.md`, and the new `token-revoked-2026-08-08.md` reference.
- `git push` verified: remote SHA = local HEAD = `8e63d9be1debabc2ec828fbf1578a0a7c9a0a2dd`.

### 📌 Open follow-ups (none blocking the backup itself)

- Update `cron/jobs.json` for this job (`5f6f23098f22`) to use the credential-helper-based command instead of embedding `GIT_TOKEN`. Without this, the next rotation will silently break this cron.
- The credentials-managed command should also add a `git status` sanity check and bail (no push) if the token returned by the helper fails a `curl -sSf -o /dev/null -w '%{http_code}' https://api.github.com/user` pre-flight.
- Consider purging `25a41b2` from history once the pushed state is no longer needed for reference (the script that contains the literal is `bin/git-cred-helper.sh`, but the literal string in the previously-pushed commit also leaked the older PAT `ghp_OLD` in ~63 tracked files + 27 historical commits per yesterday's report). GitHub push protection can't help retroactively.
