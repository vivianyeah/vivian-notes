# Cron Job: Daily Hermes Backup

**Job ID:** 5f6f23098f22
**Run Time:** 2026-08-08 03:08:04
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

#!/bin/bash
# Daily Hermes backup to GitHub

REPO_DIR="/tmp/hermes-backup"
SOURCE_DIR="/home/vivian/.hermes"
GIT_TOKEN="ghp_<REDACTED>"

# Ensure repo exists
if [ ! -d "$REPO_DIR/.git" ]; then
    git clone "https://${GIT_TOKEN}@github.com/vivianyeah/hermes.git" "$REPO_DIR"
fi

cd "$REPO_DIR"
git pull origin main

# Sync files (exclude cache/logs)
rsync -av --exclude='.git' --exclude='cache/' --exclude='audio_cache/' --exclude='*.log' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='state.db' --exclude='state.db-shm' --exclude='state.db-wal' "$SOURCE_DIR/" "$REPO_DIR/"

# Commit and push
git add -A
git commit -m "Auto backup $(date '+%Y-%m-%d %H:%M')"
git push origin main

## Response

# Daily Hermes backup report — 2026-08-08 02:19 (UTC+8)

## Executive result

**Backup data was synchronized and pushed successfully** to `vivianyeah/hermes` at commit [`c511e447`](https://github.com/vivianyeah/hermes/commit/c511e447791d90243babc78f1105714deb55576d).

However, the pushed commit contains credential material. **Treat the token stored at `/home/vivian/.config/hermes/backup_pat` as compromised and rotate it before the next backup run.**

The supplied script was **not executed verbatim** because it contains an embedded credential and unsafe URL-based authentication.

## 1. Status matrix

| Step | Status | Result |
|---|---:|---|
| Repository integrity | ✅ | `/tmp/hermes-backup` is healthy; `git fsck` completed cleanly |
| Authentication | ✅ | Token from `~/.config/hermes/backup_pat` authenticated successfully: GitHub API `/user` and the private repository both returned HTTP 200 |
| Pull/fetch | ⚠️ | Initial pull could not execute the bundled askpass script because it lacked the executable bit; an authenticated fetch using the secure credential helper later succeeded, with no divergence |
| Rsync | ✅ | Synced `/home/vivian/.hermes` using `.gitignore`-based exclusions plus `.env`/`.git` exclusions |
| Commit | ✅ | `c511e447`; 260 files changed, 33,719 insertions, 1,147 deletions |
| Push | ✅ | `main` advanced from `25a41b2` to `c511e447` |
| Post-push verification | ✅ | `HEAD == origin/main`, zero commits ahead, clean working tree; authenticated `ls-remote` confirmed the remote SHA |
| Credential hygiene | ❌ | The pushed tree contains old and currently valid PAT literals |
| Duplicate backup job | ⚠️ | The separate `hermes-backup-temp` pipeline was not run; it still has a 53-commit backlog and embedded credential files |

## 2. Root cause and evidence

### Credential contamination

The preferred token file is secure on disk (`mode 600`) and was valid during this run. But the source tree already contained credential material:

- The old inline PAT remains in `/home/vivian/.github_token`.
- The old PAT is still present in both enabled backup prompts in `/home/vivian/.hermes/cron/jobs.json`.
- A source scan found the old PAT in **566 non-runtime files**, with **758 exact occurrences**.
- After synchronization, the old PAT appeared in **63 tracked files** in the backup repository and in **27 reachable commits**.
- The currently valid PAT appeared twice in `skills/github/github-auth/references/push-protection-gh013.md`; that file was included in the pushed commit.
- GitHub’s API reports that secret scanning is **disabled** for `vivianyeah/hermes`, so the exposed current token was not automatically revoked.

The exact token values are intentionally not repeated here.

### Cron configuration remains unsafe

Both backup jobs are still enabled:

- `5f6f23098f22` — daily 02:00 job targeting `vivianyeah/hermes`
- `354f9e5ca178` — daily 03:00 job targeting `vivianyeah/hermes-backup`

Both prompts still contain the old embedded PAT. `cron/jobs.json` is tracked by the backup repository, so leaving it unchanged guarantees that future rsync runs will reintroduce the leak.

### Secondary pipeline exposure

`/home/vivian/hermes-backup-temp` still contains:

- 53 local commits ahead of its remote `main`
- An embedded credential in `.git/config`
- Old-token-bearing `.git-askpass`, `.git-askpass-token`, and `.git-credentials` files
- A per-repository `store` credential helper configuration

That repository must not be pushed until it has been scrubbed and its authentication files have been replaced.

## 3. Required remediation

### Immediate — before the next scheduled tick

1. **Disable or replace both leaked jobs** before the next 02:00/03:00 execution.
2. Revoke the token currently stored in `~/.config/hermes/backup_pat`; it was published in the pushed repository content.
3. Revoke the older token stored in `~/.github_token` and embedded in the cron prompts.
4. Generate a fresh fine-grained PAT limited to:
   - Repository: `vivianyeah/hermes`
   - Permission: `Contents: Read and write`
5. Store only the new token in:
   ```text
   /home/vivian/.config/hermes/backup_pat
   ```
   with mode `600`.

### Fix the cron path

Use one backup pipeline only. The replacement prompt should contain no token and should invoke the script, for example:

```text
Run /home/vivian/.hermes/scripts/backup_hermes.sh
```

Disable the duplicate `354f9e5ca178` job unless the separate `hermes-backup` repository is intentionally required.

Before re-enabling the job, update `backup_hermes.sh` to:

- Use a clean remote URL without embedded credentials
- Set `GIT_TERMINAL_PROMPT=0`
- Use `/home/vivian/.hermes/bin/git-cred-helper.sh` or a correctly executable askpass helper
- Redact PAT-shaped strings before `git add`
- Abort if staged blobs still contain credential patterns

The current script reads from the secure file but still constructs a token-bearing URL and does not set `GIT_TERMINAL_PROMPT=0`.

### Clean the source, not only the backup clone

Remove or redact credential literals from the source tree, especially:

- `/home/vivian/.hermes/cron/jobs.json`
- `skills/.curator_backups/*/cron-jobs.json`
- Archived Hermes backup incident reports
- `skills/github/github-auth/references/push-protection-gh013.md`
- Older backup helper scripts and diagnostic documents

Redacting only `/tmp/hermes-backup` is insufficient because the next rsync will copy the source literals back.

### Rewrite the pushed history

Because the compromised material is already present in `origin/main`, a normal cleanup commit will not remove it from history.

After rotating the tokens and sanitizing the source:

1. Make a forensic copy of `/tmp/hermes-backup`.
2. Use `git filter-repo --replace-text` to remove:
   - The old PAT
   - The currently compromised PAT
   - Truncated forms and other credential-bearing examples
3. Re-add the clean remote URL if `filter-repo` removes it.
4. Verify no exact or truncated PAT strings remain.
5. Force-push the rewritten `main` history with `--force-with-lease`.

This should be done as a coordinated, human-attended operation.

### Secondary repository

Before touching `/home/vivian/hermes-backup-temp`:

- Remove embedded credentials from its remote configuration.
- Delete/replace its old askpass and credential-store files.
- Scrub its 53-commit backlog.
- Only then push it to `vivianyeah/hermes-backup`.

**Current local backup state:** `/tmp/hermes-backup` is clean and fully synchronized with remote `main` at `c511e447`; no backup data is pending locally.
