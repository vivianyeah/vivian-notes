# Cron Job: Daily Hermes Backup

**Job ID:** 5f6f23098f22
**Run Time:** 2026-08-04 02:01:39
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

#!/bin/bash
# Daily Hermes backup to GitHub

REPO_DIR="/tmp/hermes-backup"
SOURCE_DIR="/home/vivian/.hermes"
GIT_TOKEN="ghp_<REDACTED>"

# Ensure repo exists
if [ ! -d "$REPO_DIR/.git" ]; then
    git clone "https://${GIT_TOKEN}@github.com/vivianyeah/hermes.git" "$REPO_DIR"
fi

cd "$REPO_DIR"
git pull origin main

# Sync files (exclude cache/logs)
rsync -av --exclude='.git' --exclude='cache/' --exclude='audio_cache/' --exclude='*.log' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='state.db' --exclude='state.db-shm' --exclude='state.db-wal' "$SOURCE_DIR/" "$REPO_DIR/"

# Commit and push
git add -A
git commit -m "Auto backup $(date '+%Y-%m-%d %H:%M')"
git push origin main

## Response

This is now clearer. The script is **fundamentally broken** as-written, and I will not push without resolving the issues. Let me report findings.

## Daily Hermes Backup — Cron Run Report

**Status:** ❌ FAILED — script did not push (correctly, given the bugs).

**What I tried:** Ran the script's logic minus the credential embedding (security layer blocked the hardcoded PAT). Completed the read-only parts (rsync + git status) to diagnose.

### Issues found

1. **Hardcoded GitHub PAT in script source** (`ghp_sx617aV…`). This is a credential leak — it lives in plaintext in whatever file/script holds it, will be flagged by any secret scanner, and is the reason this run was blocked by the security check. **Action:** move it to `/home/vivian/.github_token` (which already exists and is 41 bytes — likely the same token) and have the script read it from there.

2. **Wrong target repo.** The existing `/tmp/hermes-backup/.git` has remote `https://github.com/vivianyeah/vivian-notes.git`, not `vivianyeah/hermes.git` as the script intends. The `vivianyeah/hermes` repo returns **HTTP 404** on GitHub — it doesn't exist. The local clone has ~3.5k files and 1.8 GB of history that look like the AI-Trader / Hermes notes repo, not a hermes-config backup.

3. **Broken submodule.** `rsync` copied `hermes-agent/hermes-agent/` recursively, and the outer `hermes-agent/` is registered as a submodule pointing at `/tmp/hermes-backup/hermes-agent` (self-reference). `git add -A` is now erroring with:
   ```
   fatal: in unpopulated submodule 'hermes-backup'
   fatal: 'hermes-backup/.git' not recognized as a git repository
   ```
   This will block every commit until `--ignore-submodules` is added or the submodule is removed.

4. **Massive repo size (1.8 GB).** The rsync transferred ~52 MB new bytes today but the working tree is enormous. Most of it is `hermes-agent/` (cloned framework source, not config) and other bulky items the script probably didn't intend to back up.

### Recommended script fixes

```bash
# 1. Read token from file, don't hardcode
TOKEN=$(cat /home/vivian/.github_token)
# 2. Decide which repo is actually the target. If it's notes, change the URL.
REPO_URL="https://${TOKEN}@github.com/vivianyeah/vivian-notes.git"
# 3. Don't rsync hermes-agent/ (it's the framework source, already in its own repo)
rsync -av --exclude='.git' --exclude='cache/' --exclude='audio_cache/' \
  --exclude='*.log' --exclude='gateway.pid' --exclude='gateway.lock' \
  --exclude='state.db' --exclude='state.db-shm' --exclude='state.db-wal' \
  --exclude='hermes-agent/' --exclude='image_cache/' --exclude='kanban.db' \
  --exclude='sessions/' --exclude='logs/' --exclude='sandboxes/' \
  "$SOURCE_DIR/" "$REPO_DIR/"
# 4. Avoid the submodule recursion loop
git rm --cached hermes-agent 2>/dev/null
echo "hermes-agent/" >> "$REPO_DIR/.gitignore"
```

### Backup not performed
No commit was made, nothing was pushed. The local rsync did write ~52 MB of new files into `/tmp/hermes-backup/` but they are unstaged because of the submodule error. Recommend reviewing and fixing the script before the next cron tick.
