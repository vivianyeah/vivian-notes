# Cron Job: AI-Trader 模擬倉 Scan（北京 23:00）

**Job ID:** d7bfeb2c66b3
**Run Time:** 2026-08-19 23:05:59
**Schedule:** 0 23 * * 1-5

## Prompt

[IMPORTANT: The user has invoked the "stock-analysis" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: stock-analysis
"description": "Vivian's full trading system — AI-Trader cron pipeline (HermesV ID 6092). Stage 2 screening, yfinance MA/MACD/RSI/Bollinger, beta/ATR stop. Cron: block classification Types A-D + X, drift-magnitude lag fingerprint, zero-trigger counter, cash-trajectory, execute_code sandbox, TP1/TP2 state, Notes-updated vs 帳戶總值, held-in-Stage-2 cap-block, cash-at-floor counter, simultaneous sell+buy lag netting, squeeze-through micro-buy, full-closure with prior TP history, counter reset, day-boundary-2h-midnight-rollover-reset, 0-trade-TP1-audit-block, 7-candidates-top-5-evaluated, stdout-prefix per-line parser, ⭐5 fallback, broker-side HTTP 400 reject (Type X). P-MR-90 through P-MR-253 pitfalls documented. Triggers: stock analysis, Stage 2 scan, AI-Trader, ai4trade, HermesV, TP1/TP2, scan.py, market_regime, 持倉報告, 止蝕位, IB Gateway, 模擬倉."
version: 1.1.33
author: Hermes Agent
license: MIT
metadata:
    hermes:
    tags: [stocks, technical-analysis, yfinance, trading, US-stocks, HK-stocks, ai-trader, ai4trade, hermesv, market-regime, IB, cron, defensive-read, NoneType, git-push, P-MR-90, P-MR-91, P-MR-92, P-MR-93, P-MR-94, P-MR-95, P-MR-96, P-MR-97, P-MR-98, P-MR-99, P-MR-100, P-MR-101, P-MR-102, P-MR-103, P-MR-104, P-MR-105, P-MR-106, P-MR-107, P-MR-108, P-MR-109, P-MR-110, P-MR-111, P-MR-112, P-MR-113, P-MR-114, P-MR-115, P-MR-116, P-MR-117, P-MR-118, P-MR-119, P-MR-120, P-MR-121, P-MR-122, P-MR-123, P-MR-124, P-MR-125, P-MR-126, P-MR-127, P-MR-128, P-MR-129, P-MR-130, P-MR-131, P-MR-132, P-MR-133, P-MR-134, P-MR-135, P-MR-136, P-MR-137, P-MR-138, P-MR-139, P-MR-140, P-MR-141, P-MR-142, P-MR-143, P-MR-144, P-MR-145, P-MR-146, P-MR-147, P-MR-148, P-MR-149, P-MR-150, P-MR-151, P-MR-152, P-MR-153, P-MR-154, P-MR-155, P-MR-156, P-MR-157, P-MR-158, P-MR-159, P-MR-160, P-MR-161, P-MR-162, P-MR-163, P-MR-164, P-MR-165, P-MR-166, P-MR-167, P-MR-168, P-MR-169, P-MR-170, P-MR-171, P-MR-172, P-MR-173, P-MR-174, P-MR-175, P-MR-176, P-MR-177, P-MR-178, P-MR-179, P-MR-180, P-MR-181, P-MR-182, markdown-patch, git-work-tree, saturation-block-classification, drift-fingerprint, zero-trigger-counter, scan-py-truncation, cost-basis-fifo-vs-api, cash-trajectory, post-buy-cliff, cron-report-template, execute-code-sandbox-isolation, stdout-driven-block-classification, tp-state-per-position, scan-printed-vs-notes-updated, held-position-stage2-overlap, cash-at-floor-counter, simultaneous-lag-netting, squeeze-through-micro-buy, full-position-closure-with-tp-history, counter-reset-rule, lag-bounce-cancel, buy-only-notes-clean, queue-exhaustion-type-d, new-lot-tp-reset, git-push-no-credential-prompt, de-cost-basis-blended-vs-fifo, notes-trust-drift-gate, lag-magnitude-vs-observed-drift, double-micro-buy-aggregation, block-print-cheapest-only, hood-life-cycle-table, env-var-git-form-fallback,        clean-clone-default-commit-path, day-boundary-reset-validation, multi-sl-drift-decomposition, execute-code-isolation-repeated, same-day-squeeze-buy-counter-chain, notes-trust-borderline-5-10-drift, all-held-stage2-degenerate-type-b, counter-state-in-commit-message, consecutive-saturation-state-archived, cron-report-timestamp-slot-vs-wallclock, bash-approval-prompt-c-flag-cd-substitute, fstring-literal-brace-parse, stdout-prefix-per-line-parser, stage2-star5-fallback-quote, fifo-recompute-mandatory-zero-fill, broker-reject-type-x, actual-fill-cash-precision, inter-scan-cash-drift-no-trade, fresh-lot-null-persistent, multi-commit-iteration-pattern,        P-MR-186, fresh-clone-each-cron, comma-regex-bug, micro-buy-lifecycle-aggregation, ktos-cycle-5, cash-at-floor-counter-empirical-3, P-MR-188, P-MR-189, P-MR-190, P-MR-191, fstring-brace-trap-recurring, hybrid-ab-block-classification, same-day-counter-carry-forward, p-mr-180-1h-reconcile-window, inter-scan-drift-watch-footnote, day-boundary-microbuy-counter-arithmetic, P-MR-192, P-MR-193, P-MR-194, P-MR-195, P-MR-196, P-MR-197, P-MR-198, hybrid-abx-block-classification, full-saturation-break, four-trigger-scan, format-brace-escape-recipe, notes-trust-clean-textbook, hybrid-xd-block-classification, top-5-truncation-type-d, api-fifo-identity-shortcut, cross-day-reset-72h, P-MR-213, P-MR-214, P-MR-215, hybrid-abx-block-classification, full-saturation-break, four-trigger-scan, format-brace-escape-recipe, notes-trust-clean-textbook]
    category: data-science
---

# Stock Technical Analysis

## 🚨 P-MR Quick Reference Index (count-free lookup for cron time)

> **Use this section when you need a fast lookup of a specific P-MR number during a cron run.** For full pitfall context (reproduction recipe, decomposition, related references), search the rest of this skill or the `references/` subdir. **Counts are intentionally omitted** — they bloat the table; trust the recipe + source link instead.

### Block classification patterns (Hybrid A+B family)

| Pattern | Recipe | Source |
|---------|--------|--------|
| **P-MR-253** EXTREME cap-floor collapse | `cash < min(held_value)` AND `len(qty=1) ≥ 5` | deepest saturation state |
| **P-MR-255** 5-SL realization flush | 5 SL fires + 0 BUY, scan uses stale pre-trade cash | 12th Hybrid A+B sub-pattern |
| **P-MR-256** P-MR-250 git push v2 | `git fetch` + `cp` + `git reset --soft` + `git add` + push | avoids checkout--ours trap |
| **P-MR-187b** partial-saturation squeeze | 2 SL + 1 TP1 + 1 BUY success | cash-flush > $1k |
| **P-MR-189** 2-cand hybrid | 0 trades + 1 cap-block + 1 cash-block | |
| **P-MR-194** 4-type hybrid+X | 4 ⭐5 spanning success+cap+reject+implicit | |
| **P-MR-195** full-saturation-break | buy > $1k post-saturation | cf reset to 0 |
| **P-MR-203** 1st-rank-RR micro-squeeze | 1 BUY, highest-RR wins | drift ~$0 |
| **P-MR-205** multi-cap collapse | 0 BUY + 4 cap-block + 1 cash-block | |
| **P-MR-208** 2nd-rank-RR micro-squeeze | 1 BUY, cheapest blocked, 2nd deployed | |
| **P-MR-211** cash-pool-split | `cash / MAX_STOCKS < unit_price` blocks micro-buy | qty=0 |
| **P-MR-213** Hybrid X+D | broker HTTP 400 + queue exhaustion | Type X + Type D |
| **P-MR-224** Degenerate Hybrid B | 5+ cap-block, 0 cash | pure-cap saturation |
| **P-MR-229** Pure Type A | 5+ cash-block, no held-cap | |
| **P-MR-238** Hybrid A+B + 2 SL exits | 2 SL + 0 BUY + 4 blocks | cash-flush INSUFFICIENT |
| **P-MR-242** Hybrid A+B 4-cap+1-cash @ $100-$200 cap-floor | 4 Type B (held) + 1 Type A (non-held cash-block) | top-3 cap-floor collapse + rank-4 non-held |

### Drift thresholds (Notes ↔ FIFO)

| Drift | Classification | Source |
|-------|---------------|--------|
| **Notes ↔ FIFO <$10** | TRUST (cleanest) | P-MR-198/206 |
| **<$30 with-trades** | TRUST unconditionally | P-MR-142/230/239 |
| **$30-$100** | NEUTRAL (footnote both) | P-MR-230 |
| **>$100** | IGNORE / recompute headline | P-MR-230 |
| **0-trade <$30** | TRUST unconditionally | P-MR-230 |

### Counter semantics

| Variable | Reset trigger | Increment when |
|----------|---------------|----------------|
| `zt` (zero-trigger) | BUY fired (P-MR-110) OR day-boundary reset (P-MR-155) | 0 BUY in scan |
| `cf` (cash-at-floor) | post-cash > $100 (P-MR-125/129) OR day-boundary (P-MR-155) | post-cash < $100 |
| Both | BJT date change (binary, not time-dependent) | P-MR-185/215 |

**Day-boundary reset order**: RESET FIRST (zt=1, cf=0), then trade effects SECOND.

### Stale-quote (P-MR-183)

Drift between scan-printed MV and Σ(api_qty × api_price) is PURE stale-quote. Magnitude: $2-8k on 30-position account. Decompose as `Σ(qty × (fresh − snapshot))`.

### Pitfall pointer map (one-line lookups)

- **P-MR-168**: per-line API parser (catches 5%/MA10/TP1 prefix)
- **P-MR-169**: ⭐5 fallback for non-held Stage 2 prices
- **P-MR-172**: API list is pre-trade shell in same-trade scans
- **P-MR-176**: tp1_state may contain dict-valued closure audit
- **P-MR-187**: ALWAYS tee stdout to timestamped log
- **P-MR-226**: heredoc blocked on em-dash variation-selector
- **P-MR-231**: heredoc blocked on emoji variation-selector
- **P-MR-232**: terminal `-c` blocked on bash `$VAR` interpolation of Python regex
- **P-MR-233**: `fifo_pnl.py` canonical path is `~/.hermes/skills/.../scripts/`, NOT `/tmp/`
- **P-MR-235**: TP1-partial Notes-table qty lag (preserves pre-trade qty)
- **P-MR-239**: empirical FLOOR for with-trades Notes ↔ FIFO drift ≈ $17 on 29 positions
- **P-MR-240**: cash-flush INSUFFICIENT when `cash_pool_per_stock < unit_price` (P-MR-211 denominator)
- **P-MR-241**: NEW smallest 0-trade canonical drift record ($0.71, 03:30 BJT 2026-08-07) — beats P-MR-227 ($2.81)
- **P-MR-242**: Hybrid A+B 4-cap+1-cash @ $100-$200 cash-floor — distinct from P-MR-205 (multi-cap at $50-$100) and P-MR-229 (pure-cash at <$50)
- **P-MR-243**: 0-trade scan mutation guard — assert `len(post) == len(pre)` AND `post == pre` BEFORE writing report
- **P-MR-244**: P-MR-190 1h reconcile window empirically validated 3rd time (INTC + MRVL 03:00→03:30 fresh-lot reconcile)
- **P-MR-245**: stale-clone "fetch first" push rejection — superseded by P-MR-250 (`--soft` recipe avoids Hermes approval block)
- **P-MR-246**: Telegram bot token location `/home/vivian/.hermes/.env` `TELEGRAM_BOT_TOKEN=`
- **P-MR-247**: day-boundary reset at 23:00→01:00 cross-midnight (binary BJT-date detection, NOT time-dependent)
- **P-MR-248**: SCAN "Cash:" line is PRE-trade shell in same-trade scans (extends P-MR-172 from API-list to Cash line) — model post-cash = pre-cash − Σ(actual fills)
- **P-MR-249**: scan-printed Total `Cash + MV` UNDERCOUNT by ~$4-5k vs Notes when same-scan BUYs fire (use Notes as headline)
- **P-MR-250**: REFINED P-MR-245 RECOVERY: use `git reset --soft origin/main` (NOT `--hard` which trips Hermes approval scan) — rebase fresh-clone commit onto remote without clobbering working tree
- **P-MR-253**: literal `telegram:8685773783` target from cron task = BOT's own user_id (hermesswitch_bot), NOT Vivian's chat — substitute with paired user_id from `/home/vivian/hermes-backup-temp/hermes/pairing/telegram-approved.json` (typically `7290340299`)
- **P-MR-254 (NEW 2026-08-18 03:30 BJT)**: `Telegram.sendMessage` with `parse_mode='HTML'` returns 400 on messages containing unescaped `<` or `>` (e.g. literal `<br>` or `<something>`). **Recipe**: ALWAYS send cron reports with `parse_mode` OMITTED (plain text mode). Emoji + unicode + `**bold**` markers render fine in plain text. Diagnostic: `description: can't parse entities: Unsupported start tag "..."` → drop `parse_mode` from JSON payload. Reference: `references/2026-08-18-0330-cron-pitfall-253-extreme-cap-floor-2nd-observation.md` (Telegram section).
- **P-MR-252**: cleanest 2-BUY same-scan ever — HOOD fresh-lot + ALAB HELD-cap-bypass hybrid (NEW 12th Hybrid A+B sub-pattern), Notes ↔ FIFO drift −$19.87
- **P-MR-253 (2026-08-18 03:00 BJT — first observed; re-validated 03:30 BJT) — EXTREME cap-floor collapse: when `cash < min(held_value)`, even theoretical 1-share micro-buys are blocked.** Cash $3.28 < cheapest held symbol value (PFE 1@$26.83). `max_pos_per_stock = min(cash $3.28, total_value × 10% = $9,683.66) = $3.28`. All 5 ⭐5 candidates are HELD with `held_value > $3.28` trivially. Recipe: `cash < $50` AND `len(held_symbols_with_qty=1) >= 5` AND `min(held_value) > cash` → classify as "Extreme cap-floor collapse (P-MR-253)", distinct from P-MR-144 (cash < $100, held at >10% of total), P-MR-211 (cash-pool-split denominator), P-MR-224 (degenerate pure-cap), P-MR-229 (pure Type A). **Distinguishing signature**: 0 trades fired AND cash < min(held_value), making even theoretical micro-buys impossible. Deepest saturation state observed in cron history. Locked at 0-trigger until either (a) cash accumulates > min(held_value) OR (b) SL/TP fires to flush cash. 5 cap-block lines all printed (NOT P-MR-210 silent-skip) because every held_value trivially > $3.28 floor. **Fingerprint**: a top-5 of `k` HELD + `5-k` non-held will print `k` cap-block lines + `5-k` cash-block lines (when non-held unit-price > cash). 03:30 BJT saw 4 HELD (ALAB/ARM/VRT/SYM) + 1 non-held (AMD $505.66 > $3.28) → 4 cap-block + 1 cash-block. **Diagnostic**: `min(cash, total × 10%) / min(held_value) < 1.0` is the inequality that makes even theoretical 1-share micro-buys impossible. **Drift behavior**: Notes ↔ FIFO drift $2-5 routinely (P-MR-230 0-trade canonical TRUST); API↔FIFO identity EXACT (P-MR-214); stale-quote $4-5k PURE (P-MR-183). **Counter behavior**: same-day carry (P-MR-155) → 0 BUY → zt+1 (P-MR-110) → cash <$100 → cf+1 (P-MR-125). **Reference file**: `references/2026-08-18-0330-cron-pitfall-253-extreme-cap-floor-collapse.md` for full 03:30 BJT reproduction recipe + same-day counter carry trajectory + 13-rank Stage 2 truncation note.

## Critical cron pitfalls (READ FIRST before running /tmp/ai_trader_scan.py)

For P-MR-90 through P-MR-167, see the historical pitfall log at the bottom of this file. The newest pitfalls capture patterns from this morning's 0-fill scan (P-MR-168–171) AND tonight's multi-trade scan (P-MR-172–177):

- **P-MR-168 (2026-07-27 03:00)**: `🔴 5% 止蝕 <SYM> qty=...` prefix was silently dropped from a global `re.findall` over scan stdout, causing **$9.6k MV undercount** (CIFR $7,130.20 + CORZ $2,479.75). Fix: **per-line** matcher with non-greedy label capture. Always cross-check `len(parsed) == [rebuild] API 持倉 N 隻`.
- **P-MR-169 (2026-07-27 03:00)**: Non-held Stage 2 candidates need a `⭐5` fallback for `current_price` lookup (SMCI was buy-rejected by broker → not in API table → MV missing).
- **P-MR-170 (2026-07-27 03:30)**: Pure 0-fill scan still requires FIFO recompute + per-line API parser, NOT a `Notes updated` shortcut. Decompose drift as `Σ(qty × (fresh − snapshot))`; quote all three totals.
- **P-MR-171 (2026-07-27 03:30)**: `BUY <SYM> 失敗: HTTP Error 400: Bad Request` is a separate block class — **Type X broker-side reject** — distinct from Type A/B/C/D ("didn't attempt" semantics).
- **P-MR-172 (2026-07-27 22:00)**: When 2 SL + 1 BUY fire in the same scan, the **stdout API list is the pre-trade shell** (still shows CIFR/CORZ with full qty). Always use `fifo_open_positions` (post-trade truth) for MV, and compute `post_trade_cash = pre_cash + Σ(sell) - Σ(buy)` from `trades_log[pre_scan_len:]` (P-MR-170 authoritative).
- **P-MR-173 (2026-07-27 22:00)**: `git diff --cached --check` rejects `new blank line at EOF` after the cron-report `write_text` append. Fix: `Path.write_text(s.rstrip('\n') + '\n')` before `git add`. Always confirm `git log -1 --oneline` shows the new commit.
- **P-MR-174 (2026-07-27 22:00)**: `git -c credential.helper=... push` triggers Hermes `[approval_required] shell command via -c/-lc flag` — a separate `pattern_key` from P-MR-166's `-C` trigger. Fix: `cd <repo> && git config credential.helper <path>` then plain `git push`.
- **P-MR-175 (2026-07-27 22:00)**: When `GIT_TERMINAL_PROMPT=0` blocks the credential helper, OR the helper returns PAT rejection (P-MR-9), the push fails with `Invalid username or token`. There is no further Hermes-side workaround; report as non-fatal warning. Local clean-clone is the durable record.
- **P-MR-176 (2026-07-27 22:00)**: `tp1_state.json` can contain dict-valued entries for fully-closed symbols (`status: 'FULLY_CLOSED'`, closure audit trail). Defensive audit must use `isinstance(v, bool)`, NOT naive truthiness — a dict is falsy and would silently un-fire TP1.
- **P-MR-177 (2026-07-27 22:00)**: Clean textbook case of P-MR-126/131 simultaneous sell+buy lag netting: 2 SL + 1 BUY in same scan → Notes↔FIFO drift +$8.06 (trustable per P-MR-142). The lag shells (CIFR/CORZ in API, SMCI in FIFO) net out because post-trade cash already reflects the deltas.
- **P-MR-178 (2026-07-27 23:00)**: **Actual-fill modeled cash vs rounded trade-log cash arithmetic — pick one and make both lines consistent.** Trade-log entries store the strategy price (e.g. `30 × $154.05 = $4,621.50`), but the broker's response dict exposes the actual fill price (e.g. `30 × $154.0500030517578 = $4,621.50`). These are usually identical to the cent, but `pre_cash + sell - buy` from `trades_log[pre_scan_len:]` rounds to one digit different from the API-fill model. **Recipe**: when computing `post_trade_cash`, capture actual fill prices from the broker response dicts in scan stdout (regex `'symbol': 'X', 'price': ([0-9.]+)`) and use that for the model; report BOTH `rounded-log cash $X` and `actual-fill modeled cash $Y` in the cron report, with FIFO total computed against the actual-fill model. **Anti-pattern**: quoting `fifo_total = 125.43 + 92811.82 = 92937.25` when the modeled cash is actually `126.58` (rounded to two decimals from `9415.92 + 29.01 - (30*154.050003 + 162*28.992901)`); the 1-cent inconsistency will trigger operator audit questions.
- **P-MR-179 (2026-07-27 23:00)**: **Inter-scan cash drift with no trades_log event is broker-side adjustment, NOT a scan bug.** Observed: 22:00 post $9,421.24 → 23:00 pre $9,415.92 = -$5.32 with no intervening trades. **Rule**: log this as a watch footnote, do not flag as scan.py or cron failure. The broker can mark-to-market cash between scans (realized gain/loss on stale lots, fee adjustments, settlement entries). Always compare `last_cron_post_cash` to `this_cron_pre_cash` and document any delta in the cash-trajectory line. **Diagnostic**: if the drift exceeds $10 repeatedly across multiple consecutive crons, escalate to broker-side inquiry; if it's a one-off micro-adjustment, note and move on.
- **P-MR-180 (2026-07-27 23:00)**: **`current_price=null` on broker API for fresh lots persists across multiple re-queries within the same cron.** Observed: after `BUY AVAV 30 @ $154.05` and `BUY SMCI 162 @ $29.00`, two separate API re-queries (`/api/positions` and `/api/claw/agents/me`) both reported `current_price=null` for AVAV, SMCI, AND ONDS (which had been purchased ~2 days earlier). **Rule**: when computing FIFO MV for fresh-lot symbols (especially across multiple re-queries during a single cron), always source the price from `scan.py` stdout `⭐5` line OR `trades_log` entry price. Never trust `null` as `$0` — this would zero-out a fresh lot's MV and create a $4k+ phantom drift. **Diagnostic**: if a newly-purchased symbol's MV appears $0 in your FIFO recompute but a non-zero price is shown in scan stdout, you have a P-MR-180 case; override the null with the stdout/entry price.
- **P-MR-181 (2026-07-27 23:00)**: **Multi-commit iteration pattern within a single cron for staged corrections is normal and tracked cleanly via clean-clone local-only.** Observed sequence: `cb578de` (initial commit, 85 ins/27 del) → `d208816` (correct FIFO audit text, 2 ins/2 del) → `fe1a6cc` (rewrite holdings table to FIFO truth, 28 ins/25 del) → `e89bb86` (final consistency corrections, 2 ins/2 del) → `02c9102` (cash-floor counter corrected, 1 ins/1 del). Each commit is small, atomic, and self-describing. **Rule**: when a cron produces a complex report that requires multiple corrections before delivery, commit each correction as a separate atomic commit rather than trying to get it all right in one shot. The local clean-clone path (P-MR-151) tolerates this pattern; the consolidated GitHub push will eventually land all 5 commits as one update. **Anti-pattern**: trying to amend or squash commits to look "clean" — this loses the audit trail and makes per-correction bisection harder if a future cron needs to reference exactly which correction fixed which issue.
- **P-MR-182 (2026-07-27 23:00)**: **Cash-at-floor counter reset semantics — does NOT reset on normal-sized buy under $1,000 threshold.** P-MR-129 documented reset on `post_trade_cash > $1000`. A normal-sized buy (e.g. $9,318 deployment) that drops post-cash to $126 does NOT reset the counter to 0. Counter increments to 1 (post-trade cash < $100 floor). **Rule**: only `sell > $1000` OR `normal-sized buy that produces post-trade cash > $1000` resets to 0. Micro-buys and saturation-buy-cliffs keep the counter elevated. **Diagnostic**: if you see `cash-at-floor counter: 1` after a buy fired, the buy did NOT clear the counter — saturation is still active, even if `zero-trigger counter` was reset by the buy itself.
- **P-MR-183 (2026-07-28 01:00)**: **`$1k+` scan-printed-vs-FIFO drift even with PERFECT API↔FIFO recon (23=23, no lag) is scan.py internal stale-quote rounding, not broker lag.** Observed: scan-printed MV $91,853.97 vs FIFO-recompute MV $92,869.04 = +$1,015.06 drift with `only_in_api: ∅`, `only_in_fifo: ∅`, every qty identical. P-MR-97/103 documented ~$1-2k as "normal" and $2k+ as "normal even with perfect recon"; P-MR-183 elevates this — the drift is now seen to be EXACTLY the magnitude of fresh-yfinance vs broker-snapshot stale quotes, NOT broker reconcile lag. **Recipe**: when `len(api) == len(fifo) && all qty match && drift > $500`, decompose drift as `Σ(qty × (yfinance_quote − scan_quote))` to confirm stale-quote vs lag. **Anti-pattern**: attributing the $1k drift to "broker reconcile lag" when API and FIFO perfectly match — there is no lag to reconcile. Always quote drift, but don't call it "lag" without evidence.
- **P-MR-184 (2026-07-28 01:00)**: **Full-closure retry arc validation — broker-rejected SL orders from prior cron (P-MR-171) may succeed on a subsequent cron.** The 03:30 cron had 3 broker HTTP 400 rejects (CIFR SELL, CORZ SELL, SMCI BUY). At 22:00, CIFR SELL 308 @ $22.85 and CORZ SELL 109 @ $21.69 fired SUCCESSFULLY (the CIFR/CORZ retry path), completing the FULL_CLOSURE of both positions. By 01:00 (2026-07-28) the FIFO shows CIFR open=0 (buys=429, sells=429) and CORZ open=0 (buys=109, sells=109), and the scan stdout API view no longer lists either symbol. **Rule**: when a broker-rejected SL appears in a prior cron's report, the next cron may show it fired successfully — always re-check `fifo_open_positions` and per-line API view to detect full-closure events that span crons. **Diagnostic**: if a previously-rejected SL symbol is MISSING from the API view but its TP1 state is still True/dict, full closure happened; update the AI-Trader.md 持倉 table to remove the symbol and add a closure life-cycle note.
- **P-MR-185 (2026-07-28 01:00)**: **Day-boundary counter reset validation — works correctly across long gaps.** The 2026-07-27 day's last cron was 03:30 (zero-trigger counter=3, cash-at-floor counter=3). The 2026-07-28 01:00 cron (next cron ~21.5h later) reset both counters per P-MR-155/156: zero-trigger=1 (this is the new day's first cron), cash-at-floor=0 (cash $117.23 > $100 → P-MR-125/129 reset). **Rule**: day-boundary reset semantics are NOT time-dependent — they trigger whenever the prior cron's BJT date ≠ this cron's BJT date, regardless of how many hours elapsed. **Diagnostic**: when a cron is the first of a new BJT day, ALWAYS expect counter=1 (zero-trigger) and counter=0 (cash-at-floor, IF cash > $100); do NOT carry over stale counters.
- **P-MR-186 (2026-07-28 03:30)**: **KTOS RTH-close micro-buy squeeze-through at deep saturation; fresh-clone recovery is timestamp-fresh, not slot-reused.** (1) The previously-documented clean-clone at `/tmp/_vivian_notes_clean` ALSO accumulated corruption by 03:30 — committing produced `fatal: unable to read bab66737b9d8866c8dfacede2b07c791c5e7aba8`. Even `git reset HEAD -- file; git add file; git commit` failed with the same missing-blob error. **Fix**: do NOT reuse the clean-clone path; instead, each cron must build a fresh-timestamp directory (`/tmp/__notes_cron__`, `/tmp/__notes_03_30__`, etc.), `git init -b main` inside it, copy AI-Trader.md, `git add`, `git commit`. The risk of corruption accumulation makes slot-reuse incorrect advice. (2) **Comma-formatted number regex bug**: a sloppy `\$([\d.]+)` group on `"持倉市值: $91,853.97"` captures `"853.97"` (drops the leading "91"). Total goes $91,000 low. **Fix**: `\$([\d,.]+)` and ALWAYS `float(s.replace(',',''))` before arithmetic. (3) **KTOS lifecycle reached cycle 5**: 11 trade entries in trades_log, all closed then re-opened, now accumulated qty=2 across two adjacent lots (`$49.34` + `$49.32`). When a symbol has been fully-closed-and-reopened multiple times, FIFO recompute MUST aggregate post-buy lot avg, and API-MV line will show the pre-buy shell (1 lot) creating the textbook P-MR-180 buy-side lag fingerprint. (4) **Cash-at-floor counter empirically confirmed escalating**: cash $117.23 → micro-buy $49.32 → post-cash $67.88. Counter went 0 → 1 per P-MR-182 (micro-buy under $1000 threshold does not reset). This is the third empirical observation of the P-MR-182 rule.

Reproduction recipes and cron-report templates:
- 0-fill scan: `references/2026-07-27-0300-cron-pitfall-168-171-stdout-prefix-regex-stage2-fallback-fifo-recompute-broker-reject.md`
- Multi-trade scan: `references/2026-07-27-2200-cron-pitfall-172-177-sell-buy-same-scan-post-trade-cash-eof-push-credential-schema.md`
- Multi-trade + 2 buys same scan: `references/2026-07-27-2300-cron-pitfall-178-182-actual-fill-cash-inter-scan-drift-null-fresh-price-multi-commit.md`
- Day-boundary + full-closure retry arc (NEW): `references/2026-07-28-0100-cron-pitfall-183-185-stale-quote-drift-full-closure-arc-day-boundary-reset.md`
- 4th-scan saturation extension: `references/2026-07-28-0300-cron-pitfall-186-saturation-4th-scan-pm-183-pm-185-revalidation.md` — clean textbook 0-trigger at deep saturation; revalidates P-MR-155/168/176/183/185 in one run; documents reproduction recipe + cron-report 10-block template.
- 5th-scan KTOS squeeze-through: `references/2026-07-28-0330-cron-pitfall-186-ktos-micro-buy-fresh-clone-recovery-comma-regex.md` — RTH-close micro-buy fires through cap-block; documents the fresh-init recovery (slot-reuse is wrong), the comma regex bug, and KTOS cycle-5 lifecycle aggregation.
- Partial-saturation squeeze + tee-stdout recipe (NEW): `references/2026-07-28-2200-cron-pitfall-187-tee-stdout-recipe-partial-saturation-squeeze.md` — 2 SL + 1 TP1 + 1 BUY same scan; cash $70→$6,804→$70; documents the tee-stdout invariant (re-run safe) and the partial-saturation squeeze classification (hybrid Type A+B, distinct from P-MR-127).
- Pure 0-trigger saturation with P-MR-188 f-string trap (NEW): `references/2026-07-28-2300-cron-pitfall-188-191-fstring-brace-trap-hybrid-ab-pm180-1h-window.md` — 0 buy/SL/TP, 2 Stage 2 candidates (ONDS held-cap, CSCO new-cash), 23=23 perfect recon; documents the P-MR-188 f-string crash with `{FUTU}` in markdown, the Hybrid A+B classification pattern (P-MR-189), the same-day counter carry-forward + P-MR-180 1h reconcile validation (P-MR-190), and P-MR-191 inter-scan $6.74 drift watch footnote.
- Day-boundary reset + micro-buy in same cron (NEW): `references/2026-07-29-0100-cron-pitfall-192-day-boundary-microbuy-counter-step-by-step.md` — 1 BUY SMCI 1 @ $28.55 at deep saturation, day-boundary reset of cf 2→0, then micro-buy brings cf back to 1; documents P-MR-192 day-boundary + micro-buy counter arithmetic (reset FIRST, trade effects SECOND), P-MR-190 2nd 1h reconcile confirmation (SMCI 01→02 prediction), 24-vs-23 API↔FIFO 1h buy-lag fingerprint.
- **4-trigger scan with full-saturation-break (NEW)**: `references/2026-07-29-2300-cron-pitfall-194-198-hybrid-abx-full-saturation-break-notes-trustable.md` — 2 SL + 1 BUY success + 1 Type X reject + 1 cap-block in same scan; documents `Hybrid A+B+X saturation` classification (P-MR-194, 4-trigger-type distinct from P-MR-189's 2-trigger-type), full-saturation-break after partial-saturation-squeeze (P-MR-195, $2,315 post-cash normal-sized buy vs P-MR-187b micro-squeeze), P-MR-190 1h reconcile window empirically validated for FUTU 22:00→23:00, P-MR-197 f-string `.format()` `{{...}}` brace-escape recipe with working code example, P-MR-198 cleanest Notes↔FIFO $3.99 drift trustable per P-MR-142.
- **Hybrid A+B 4th sub-pattern with 1 micro-buy squeeze-through at cash-floor (NEW)**: `references/2026-07-31-0100-cron-pitfall-203-hybrid-ab-microbuy-cleanest-textbook-decomposition.md` — 1 BUY CIFR 2 @ $22.68 (highest RR=2.5, lowest unit-price), 3 cap-block (PATH/BA/AVGO), 1 cash-block (LMT), 0 SL/TP/reject; documents Hybrid A+B sub-pattern distinction matrix (P-MR-187b vs P-MR-189 vs P-MR-194 vs P-MR-195 vs P-MR-203), full P-MR-200 5-step decomposition on a clean 1-Trigger scan (+$2,269.94 PURE stale-quote, $45.36 buy-lag lift, -$45.36 cash deployment), P-MR-192 day-boundary + micro-buy arithmetic validated for 5th time (prior cf=1 → day-boundary reset to 0 → micro-buy → +1 → cf=1 FINAL), P-MR-198 smallest with-trades Notes↔FIFO drift -$27.47 NEUTRAL ever.
- **Hybrid A+B 4th sub-pattern with 1 micro-buy squeeze-through at cash-floor (NEW)**: `references/2026-07-31-0100-cron-pitfall-203-hybrid-ab-microbuy-cleanest-textbook-decomposition.md` — 1 BUY CIFR 2 @ $22.68 (highest RR=2.5, lowest unit-price), 3 cap-block (PATH/BA/AVGO), 1 cash-block (LMT), 0 SL/TP/reject; documents Hybrid A+B sub-pattern distinction matrix (P-MR-187b vs P-MR-189 vs P-MR-194 vs P-MR-195 vs P-MR-203), full P-MR-200 5-step decomposition on a clean 1-Trigger scan (+$2,269.94 PURE stale-quote, $45.36 buy-lag lift, -$45.36 cash deployment), P-MR-192 day-boundary + micro-buy arithmetic validated for 5th time (prior cf=1 → day-boundary reset to 0 → micro-buy → +1 → cf=1 FINAL), P-MR-198 smallest with-trades Notes↔FIFO drift -$27.47 NEUTRAL ever.
- **5-candidate Hybrid A+B 0-trigger saturation with P-MR-204 execute_code heredoc-parser pitfall + P-MR-205 multi-cap collapse + P-MR-206 0-trade Notes-canonical $7.97 + P-MR-207 same-day increment (NEW)**: `references/2026-07-31-0300-cron-pitfall-204-207-hybrid-ab-multi-cap-0trigger-heredoc-parser-zero-trade-canonical.md`
- **P-MR-226/227 (2026-08-04 03:30) — heredoc em-dash security-scan block + 5th-run same-BJT-day carry-forward validation + cleanest 0-trade canonical drift (NEW)**: `references/2026-08-04-0330-cron-pitfall-226-227-heredoc-security-block-5th-run-canonical.md` — documents the `[approval_required] pattern_key: tirith:variation_selector` block on heredoc bodies with `—` (recipe: write_file then `python3`); 03:30 0-trade cron with zt 1→2 cf 2→3 (P-MR-201 5th validation) and Notes↔FIFO drift +$2.81 (new smallest 0-trade canonical ever); P-MR-214 identity exact hit on 30 positions × ~$180 avg = $5,331.85 pure stale-quote.
- **Hybrid X+D 0-fill + API↔FIFO identity shortcut + 72h day-boundary reset (NEW)**: `references/2026-08-03-0100-cron-pitfall-213-215-hybrid-xd-api-fifo-identity-72h-day-boundary.md` — cleanest possible 0-fill scan (2 Type X CIFR/ORCL + 3 Type D SMCI/ONDS/BIDU); documents P-MR-213 Hybrid X+D 9th distinct sub-pattern, P-MR-214 `sum_api == fifo_mv` identity shortcut for collapsing the P-MR-200 5-step decomposition when API=FIFO perfectly, P-MR-215 binary day-boundary reset validated across a 72h gap (3 calendar days).
- **P-MR-216 Level 3 escalation + RTH-close 0-trade scan (NEW)**: `references/2026-08-03-0330-cron-pitfall-216-escalation-level3-rth-close-scan-no-trade.md` — 3rd consecutive same-BJT-day CIFR/ORCL HTTP 400 reject (upgrade from P-MR-216 level 2 to level 3 structural signal); documents P-MR-217 RTH-close validation (0-trade end-of-day scan is healthy steady-state), P-MR-218 distinction between P-MR-184 retry-success and P-MR-216 retry-persist patterns, P-MR-219 0-trade 3-cron consecutive Type X pattern (distinct from P-MR-120 broad saturation).
- **Type X retry-arc persistence — 2x Hybrid X+D same-pair repeat (NEW)**: `references/2026-08-03-0300-cron-pitfall-216-type-x-retry-arc-persistence-hybrid-xd-2x.md` — 2nd consecutive same-BJT-day occurrence of CIFR + ORCL HTTP 400 reject; documents P-MR-216 escalation levels (1st=transient per P-MR-213, 2nd=schema/validation diagnosis hint, 3rd=structural signal); distinguishes from P-MR-184 successful retry arc; provides reproduction recipe for diffing Type X rejections across consecutive crons.
- **2nd-rank-RR micro-squeeze with P-MR-208/P-MR-209/P-MR-210 NEW smallest with-trades drift + cross-scan null-persistence + silent-cap-skip (NEW)**: `references/2026-07-31-0330-cron-pitfall-208-210-micro-squeeze-2nd-rank-cross-scan-null-silent-cap-skip.md` — 1 BUY ONDS 3 @ $7.60 ($22.80 micro-deploy); CIFR top-RR cash-blocked, ONDS 2nd-RR deployed; 5 consecutive crons cash < $100 (cf=3 NEW longest streak); documents P-MR-208 2nd-rank-RR micro-squeeze sub-pattern (NEW smallest with-trades drift −$1.29, beat P-MR-198 +$3.99), P-MR-209 cross-scan null-persistence (ONDS null at 23:00 prior day AND after this scan's BUY), P-MR-210 silent-cap-skip for ⭐5 candidates that already exceed cap (CVX/COP/FUTU appeared but no `倉位已達10%上限` print), full Hybrid A+B 7-pattern distinction matrix.
- **P-MR-221/222/223 (2026-08-03 23:00) (NEW)**: `references/2026-08-03-2300-cron-pitfall-221-223-2buy-queue-bypass-cf-floor-during-success-diff-check-semantics.md` — P-MR-221 NEW 2-BUY queue-bypass success (top-2 RR both clear, ranks 3-5 Type D); P-MR-222 correction to P-MR-219 (git diff --cached --check ALWAYS returns non-zero on whitespace, commit still succeeds); P-MR-223 $SQ delisted warning is benign.
- **P-MR-211 (2026-07-31 22:00) — Hybrid A+B+D cash-pool-split triple block**; details in `references/2026-07-31-2200-cron-pitfall-211-hybrid-abd-cash-pool-split.md` and the **`Distinction matrix update (Hybrid A+B sub-patterns now 10+)`** in `references/2026-08-03-2300-cron-pitfall-221-223-2buy-queue-bypass-cf-floor-during-success-diff-check-semantics.md`.

**P-MR-224 (2026-08-04 03:00) — Degenerate Hybrid B pure-cap saturation** (NEW 10th sub-pattern). 0 BUY / 0 SL / 0 TP / 0 Type X; 5 ⭐5 candidates all HELD, all Type B cap-block; top-5 fully consumed by held-cap-block (no Type D queue needed); 7 Stage 2 candidates skipped by top-5 truncation. Cash $13.68 → P-MR-144 cap-floor collapse full effect. **Recipe**: `len(⭐5) ≥ 3` AND every ⭐5 candidate is HELD AND scan printed `倉位已達10%上限` for ALL of them AND cash < $50 → "Degenerate Hybrid B pure-cap saturation". P-MR-214 identity shortcut hit EXACTLY (`sum_api == fifo_mv = $97,989.61`); drift $5,299.95 PURE stale-quote (P-MR-183). Counters: zt 0→1, cf 1→2. P-MR-205 = 4 cap + 1 cash; P-MR-224 = **5 cap + 0 cash** (cash too small for Type A). Full 11-pattern matrix + recipe in `references/2026-08-04-0300-cron-pitfall-224-225-degenerate-hybrid-b-pure-cap-saturation-tp2-observation.md`.

- **P-MR-226 (2026-08-04 03:30) — Hermes security-scan BLOCKS heredocs containing em-dashes / non-ASCII (extended to emoji by P-MR-231). Recipe**: ALWAYS write cron-step scripts via `write_file` (e.g. `/tmp/_git_commit_0330.py`), then run `python3 /path/to/script.py`. Commit messages ASCII-safe (`-` not `—`). Both markdown report bodies AND Telegram message bodies must avoid Unicode variation-selector chars in heredoc context. Diagnostic: `terminal(... python3 - <<'PY' ... PY ...)` returning `status: approval_required` + `pattern_key: tirith:variation_selector` → heredoc body is the culprit. Workaround using python: write content to file via write_file (NOT heredoc), then python3 /tmp/_script.py reads file and runs `subprocess.run([curl, ...])`. (P-MR-231 extends to emoji; P-MR-254 refines for Telegram parse_mode regression.)

- **P-MR-227 (2026-08-04 03:30) — 5th-run same-BJT-day carry-forward + cleanest 0-trade canonical Notes↔FIFO drift.** 03:00 zt=1 cf=2 → 03:30 zt=2 cf=3 (P-MR-201 5th validation). Notes $98,038 − FIFO $98,035.19 = **+$2.81** — new smallest 0-trade Notes↔FIFO drift ever (beat P-MR-198 $3.99 with-trades; P-MR-206 $7.97 0-trade). P-MR-214 identity hit exactly (`sum_api == fifo_mv = $98,021.51`); 100% stale-quote ($5,331.85 P-MR-183). Inter-scan cash drift $0.00 (P-MR-179 trivial). **Recipe**: 5th-run same-BJT-day pure 0-trigger canonical scan with Notes↔FIFO drift <$5 AND P-MR-214 identity AND inter-scan cash drift = $0 → P-MR-227 0-trade TRUST textbook; headline with FIFO. cf=3 is the longest same-day cap-floor streak on this account.
- **P-MR-229 (2026-08-05 03:00) — Pure Type A 5-candidate cash-pool-split saturation — distinct from P-MR-211 (Hybrid A+B+D).** Top-5 ⭐5 = IBM / CIFR / ARM / ALAB / INTC. ALL 5 blocked by cash-pool-split denominator ($32.99 / MAX_STOCKS 2 = $16.50/stock). Every unit price > $16.50 → qty=0. CIFR is HELD but cash-floor collapse means no deploy (held value $21.71 < cap-floor $32.99). No Type B (no held at 10% violation since cash $32.99 below all position values), No Type X (never attempted BUY loop), No Type D (pre-loop filter exhausted). Recipe: `len(⭐5) == 5` AND `all ⭐5 blocked by cash-pool-split` AND `no held at >10% cap` AND `cash < $50` → **Pure Type A 5-cand saturation**. Distinction matrix update: P-MR-211 = Hybrid A+B+D (5c mix: 1c held-cap + 1c cash-block + 2c silent-cap + 1c queue-exhausted) vs **P-MR-229 = Pure Type A (5c all cash-pool-split blocked, NO held-cap, NO queue-exhaustion)**. Counter trajectory (same-BJT-day carry, P-MR-201): prior 01:00 zt=2 cf=1 → 03:00 zt=3 cf=2 (0 BUY → zt+1; cash $32.99 <$100 → cf+1; no reset trigger). Stale-quote drift +$7,444.68 = largest pure stale-quote today (30 positions × ~$110 avg, P-MR-183).
- **P-MR-230 (2026-08-05 03:00) — Refines P-MR-117/198/206/227 0-trade TRUST threshold: drift <$30 still TRUST.** Observed: Notes $100,158.00 vs FIFO $100,144.26 = **+$13.74**. P-MR-198/206/227 documented "$<10 = cleanest", but this 03:00 cron exceeds $10 yet remains within P-MR-117 0-trade tolerance ("Notes ≈ FIFO" with no trades intervening). Rule: **0-trade scans: drift <$30 = TRUST unconditionally; $30-$100 = NEUTRAL (footnote); >$100 = IGNORE (recompute headline)**. Rationale: without trade events, the only drift sources are (a) stale-quote per-position (P-MR-183, large magnitudes common) and (b) inter-scan cash adjustment residual (P-MR-179, typically <$10). Buy-lag and SL-lag are mathematically 0. The Notes-line update lag vs FIFO recompute is bounded by cron-runner precision (~$10 on a 30-position $100k portfolio). Anti-pattern: rejecting a 0-trade TRUST verdict solely because drift >$10 — the <$10 rule was an empirical "cleanest ever" tag, not a hard cut-off. Diagnostic: when a 0-trade scan drifts $10-$30, classify as **0-trade canonical TRUST (P-MR-230)** with drift magnitude footnoted.
- **P-MR-231 (2026-08-05 03:00) — P-MR-226 heredoc security-scan extends to EMOJI variation-selectors, not just em-dashes.** Confirmed: `cat << EOF ... ⏰ ... 📊 ... EOF` (multi-emoji Telegram-style message body) trips `[approval_required] pattern_key: tirith:variation_selector` — same pattern_key as P-MR-226's em-dash case. Recipe UNCHANGED: write cron-step scripts via `write_file` (e.g. `/tmp/_send_tg_0300.py`), then run `python3 /path/to/script.py`. Both heredoc bodies (markdown report) AND Telegram message bodies must avoid Unicode variation-selector chars in heredoc context. Diagnostic: any heredoc-embedded `⏰` `📊` `🎯` `🚫` `🔢` `💰` `📈` `🏆` `⚠️` `✅` `💼` `🔴` `🟢` `🟡` `⭐` will trip the block. Workaround using python: write content to file via write_file (NOT heredoc), then python3 /tmp/_script.py reads file and runs `subprocess.run([curl, ...])`. Distinction from P-MR-99 (cat|python, curl|python blocked): P-MR-99 blocks pipe-chars; P-MR-231 blocks heredoc body Unicode chars. Same fix family (write_file-then-run).
- **P-MR-228 (2026-08-04 22:02) — Cleanest 1-trigger scan ever: 1 SL + 0 BUY + Notes↔FIFO drift $0.96 (NEW record, beat P-MR-198/206/227).** Full reproduction recipe + 5-step decomposition + counter cf=4 longest-streak context in `references/2026-08-04-2200-cron-pitfall-228-cleanest-1-trigger-scan-new-drift-record.md`. Hybrid A+B 0-trigger (P-MR-211 cash-pool-split blocks all 4 non-held ⭐5; IREN held-cap blocks the 5th). CIFR SL qty=2 @ $20.83 = $41.66 cash credit lifts cash $13.68→$55.34 (still <$100 floor → cf=4). zt 2→3 (P-MR-110). Same-BJT-day carry (P-MR-201). **Why so clean**: 1 trigger → 1 lag shell; CIFR sell-side lag exact-match in FIFO MV recompute (subtract just-sold from API sum); pure stale-quote $2,963 (P-MR-183) is the only non-zero drift component; Notes ↔ FIFO $0.96 unconditional TRUST (P-MR-117/142/206 with-trades). **Diagnostic**: if a 1-trigger scan drifts >$5, recheck the FIFO-MV-minus-just-sold recipe (P-MR-228 checklist in reference file). **Pattern signature for future classification**: 1 SL + 0 BUY + 4+ Type A (cash-pool-split) + 1 Type B (held-cap) + drift <$5 → **P-MR-228**.

- **P-MR-229 (2026-08-05 03:00)**: Pure Type A 5-candidate cash-pool-split saturation (NEW 11th Hybrid A+B-family sub-pattern). See reference file.
- **P-MR-229 same-BJT-day 2nd occurrence validation (2026-08-05 22:00)**: First observed P-MR-229 same-day repeat; top-5 = IBM/CIFR/ARM/ALAB/INTC all cash-pool-split blocked. See reference.
- **P-MR-230 (2026-08-05 03:00)**: Refines 0-trade canonical TRUST threshold: drift <$30 still TRUST. Rule: 0-trade scans: **<$30 = TRUST unconditionally** (Notes headline, FIFO footnote); **$30-$100 = NEUTRAL** (cite both); **>$100 = IGNORE** (recompute headline from FIFO). Anti-pattern: rejecting TRUST verdict solely because drift >$10.
- **P-MR-231 (2026-08-05 03:00)**: Heredoc variation-selector security-block extends to EMOJI bodies. Recipe UNCHANGED from P-MR-226: write cron-step content to `/tmp/_file.md` via `write_file`, run via `python3 /tmp/_script.py`. Both markdown report bodies AND Telegram message bodies must avoid Unicode variation-selector chars in heredoc context.
- **P-MR-232 (2026-08-05 03:30)**: Bash `$VAR` interpolation inside `terminal -c "..."` breaks Python regex code. NEVER pass multi-line Python with regex `$` patterns through `terminal -c`. ALWAYS write the script to `/tmp/_script.py` via `write_file`.
- **P-MR-233 (2026-08-05 03:30)**: `fifo_pnl.py` canonical import path is `~/.hermes/skills/data-science/stock-analysis/scripts/fifo_pnl.py` — `/tmp/fifo_pnl.py` does NOT exist. When writing cron-step scripts, ALWAYS prepend `sys.path.insert(0, '/home/vivian/.hermes/skills/data-science/stock-analysis/scripts')`.
- **P-MR-234 (2026-08-05 03:30)**: `trades_log` entries use `action/symbol/price/qty/content` fields; only 23/238 have `executed_at`. Use action/content keys, not timestamp keys.
- **P-MR-235 (2026-08-06 01:00)**: TP1-partial Notes-table qty lag (NEW). See `references/2026-08-06-0100-cron-pitfall-235-tp1-partial-notes-qty-lag.md`.
- **Hybrid A+B+X with held-symbol cap-bypass BUY (NEW)**: `references/2026-07-30-0330-cron-pitfall-199-201-hybrid-abx-cap-bypass-drift-decomposition-day-end-counter.md` — 1 BUY CSCO +19 (held symbol at 10% cap, pre-buy value at threshold so scan allowed through), 1 Type X LOW reject, 2 cap-block DE+COP; documents P-MR-199 HELD-symbol cap-bypass edge case (distinct from P-MR-124), P-MR-200 drift decomposition diagnostic (stale-quote vs buy-lag, $1,117.50 decomposes to PURE stale-quote, NOT buy-lag), P-MR-201 03:30 RTH pre-close 5-scan same-day sequence counter carry-forward validation.

- **P-MR-193 (2026-07-29 03:30)**: **`fifo_realized(trades, scope=...)` must replay pre-scope SELLS, not only preload all buys.** The old helper loaded every historical buy first, then matched only scoped sells; this let already-consumed ancient lots reappear in session P&L and changed the 2026-07-28/29 session result from the correct **+$147.53** to **−$212.37**. Fix: walk the complete log chronologically; every sell consumes FIFO lots, but append P&L rows only when that sell belongs to `scope`. `session_realized_pnl(log, n)` now inherits this corrected behavior. **Diagnostic**: for the last four events `SELL SMCI, SELL DHR, BUY FUTU, BUY SMCI`, scoped P&L must equal `−$262.44 + $409.97 = +$147.53`.
- **P-MR-194 (2026-07-29 23:00)**: **Four-trigger scan classification: `Hybrid A+B+X saturation` — when Stage 2 has 4 candidates across all block types (BUY-success + cap-block + broker-reject + implicit-cash-block), enumerate each separately rather than collapsing to one type.** The 23:00 BJT cron saw 4 Stage 2 candidates: DE = Type B (held, cap), CVX = success, LOW = Type X (HTTP 400), CSCO = Type A implicit (deferred due to CVX higher RR rank). All four classifications are distinct and each is a real signal. **Recipe**: list each candidate with its own type line in the cron report; do NOT pick a single label. **Distinction from P-MR-189 Hybrid A+B**: P-MR-189 was 2 candidates of 2 types (held-cap + new-cash); P-MR-194 is 4 candidates of 4 types. **Diagnostic**: when `Stage 2 候選: N 只` with N ≥ 3 and triggers span success + cap + reject + implicit-cash, classify as "Hybrid A+B+X" (or extend the type label to match the mix). The KEY insight is the broker-reject (Type X) — without Type X the scan would be "Hybrid A+B with success", and the cron report would miss the retry-arc implication (P-MR-184).
- **P-MR-195 (2026-07-29 23:00)**: **Saturation break after partial-saturation squeeze — distinct from P-MR-187(b) "partial-saturation squeeze" pattern.** Sequence: 22:00 cron = P-MR-187(b) partial-saturation squeeze (cash $34 → $4,122 → $4,122 net of FUTU micro-buy; cf=1); 23:00 cron = full-saturation-break (cash $2,315.27 post CVX $1,922 buy; cf=0, zt=0). The 23:00 buy is a NORMAL-sized buy (>$1k), NOT a micro-buy squeeze. **Recipe**: classify as "full-saturation-break" when post-trade cash > $100 floor AND the buy was >$1k (not micro). Counter behavior: zt resets per P-MR-110 (buy fired), cf stays 0 per P-MR-129 (post-cash > $100). **Diagnostic**: when consecutive crons show the trajectory `$XX → $4k+ spike → $2k+ steady`, that's full-saturation-break (P-MR-195), distinct from `$XX → $4k+ spike → <$100 cliff` which is partial-saturation squeeze (P-MR-187b).
- **P-MR-196 (2026-07-29 23:00)**: **P-MR-190 1h reconcile window empirically validated — predict-then-confirm protocol.** 22:00 cron noted `only_in_fifo = {FUTU}` for the 22:00 FUTU micro-buy. 23:00 cron (1h later) showed FUTU at API qty=67, FIFO qty=67 MATCH. This confirms the P-MR-180 1h broker reconcile window for fresh-lot symbols. **Recipe for future crons**: when a cron observes `only_in_fifo: {X}`, write a prediction in the cron report: "next cron will reconcile X into API view at qty matching FIFO". If next cron does NOT reconcile (X stays only_in_fifo), escalate as broker-side lag persistence. **Diagnostic**: the prediction is per-fresh-lot; multiple fresh lots in the same scan may have different reconcile speeds (the SL+BUY same-scan lag shell `only_in_api` typically reconciles within 1 cron, the BUY lag shell `only_in_fifo` typically reconciles within 1 cron; both windows validated empirically now).
- **P-MR-197 (2026-07-29 23:00)**: **P-MR-188 f-string brace trap WORKAROUND via `.format()` with `{{...}}` escapes is the recipe.** P-MR-188 documented the trap: literal `{FUTU}` inside an f-string evaluates `FUTU` as a name and crashes with `NameError`. The 23:00 cron successfully avoided this by using `.format(date=..., time=..., ...)` with `{{FUTU}}` → `{FUTU}` literal. **Recipe**: ALWAYS use plain triple-quoted strings (`"""..."""`) for cron-report markdown templates. If interpolation is needed, use `.format(key=value, ...)` with `{{` and `}}` for literal braces (Python's `.format()` interprets `{{` as literal `{` and `}}` as literal `}`). Example working pattern:
    ```python
    report = """## ⏰ {date} {time} BJT
    - only_in_api = {{SMCI, KTOS}}
    - only_in_fifo = {{CVX}}
    """.format(date="2026-07-29", time="23:00")
    # Output:
    # ## ⏰ 2026-07-29 23:00 BJT
    # - only_in_api = {SMCI, KTOS}
    # - only_in_fifo = {CVX}
    ```
- **P-MR-198 (2026-07-29 23:00)**: **Notes↔FIFO drift $3.99 trustable per P-MR-142 — cleanest textbook case yet.** 23:00 cron: scan-printed MV $88,424.14 vs FIFO MV $90,977.74 = +$2,553.60 (large; P-MR-183 stale-quote drift); scan-printed Total $92,542.64 vs FIFO Total $93,293.01 = +$750.37 (smaller); **Notes updated $93,297.00 vs FIFO Total $93,293.01 = +$3.99** → trustable. The $3.99 drift is the residual after (a) cash flush from broker processing 2 SLs + 1 BUY and (b) position-table update lag for CVX. **Recipe**: when scan-printed Total vs FIFO Total drift > $500 but Notes updated vs FIFO Total drift < $10, classify as "Notes trustable" and use Notes as the headline number in the cron report's 帳戶 block, with FIFO as the audit-truth footnote. This is the cleanest empirical case for P-MR-142's "Notes can be ALMOST EXACTLY equal to FIFO recompute in with-trades scans when broker cash has flushed but positions table lags" rule.
- **P-MR-199 (2026-07-30 03:30)**: **Hybrid A+B+X with HELD-symbol BUY-success bypassing its own cap-block — when a held symbol appears in Stage 2 AND its cap-block hasn't fired yet (because cap is computed pre-buy), the scan can both pass the candidate AND add to the held position.** Observed: CSCO held qty=10 (10% of total = HIT cap). 03:30 Stage 2 listed CSCO $114.30, but the explicit cap-block print was **NOT** emitted for CSCO because the scan re-evaluated cap on its pre-buy position-value (which was at exactly the threshold but not over). The buy fired +19股 (10→29). DE and COP also held at 10% cap and DID emit explicit cap-block prints. **Recipe**: when a held symbol is in Stage 2 candidates and the BUY-success fires, verify the cap-block was evaluated at PRE-trade position-value (scan-side), not at POST-trade. If the held symbol's pre-buy position is exactly at 9.9%-10.0% cap, scan may allow it through; if it's >10.0%, scan emits the explicit cap-block skipped line. **Diagnostic**: when an account has 21+ positions and an add-on BUY fires, check the held symbol's pre-buy position-value vs `total_value × 10%`. If pre-buy < 10% but post-buy > 10%, it's a "cap-bypass" — the scan allowed it because the rule fires at scan-time, not at fill-time. Future scan will see the higher position and emit cap-block. **Distinction from P-MR-124**: P-MR-124 documents held symbols appearing in Stage 2 candidates and cap-blocking correctly; P-MR-199 documents the EDGE case where the cap-block is missed because the pre-buy position is at exactly the threshold.
- **P-MR-200 (2026-07-30 03:30)**: **Drift decomposition diagnostic — when both `Σ(qty × Δprice)` (stale-quote) and `Σ(Δqty × price)` (buy-lag) contribute, the math is non-obvious and easy to misattribute.** Observed: drift FIFO Total $92,992.55 − scan Total $91,875.05 = +$1,117.50. Initial guess: "this is buy-lag, FIFO has CSCO qty=29 vs API qty=10 → +$2,171.70 MV lift, but cash accounted cash deployment $2,171.70 → net ~$0". But the $1,117.50 is NOT buy-lag — it's stale-quote (P-MR-183) because scan's MV uses a different per-position quote source than what stdout 現價=$ shows. The buy-lag cancellation is exact ONLY if you use the same price source for both. **Recipe (decomposition step-by-step)**: 
    1. Compute `sum_api = Σ(qty_api × price)` from per-line stdout parser. Compare to scan-printed MV. Drift = `sum_api − scan_MV` → pure stale-quote (P-MR-183).
    2. Compute `sum_api + pre_cash` → "scan-equivalent Total". Compare to scan-printed Total. Should match closely.
    3. Compute FIFO Total = `post_cash + Σ(qty_fifo × price)` where `qty_fifo` uses overrides for buy-lag symbols (see P-MR-149/180/192).
    4. Compare FIFO Total vs scan-equivalent Total. Drift = (qty overrides value) + (cash adjustment) + (price stale-quote residual).
    5. Cite each component separately in the cron report. **Anti-pattern**: attributing the entire $1,117.50 to "buy-lag" when the actual buy-lag component is exactly $0 (lifts and deployments cancel). Always decompose; the surprise is usually the stale-quote magnitude.
- **P-MR-201 (2026-07-30 03:30)**: **Day-end cron (03:30 RTH pre-close) — 5-scan same-day sequence validates P-MR-190 counter carry-forward across multiple same-BJT-day crons.** All 5 crons on 2026-07-30 BJT (22:00 / 23:00 / 01:00 / 03:00 / 03:30): counters carried forward correctly across the sequence with no day-boundary resets. Counter trajectory: 22:00 zt=0 cf=1 → 23:00 zt=1 cf=2 → 01:00 zt=0 cf=0 (day-boundary reset, then BUY resets cf to 0) → 03:00 zt=0 cf=0 → 03:30 zt=0 cf=0. **Rule**: when the cron is on the SAME BJT day as the immediately preceding cron, all counters carry forward exactly. The 03:30 is the LAST US RTH cron (16:00 EST = 04:00 BJT closed); the next cron would be 04:00 BJT post-close paper-mode. **Diagnostic**: when running a 03:30 cron, always check `last_cron_bjt_date == this_cron_bjt_date`; if yes, no reset; if no, apply P-MR-155/192 day-boundary reset. The 03:30 cron is NOT a day-boundary case (still 2026-07-30 BJT) — do not falsely apply reset logic. **Anti-pattern**: reflexively applying day-boundary reset on every 03:30 cron because "it's late at night" — only reset when the BJT date changes.
- **P-MR-202 (2026-07-30 23:00)**: **Prior-cron MD-skip anomaly — trades_log + state files updated but AI-Trader.md cron section never appended.** Detected when comparing `trades_log` entries (PATH +100 @ $11.91 cycle 4, BA +5 @ $218.68 from 22:00 cron) vs last MD section timestamp (03:30 from 2026-07-30, missing 22:00 entirely). The 22:00 cron ran scan.py successfully, fired 2 BUYs, updated trades_log/tp*_state.json, but **silently skipped the MD write step** — likely a `write_text` exception that was caught by an outer try/except, or a terminal/Pager interruption during the markdown append. **Rule**: at the start of EVERY cron, BEFORE writing the new section, query `trades_log[last_md_section_idx:]`; if `len > 0` but the corresponding time window has no MD `## ⏰ <date> <time> BJT` section, the prior cron's MD write failed — back-fill it now OR explicitly document the skip in this cron's report. **Diagnostic recipe**: 
  ```python
  md = Path('/tmp/vivian-notes/AI-Trader.md').read_text()
  last_section_idx = max(re.finditer(r'## ⏰ (\d{4}-\d{2}-\d{2}) (\d{2}:\d{2}) BJT', md))
  log = json.loads(Path('/tmp/ai_trader_trades_log.json').read_text())
  trades_since_last_section = count_trades_after(log, last_section_idx)
  if trades_since_last_section > 0:
      print(f"⚠️ P-MR-202: {trades_since_last_section} trades unaccounted for in MD")
  ```
  **Anti-pattern**: writing a new section that pretends the prior cron never happened — the trades are real in trades_log, the cash impact is real, the cron should be acknowledged even if its MD section was lost. **Distinction from P-MR-111** (scan.py truncates prior cron sections): P-MR-111 is scan.py internally dropping content on `update_notes()`; P-MR-202 is the cron runner silently failing to write its MD section while still updating the JSON state files.
- **P-MR-203 (2026-07-31 01:00)**: **Cleanest textbook 1-Trigger Stage 2 micro-buy at cash-floor — full P-MR-200 5-step decomposition on a with-trades scan with only ONE trade, ONE lag fingerprint, ONE drift.** This is the first clean scan where all four Hybrid A+B patterns (P-MR-187b / P-MR-189 / P-MR-194 / P-MR-195) were carefully distinguished from a 4th sub-pattern: **Hybrid A+B saturation with 1 micro-buy squeeze-through** at cash-floor, where ONLY the highest-RR Stage 2 candidate that fits inside cash-deployable micro-buy can fire. Observed: 1 BUY CIFR 2 @ $22.68 = $45.36 (highest RR=2.5, cheapest unit-price $22.65); PATH/BA/AVGO all cap-blocked (Type B); LMT cash-blocked (Type A, needs $5,608). **Distinction matrix**:
    - P-MR-187b: needs 2 SL + 1 TP1 trigger pattern (sells unblock cash temporarily)
    - P-MR-189: needs 0-trigger (both held-cap AND new-cash blocked)
    - P-MR-194: needs 4 candidates spanning success+cap+reject+implicit (4 types)
    - P-MR-195: needs >$1k normal-sized buy (full-saturation-break)
    - P-MR-203: 1 BUY success, 1st-rank-RR (highest RR wins the squeeze); distinct from P-MR-208 (2nd-rank-RR when top is cash-blocked)
    - P-MR-205: 0-trigger, multi-cap collapse (≥4 cap-block + 1 cash-block)
    - **P-MR-208 NEW**: 1 BUY success, 2nd-rank-RR (top-RR cash-blocked, scan falls through); smallest with-trades drift ever (-$1.29)
  **Drift decomposition result**: scan-printed MV $91,772.32 vs FIFO MV $94,087.62 = **+$2,269.94 PURE stale-quote (P-MR-183)**; CIFR buy-lag lift +$45.36; cash deployment −$45.36 (offsets exactly); FIFO Total $94,140.53 vs Notes $94,168.00 = **−$27.47** smallest NEUTRAL drift ever on a with-trades scan. **Rule**: when only ONE trade fires and the drift decomposition is CLEAN (stale-quote only, no buy-lag residual, no SL-lag residual), classify as "Hybrid A+B saturation with 1 micro-buy squeeze-through" and use Notes as headline (NEUTRAL). **P-MR-192 day-boundary + micro-buy arithmetic validated for 5th time**: prior cf=1 → day-boundary reset to 0 → micro-buy CIFR $45.36 → +1 (post-cash $52.91 < $100) → cf=1 FINAL. Sequence: reset FIRST, trade effects SECOND. **Diagnostic**: CIFR is now at 6 historical buy entries across multiple full-closure-then-reopen cycles (similar to KTOS P-MR-186 cycle 5 pattern); FIFO recompute after fresh micro-buy at qty=2 creates textbook P-MR-180 buy-side lag fingerprint (only_in_fifo = {CIFR}, predicted 1h reconcile window). **Recipe**: when Stage 2 has 5+ candidates with a mix of cap-block (held) + cash-block (non-held) + 1 deployable micro-buy, always verify the micro-buy is the highest-RR candidate AND the only one with unit-price × min-lot ≤ cash; document this in the Block Classification block to justify why it won over LMT (which has higher MA20-distance but cash-deployable size mismatch).
- **P-MR-204 (2026-07-31 03:00)**: **`execute_code` cannot contain large heredocs whose body embeds literal `"""..."""` or `{{...}}` adjacent to backticks — Python's parser fails BEFORE the script runs with `SyntaxError: invalid syntax`.** Symptom: `report = """... line with `{{CIFR}}` in markdown body ..."""` raises SyntaxError at the inner `"""` token, not at runtime. **Fix**: write the cron-report markdown to a temp file via `write_file`, then read it back via `Path.read_text()` and append to `AI-Trader.md`. NEVER embed multi-thousand-char heredocs in `execute_code` blocks — even with proper `.format()` brace-escapes (P-MR-197), adjacent backtick-quote sequences break the parser. **Recipe**:
    ```python
    # WRONG — SyntaxError on inner triple-quote adjacent to backticks
    report = "...""" << this fails to parse
    ```
    ```python
    # RIGHT — write_file then read_text
    Path('/tmp/_cron_report.md').write_text(report_body)
    body = Path('/tmp/_cron_report.md').read_text()
    Path('/tmp/vivian-notes/AI-Trader.md').write_text(existing + body.rstrip('\n') + '\n')
    ```
    **Diagnostic**: if `execute_code` returns `SyntaxError: invalid syntax` pointing at a line containing `"""` near backticks or `{{...}}`, the heredoc is too dense for the sandbox parser; split it via write_file. **Anti-pattern**: trying to escape `\` around the inner triple-quotes — the parser sees them as Python source-level tokens, not string content. **Distinction from P-MR-188/197**: P-MR-188/197 are about f-string `{NAME}` evaluation at runtime; P-MR-204 is about parser-level triple-quote termination BEFORE the script body even executes.
- **P-MR-205 (2026-07-31 03:00)**: **5-candidate Hybrid A+B 0-trigger saturation with 1 cash-block + 4 cap-block — 5th distinct Hybrid A+B sub-pattern, complementary to P-MR-203.** Observed: 0 BUY fired, 5 Stage 2 candidates (CIFR, BA, PATH, AVGO, CVX); CIFR cash-block (Type A, $52.87 < 2×$22.69 micro-deploy); BA/PATH/AVGO/CVX all cap-block (Type B, held 10% cap). Notes $94,383 vs FIFO $94,375.03 = **+$7.97** smallest Notes↔FIFO drift ever on a **0-trade canonical** (better than P-MR-198's $3.99 with-trades). **Distinction from P-MR-189**: P-MR-189 was 2 candidates (1 cap + 1 cash); P-MR-205 is 5 candidates (4 cap + 1 cash). The classification stays "Hybrid A+B 0-trigger" but the scale matters for cron report framing — when 4 cap-blocks fire simultaneously, the cap-collapse (P-MR-144) is in full effect (cash $52.87 < $100 makes every held symbol trivially "at-cap"). **Rule**: when Stage 2 has ≥4 cap-block candidates AND 0 buys fire, classify as "Hybrid A+B saturation 0-trigger — multi-cap collapse" and explicitly note the P-MR-144 cash-floor cap-collapse context. **Counter arithmetic**: same-day carry-forward (P-MR-201) from 01:00 zt=0 cf=1 → 03:00 zt=1 cf=2 (0-trigger zt+1, cash $52.87 <$100 cf+1, no reset trigger). **Drift decomposition (P-MR-200 0-trade variant)**: sum_api $94,322.16 − scan_mv $91,817.68 = **+$2,504.48 PURE stale-quote (P-MR-183)**; FIFO Total $94,375.03 − scan_total $91,870.54 = +$2,504.49 (matches stale_quote exactly since API=FIFO perfect recon, zero buy-lag); Notes − fifo = +$7.97 → **TRUST per P-MR-117/198 0-trade canonical**. **Diagnostic**: when scan prints `Stage 2 候選: 5+` with 0 buy fired AND drift <$10 Notes↔FIFO AND stale-quote drift >$2k, you have a textbook P-MR-205 case. The 2.5k stale-quote magnitude is the yfinance-vs-snapshot difference for 24 positions × ~$100 avg price.
- **P-MR-206 (2026-07-31 03:00)**: **0-trade Notes↔FIFO drift $7.97 is the cleanest 0-trade canonical ever — refines P-MR-117/198 to a stricter "<$10 TRUST" rule for 0-trade scans.** Observed: 0 BUY, 0 SELL, 0 TP1, 0 TP2, 0 SL, 0 Type X — perfect 0-trade cron. Notes $94,383.00 vs FIFO $94,375.03 = **+$7.97**. Per P-MR-117 0-trade exception: "When 0 trades fire, Notes ≈ FIFO". Per P-MR-198: "<$10 = TRUST". Combined: 0-trade + drift <$10 + perfect API=FIFO recon → **TRUST unconditionally** (Notes headline, FIFO audit footnote). **Comparison with P-MR-198**: P-MR-198 had $3.99 drift on a with-trades scan (2 SL + 1 BUY, partial-saturation squeeze); P-MR-206 has $7.97 drift on a pure 0-trade scan (cleaner because no buy-lag or SL-lag residual). **Recipe**: when 0 trades fire AND `len(api) == len(fifo)` AND `len(only_in_api ∪ only_in_fifo) == 0` AND `|Notes − FIFO| < $10`, classify as "0-trade Notes-canonical TRUST" and use Notes $X as the headline. **Diagnostic**: drift magnitude 0-trade scans is dominated by (a) yfinance-vs-broker-snapshot per-position quote freshness (P-MR-183 stale-quote) and (b) inter-scan cash adjustment residuals (P-MR-179). Buy-lag and SL-lag are mathematically 0 in 0-trade scans.
- **P-MR-207 (2026-07-31 03:00)**: **Same-day counter increment validation — 2nd clean same-day carry-forward sequence after P-MR-201's 5-scan.** Sequence: 01:00 (2026-07-31 BJT) zt=0 cf=1 → 03:00 (2026-07-31 BJT) zt=1 cf=2. Day-boundary check: last_cron_bjt_date = 2026-07-31 == this_cron_bjt_date = 2026-07-31 → NO reset (P-MR-201). Trade effects: 0 BUY → zt +1; cash $52.87 <$100 → cf +1; no reset trigger. **Rule**: when both crons are same-BJT-day AND the earlier cron already documented its carry-forward counters, the next cron can compute directly as `zt_next = zt_prior + (0 BUY ? 1 : 0)` and `cf_next = cf_prior + (cash_next <$100 ? 1 : 0)` MINUS any reset trigger. **Diagnostic**: P-MR-201's 5-scan sequence was 22:00→23:02→01:00→03:00→03:30 all same-day with mixed resets; P-MR-207's 2-scan sequence is the simplest same-day extension (carry-forward → increment only). Both confirm P-MR-110/125 arithmetic is deterministic and additive across same-day crons. **Anti-pattern**: reflexively applying day-boundary reset on every late-night cron — only reset when the BJT date string changes (P-MR-201 explicit rule). The 03:00 cron is NOT a day-boundary case (still 2026-07-31 BJT) — do not falsely apply reset logic.
- **P-MR-208 (2026-07-31 03:30)**: **Hybrid A+B saturation with 2nd-rank-RR micro-buy squeeze-through — NEW smallest with-trades Notes↔FIFO drift −$1.29 ever.** Observed: 1 BUY ONDS 3 @ $7.60 actual-fill ($22.80 micro-deploy) fired at cash-floor; CIFR (top-RR 2.56) cash-blocked, ONDS (2nd-RR 1.85) deployed instead. Stage 2 候選 printed 8 ⭐5 lines but CVX/COP/FUTU omitted explicit cap-block prints (P-MR-210 silent-skip pattern). Drift decomposition: scan-printed MV $91,817.68 vs FIFO MV $94,467.22 = +$2,649.54 PURE stale-quote (P-MR-183); FIFO Total $94,497.29 vs Notes $94,496.00 = **−$1.29** NEW record smallest with-trades drift. **Distinction from P-MR-203**: P-MR-203 was 1st-rank-RR micro-buy (cheapest, highest-RR); P-MR-208 is **2nd-rank-RR** micro-buy (when top-RR is cash-blocked, scan falls through to 2nd-highest that fits cash). **Rule**: when 0 SL/TP/reject, exactly 1 BUY success, AND that BUY is NOT the highest-RR Stage 2 candidate (the highest was blocked by Type A), classify as P-MR-208. Conjecture: drift magnitude inversely correlates with trigger count — fewer concurrent triggers → smaller Notes↔FIFO drift (P-MR-187b drift +$2,553 with 3 triggers vs P-MR-208 drift −$1.29 with 1 trigger). **Diagnostic**: always check the rank position of the firing candidate in the Stage 2 sorted-by-RR list; if it's not #1, suspect P-MR-208 pattern. **P-MR-182 6th empirical observation**: micro-buy $22.80 < $1000 → cf NOT reset; cash $30.07 < $100 → cf +1 → cf=3 NEW longest floor streak (5 consecutive crons cash < $100).
- **P-MR-209 (2026-07-31 03:30)**: **`current_price=null` for fresh lots can persist ACROSS SCAN RUNS, not just within a single cron.** P-MR-180 documented null persistence within a single cron; P-MR-209 documents cross-scan persistence. ONDS was last bought at 23:00 on 2026-07-28 (~04h before this scan's 03:30 BJT) and showed `current_price=null` on broker API at 23:00. The 03:30 BJT cron just bought ONDS again (cycle 2 fresh lot, 3 @ $7.60) and immediately after the BUY, broker API STILL returns `current_price=null` for ONDS until next price tick. **Rule**: every fresh-lot symbol must be sourced from `⭐5` line OR `trades_log` entry price when computing FIFO MV; never use broker-API `current_price` for symbols with new lots in the same cron. **Diagnostic**: at the FIFO MV recompute step, query `api_positions` for the fresh-bought symbol; if missing OR `current_price=null`, override with stdout ⭐5 price (P-MR-169 fallback) and document the override in the cron report. **Anti-pattern**: assuming that an N-hour-old broker-API price should be populated by the next cron — fresh lots with sub-tick prices WILL show null even hours after fill.
- **P-MR-210 (2026-07-31 03:30)**: **Stage 2 ⭐5 candidates that already exceed 10% cap may NOT emit an explicit `倉位已達10%上限` print line** — supersedes P-MR-133 "scan emits block prints for SOME candidates only" by adding the explicit-skip rule. Observed at 03:30: CVX $192.63 (12 股, 2.5% cap), COP $118.65 (64 股, 7.6% cap), FUTU $103.77 (67 股, 7.0% cap) all appeared in `⭐5` Stage 2 list but DID NOT print `倉位已達10%上限($X/$Y)，跳過` — instead scan internally de-prioritized them. The candidates that DID emit prints were BA/PATH/AVGO/CIFR (cap-floor collapse per P-MR-144). **Rule**: when constructing the Block Classification block, ALWAYS cross-reference Stage 2 ⭐5 list against explicit block-print lines. Candidates that are in ⭐5 but missing from both print lines AND missing from `BUY <SYM>` success lines must be classified as **Type B silent-cap-skip** (NOT implicit / Type C). **Diagnostic**: when `len(⭐5) > len(explicit_prints) + len(success_prints)`, the gap is silent cap-skips. Document each gap entry in the cron report's Block Classification block. **Distinction from P-MR-124**: P-MR-124 documents HELD symbols appearing in Stage 2 with explicit cap-block print; P-MR-210 documents HELD symbols appearing in Stage 2 WITHOUT explicit cap-block print (silent skip via internal de-prioritization).
- **P-MR-213 (2026-08-03 01:00)**: **Hybrid X+D block classification — when broker-side HTTP 400 rejects fire (Type X) AND MAX_STOCKS=2 queue exhaustion silently skips the remaining top-5 candidates (Type D), classify as "Hybrid X+D 0-fill" rather than a single type.** The 01:00 BJT cron saw 8 Stage 2 candidates; scan printed + evaluated top 5 only (per scan.py L716 `qualified[:5]`). Top 2 (CIFR $22.32 RR=2.93, ORCL $129.87 RR=2.51) both hit `BUY <SYM> 失敗: HTTP Error 400: Bad Request` — these are Type X (attempted-and-refused per P-MR-171), NOT Type A (cash不足) or Type B (cap-block). Slots 3-5 (SMCI/ONDS/BIDU) never received an attempt because `can_buy[:MAX_STOCKS]` truncation in the BUY loop ran out of slots after the two failures; these are Type D (queue exhaustion per P-MR-138/143). **Recipe**: (1) for any `BUY <SYM> 失敗` line, ALWAYS classify as Type X regardless of what the broker's reason field says — the attempt itself is the signal that scan.py tried to deploy, distinct from "didn't attempt" semantics of A/B/C/D. (2) For each Stage 2 ⭐5 candidate NOT in the explicit failure list AND NOT in success list, classify as Type D (queue exhaustion) — they passed the cap-floor-collapse check (per P-MR-144) but never reached `can_buy[:MAX_STOCKS]`. (3) Rank 6+ candidates that never even print a `⭐5` line are NOT counted as Type D — they are `top-5 truncation` and should be noted separately. **Counter behavior**: zero-trigger counter increments (no BUY success), cash-at-floor counter stays at reset base (no micro-buy cliff). **Distinction from prior Hybrid patterns**: P-MR-211 was Hybrid A+B+D (cash-pool-split + cap-floor + queue); P-MR-213 is Hybrid X+D (broker reject + queue) — different first axis (Type X vs Type A), same second axis (Type D). **Diagnostic**: when `len(BUY_FAIL) >= 1` AND `len(⭐5) > len(BUY_FAIL) + len(BUY_SUCCESS)`, the gap is Type D queue exhaustion triggered by failures eating slots.
- **P-MR-214 (2026-08-03 01:00)**: **API↔FIFO perfect identity (`api_mv == fifo_mv`) is a clean diagnostic for 0-fill scans — drift is then PURE stale-quote (P-MR-183) with zero buy-lag or SL-lag components.** Observed: per-line API parser (P-MR-168) captured all 24 positions with fresh stdout prices; `fifo_open_positions(log)` returned the same 24 symbols with identical qty; `sum(qty × price)` from API source exactly equaled the FIFO MV recompute. Therefore `drift = scan_mv − sum_api = −$4,173.00` was 100% pure stale-quote (broker snapshot vs yfinance fresh quotes); no buy-lag lift, no SL-lag residual, no Type X fill reconciliation pending. **Recipe (0-fill drift decomposition shortcut)**: (1) verify `len(api) == len(fifo)` AND no qty diff; (2) verify `sum_api == fifo_mv` (identity check); (3) IF both pass, report drift as pure stale-quote (P-MR-183) without decomposing buy-lag/sell-lag/cash-deployment components. (4) Notes ↔ FIFO drift is then the only meaningful secondary metric — if <$10, use Notes as headline (P-MR-206 0-trade canonical TRUST). **Anti-pattern**: running through P-MR-200's full 5-step decomposition when the identity holds — it's redundant. **Diagnostic**: this is the cleanest possible 0-fill scan output; the recipe applies whenever the scan fires 0 trades AND no held-symbol buy-lag is in flight (i.e. last cron was ≥1h ago). When any held symbol was purchased in the prior cron AND broker hasn't reconciled (P-MR-190 1h window), `only_in_fifo` will be non-empty and the identity breaks — fall back to full decomposition.
- **P-MR-215 (2026-08-03 01:00)**: **Cross-BJT-day counter reset validated after a 3-day gap (~72h from 2026-07-31 23:00 → 2026-08-03 01:00).**
- **P-MR-216 (2026-08-03 03:00)**: **Type X retry-arc persistence — when the same broker-rejected symbols (HTTP 400) repeat across consecutive same-BJT-day crons, escalate the watch signal.**

- **P-MR-217 (2026-08-03 22:00)**: **TP1-partial symbols disappear from per-line API parser (P-MR-168 misses a 4th price-source category).** When scan.py fires TP1 partial 1/3 sell for a held symbol (MSFT 24→16, JD 3→2), the symbol STILL appears in FIFO open positions (partial lot remains) BUT it does NOT appear in the regular `🟢 OK SYM qty=N 現價=$X` 持倉 block — only in the `💰 +20% TP1 → 賣 1/3 SYM qty=X 現價=$Y` line. **4-source price-fallback chain** for FIFO MV: `api → star5 (P-MR-169) → tp1-partial (NEW) → avg_cost (P-MR-180)`. **Diagnostic**: when `per_line_api_count + len(💰+20%TP1 lines) == fifo_count` and the gap matches TP1 fires, classify the gap as sell-lag residual. See `references/2026-08-03-2200-cron-pitfall-217-220-tp1-partial-lag-mixed-realized-buy-healthy-cifrorcl-rank-driven.md` for the full recipe.

- **P-MR-218 (2026-08-03 22:00)**: **Mixed TP1-partial + fresh-BUY scan classification — distinct from all prior Hybrid patterns.** Recipe: when `len(⭐5) >= 3 AND len(BUY_SUCCESS) >= 2 AND len(TP1_FIRES) >= 2 AND len(TypeX) == 0 AND len(TypeB) == 0`, classify as "TP1-realized + fresh-BUY healthy" and document ONLY the counter reset (zt→0) without escalation. Drift fingerprint: Notes ↔ FIFO in $0-$100 NEUTRAL (this scan: −$72.92).

- **P-MR-219 (2026-08-03 22:00)**: **`git diff --cached --check` reports trailing whitespace as a warning (exit 0), not an error (non-zero exit).** P-MR-173's blank-EOF recipe handles one case; P-MR-219 documents that body trailing whitespace is non-blocking. **Recipe**: accept the warning if exit code is 0, do NOT block commit. Optional: `sed -i 's/[[:space:]]*$//'` only inside fresh clean-clones, never on the source `/tmp/vivian-notes/AI-Trader.md`.

- **P-MR-220 (2026-08-03 22:00)**: **CIFR/ORCL Type X retry-arc — natural resolution via top-RR shift.** When prior crons show consistent Type X rejects on a symbol pair but the next cron shows zero Type X AND zero successful buys for that pair, check whether the rejected symbols simply fell below the top-N scan-attempt threshold. Diagnostic: `set(prior_type_x) ⊆ Stage 2 current AND current_type_x == ∅` → rank-driven rejection, downgrade "structural" to "top-2-only trigger". Distinction from P-MR-184: P-MR-184 = SUCCESSFUL retry; P-MR-220 = NATURAL AVOIDANCE via threshold drop. P-MR-213 documented the first-occurrence Hybrid X+D pattern; P-MR-216 documents the **second-consecutive-occurrence** upgrade. Observed: 01:00 cron saw CIFR ORCL HTTP 400; 03:00 cron (same BJT day, 2h later) saw the SAME pair rejected again. This 2x persistence suggests scan.py's simulated broker signal schema/validation rule is rejecting a specific shape that these symbols share (likely price-quantity mismatch or lot-size constraint for held-cap symbols). **Recipe**: (1) at the first occurrence, classify as "Hybrid X+D 0-fill" per P-MR-213 with a `Next Cron Watch` line about retry-arc. (2) at the second consecutive same-BJT-day occurrence of the SAME pair, escalate the watch with explicit "scan.py 模擬 broker signal schema/validation 設定" diagnosis hint. (3) at the third consecutive occurrence, document as a structural signal — not a transient broker glitch. **Diagnostic**: when `set(this_cron_type_x) & set(last_cron_type_x) != ∅` AND `last_cron_bjt_date == this_cron_bjt_date`, classify as P-MR-216 escalation level 2. **Distinction from P-MR-184** (full-closure retry arc): P-MR-184 documents a SUCCESSFUL retry after a prior reject (reject → success → full closure); P-MR-216 documents REPEATED REJECTS of the same pair (reject → reject → still pending). **Counter behavior**: unchanged from P-MR-213 (zero-trigger +1, cash-at-floor unchanged). **Drift behavior**: identical to P-MR-206 0-trade canonical ($0.23 territory) — pure stale-quote + Notes canonical TRUST. **Anti-pattern**: dismissing the 2x persistence as "random broker glitch" — repeated HTTP 400 on the same symbol pair within 2h is a structural signal, not noise. P-MR-185 documented "day-boundary reset is NOT time-dependent" up to ~24h gaps. This cron extends the validation to 72h: both `zero-trigger-counter` and `cash-at-floor-counter` reset to their base values (zt=1, cf=0) on the new BJT date, regardless of elapsed hours. **Counter trajectory**: prior cron (2026-07-31 23:00) had zt=1 cf=0 (per P-MR-129 sell >$1000 reset); this cron (2026-08-03 01:00) starts at zt=1 cf=0, applies 0 BUY + 2 Type X rejects → zt=1 stays at base (no BUY to reset per P-MR-110); cf=0 stays at base (cash $1,163.31 > $100, no micro-buy cliff). **Rule**: the reset is a function of `last_cron_bjt_date != this_cron_bjt_date`, NOT of `now - last_cron_time`. Any gap longer than 24h still triggers exactly one day-boundary reset on the first cron of the new BJT day. **Anti-pattern**: scaling the reset magnitude by elapsed hours (e.g. "72h gap = 3× reset") — wrong, the rule is binary day-boundary-detected → base reset, period. **Diagnostic**: when computing counters for a cron that hasn't run for >24h, ALWAYS check BJT date first; if date differs, reset to base values regardless of time gap.

## Pre-flight checklist (run BEFORE `python3 /tmp/ai_trader_scan.py`)

```python
# Use execute_code (write_file + run pattern) — NEVER pipe to python from terminal (P-MR-85 + P-MR-99)
import json
from pathlib import Path

# 1. State files parse cleanly (P-MR-88/89)
for name in ['tp1', 'tp2']:
    p = Path(f'/tmp/ai_trader_{name}_state.json')
    try:
        json.loads(p.read_text()) if p.exists() else None
        print(f"{name} OK")
    except Exception as e:
        print(f"{name} BROKEN: {e}")

# 2. trades_log + trade-only filter (P-MR-88/89)
# NOTE: load_log() in fifo_pnl.py takes 0 positional args. Load manually:
log = json.loads(Path('/tmp/ai_trader_trades_log.json').read_text())
bad = [t for t in log if t.get('action') not in ('buy','sell')]
print(f"log OK, {len(log)} entries, {len(bad)} non-trade")

# 3. FIFO helper export check (P-MR-96)
import sys; sys.path.insert(0, str(Path.home()/'.hermes/skills/data-science/stock-analysis/scripts'))
import fifo_pnl as fp
required = ['fifo_realized','session_block','session_block_by_time','live_unrealized',
            'load_log','fifo_open_positions','session_realized_pnl']
missing = [n for n in required if not hasattr(fp, n)]
print(f"fifo_helpers: {'all present' if not missing else f'MISSING: {missing}'}")
```

```bash
# 4. Git push is best-effort — never fail the cron on push error (P-MR-9 + P-MR-100 + P-MR-107 + P-MR-145 + P-MR-154 + P-MR-156)
# Note: `cd /tmp/vivian-notes && git add -A` is BROKEN due to nested .git recursion bug (P-MR-100).
# Note: bare `git -C /tmp --git-dir=/tmp/.git` (no --work-tree) is BROKEN — silently stages deletions (P-MR-107).
# Note: P-MR-154/156/186 — `/tmp/.git` is structurally broken (missing blobs); corruption RE-OCCURS within ~19h.
#       The three-flag form and env-var form may BOTH fail at commit time with "invalid object" errors.
#       Previously-promoted clean-clone at `/tmp/_vivian_notes_clean` ALSO accumulates corruption by
#       ~24h. The fixed recovery is FRESH-EACH-CRON — see P-MR-186 below.

# 4a. (P-MR-187) ALWAYS tee scan stdout to a timestamped log on the SAME line as the scan:
python3 /tmp/ai_trader_scan.py 2>&1 | tee /tmp/_scan_stdout_$(date +%s).log
# Re-runs overwrite terminal scrollback; only the tee'd file preserves the post-trade stdout
# for cron report recon. Without tee, a parser-test or manual retry will desync the report
# from the actual trades_log + tp*_state.json state.
```

```python
# 4b. (P-MR-151 + P-MR-186) Fresh-each-cron clean commit path. Do NOT reuse a clean-clone
# directory across crons; corruption accumulates. Build a timestamp-fresh repo each cron:
import os, shutil, subprocess
from pathlib import Path

fresh_repo = Path(f"/tmp/__notes_{Path('/tmp/ai_trader_scan.py').stem}__{os.environ.get('CRON_ID','manual')}__")
if fresh_repo.exists():
    shutil.rmtree(fresh_repo)
fresh_repo.mkdir(parents=True)

notes_dir = fresh_repo / 'vivian-notes'
notes_dir.mkdir()
shutil.copy2('/tmp/vivian-notes/AI-Trader.md', notes_dir / 'AI-Trader.md')

os.chdir(fresh_repo)
subprocess.run(['git', 'init', '-q', '-b', 'main'], check=True)
subprocess.run(['git', 'config', 'user.email', 'hermes-cron@ai-trader.local'], check=True)
subprocess.run(['git', 'config', 'user.name', 'AI-Trader Cron'], check=True)
subprocess.run(['git', 'add', 'vivian-notes/AI-Trader.md'], check=True)
subprocess.run(['git', 'commit', '-m', f'cron: {os.environ.get("CRON_ID","manual")}'], check=True)
# verify
print(subprocess.run(['git', 'log', '-1', '--oneline'], capture_output=True, text=True).stdout)
# Best-effort push (expected to fail per P-MR-9/175)
subprocess.run(['git', 'remote', 'add', 'origin',
                'https://github.com/vivianyeah/vivian-notes.git'],
               capture_output=True)
subprocess.run(['git', 'config', 'credential.helper',
                '/home/vivian/.hermes/bin/git-cred-helper.sh'], check=False)
r = subprocess.run(['git', 'push', 'origin', 'main'],
                   capture_output=True, text=True,
                   env={**os.environ, 'GIT_TERMINAL_PROMPT': '0'})
if r.returncode != 0:
    print(f"⚠️  Push failed (non-fatal per P-MR-9/175): {r.stderr.strip()}")
    print(f"   Local commit in {fresh_repo} is the durable record.")
```

## Defensive-read pattern (recurring lesson from P-MR-88/89 + P-MR-90)

External data (broker API, trades_log JSON, state files) may contain explicit `None`, missing keys, or wrong types. Default-arg `.get('field', default)` does NOT cover explicit `None`. Always:

```python
# WRONG — crashes if value is None
p.get('current_price', 0)

# RIGHT — falls back through None, missing key, falsy value
(p.get('current_price') or p.get('entry_price') or 0)
```

**Comma-formatted numbers** (P-MR-186): scan stdout emits `$91,853.97`, `$93,388.11`, etc. Always parse with comma-aware groups and `replace(',','')`:

```python
# WRONG — drops leading digits on comma-formatted totals
m = re.search(r'持倉市值:\s*\$([\d.]+)', stdout)
total = float(m.group(1))  # "853.97" instead of "91853.97" → off by $90k

# RIGHT — capture comma-formatted + strip commas
m = re.search(r'持倉市值:\s*\$([\d,.]+)', stdout)
total = float(m.group(1).replace(',', ''))  # 91853.97 ✓
```

Apply both patterns to any place scan.py reads API positions, computes sums, or filters dicts.

## P&L aggregation scripts

When the cron report needs **session realized P&L** or **all-time realized P&L**, scan.py does not surface this. Use `scripts/fifo_pnl.py`:

```bash
python3 ~/.hermes/skills/data-science/stock-analysis/scripts/fifo_pnl.py
# All-time realized P&L: USD -157.95  (61 closed trades)
# Session realized P&L:   USD -1276.87  (25 entries in block)
```

The script exports:
- `fifo_realized(log)` — full FIFO closed-trade list `[{sym, qty, buy, sell, pnl}]`
- `session_block(log, n)` — last-N heuristic (P-MR-91 caveat)
- `session_block_by_time(log, bjt_date)` — time filter on `executed_at` (P-MR-94: returns [] for current data)
- `fifo_open_positions(log)` — `{sym: {qty, avg_cost, cost_basis}}` for current open positions. **Use this to cross-check scan.py's API view** (P-MR-92). The two should reconcile within 1 cron interval.
- `session_realized_pnl(log, n)` — accurate session P&L by replaying FIFO from `log[session_start-1]` state. Returns a **`float`** (the realized PnL total in USD), NOT a dict. **Do not** index `['total']` / `['trades']` — they don't exist. Example: `pnl = session_realized_pnl(log, 25); print(f"${pnl:.2f}")`. Use when session has both pre-existing positions and new trades (P-MR-91/94).
- `live_unrealized(positions)` — total unrealized P&L from broker position snapshots (expects `{sym|symbol, qty, cost, price}` after P-MR-93 fix).

For the **P-MR-140/141/142-aware FIFO recompute + drift decomposition + Notes-trust gate**, see `scripts/recompute_account.py`. Usage:
```python
from recompute_account import recompute, notes_trust_gate
# this_scan_trades = log[pre_scan_log_len:]
fifo_total, notes_total, drift, lag = recompute(
    stdout, log, fifo_open_positions, scan_trades=this_scan_trades
)
verdict = notes_trust_gate(drift, lag)  # 'TRUST' | 'IGNORE' | 'NEUTRAL'
```
This consolidates the post-trade-cash adjustment (P-MR-141), the broad-regex stdout parse (P-MR-140), and the 4-way Notes-trust gate (P-MR-117/131/142). Use it on every cron to avoid re-deriving these steps inline.

Defensive-read is applied throughout (P-MR-88/89 + P-MR-90 patterns baked in).

## Cron report — four "always" rules (P-MR-96 + P-MR-97 + P-MR-101 + P-MR-103)

Every cron report, regardless of whether signals fired, must do these four things:

1. **Always verify FIFO helper imports exist** before computing anything:
   ```python
   from fifo_pnl import fifo_open_positions, session_realized_pnl, live_unrealized
   ```
   If this fails (docs/script drift), patch `fifo_pnl.py` immediately and add a P-MR entry. Do not silently fall back to inline re-implementations.

2. **Always quote the FIFO-recompute account total**, with the scan-printed total as a footnote (refined by P-MR-170 to also include `Notes updated`):
   ```python
   fifo_total = cash + sum(q * current_price for _, q, current_price in api_positions)
   drift = fifo_total - scan_printed_total
   ```
   The scan total uses a stale broker snapshot (~$1-2k drift is normal per P-MR-97; $2k+ drift seen in P-MR-103 even with perfect position reconciliation). The recompute uses the fresh per-ticker yfinance quotes shown in scan stdout. P-MR-170: in 0-fill scans, decompose residual as quote freshness, NOT broker lag.

3. **Always do the API↔FIFO reconciliation** (next section) — use the **per-line** parser from P-MR-168 to catch all `5% 止蝕` / `MA10止蝕` / `+X% TP1 → 賣` prefix variants. If `len(api_positions) != len(fifo_open_positions)` or any qty differs by ≥1, the cron ran during broker reconcile lag (P-MR-92) and the cron report must annotate which positions show trigger-time vs current state (P-MR-95). Report drift and position diff as **independent diagnostics**.

4. **Always report on 0-trigger crons** (P-MR-101). Even when scan prints `買入信號: 0 只` and no SL/TP fires, append a cron report section to `AI-Trader.md` with: result count, account total (FIFO recompute), API↔FIFO reconciliation result, Stage 2 candidate count, and explicit reason for no-trades (e.g. "資金已滿倉，上限阻塞" per P-MR-102). Never `[SILENT]`-suppress a 0-trigger cron.

   **P-MR-172 refinement**: even when the cron *does* fire trades (2 SL + 1 BUY same scan, etc.), the same per-line API parser will see the **pre-trade shell** including positions about to be sold. Always annotate `only_in_api` and `only_in_fifo` with this caveat: "API list is pre-trade snapshot; FIFO list is post-trade truth." Do NOT infer broker reconcile lag from `api_n != fifo_n` in same-trade scans — the difference is the trades *themselves*, not lag.

5. **Always include the cash-trajectory line** (P-MR-114). In the Diagnostics block, show the last 3-4 crons' Cash as a one-liner trajectory. Combined with the P-MR-110 consecutive zero counter, this lets the operator distinguish: (a) never-buys state (steady cash <$100, 5+ consecutive zeros), (b) post-buys saturation (just bought, cash cliffed back to <$100, only 1-2 consecutive zeros), (c) cash recovery (cash $5k+ growing). Combined with P-MR-125 cash-at-floor counter, the trajectory should also report `cash-at-floor: N` (consecutive crons with Cash <$100). See `references/cron-report-template.md` for the full 10-block template.

6. **Always classify Stage 2 blocks with P-MR-116 + P-MR-171**: distinguish Type A (cash-block), Type B (cap-block, possibly degenerate per P-MR-144), Type C (implicit), Type D (MAX_STOCKS queue exhausted, P-MR-138/143), and **Type X (broker-side HTTP 400 reject, attempted-and-refused, P-MR-171)**.

## Cron report recipe: API view vs FIFO view cross-check (P-MR-92 + P-MR-168)

After every scan, do NOT trust AI-Trader.md table row count alone. Use the **per-line** parser from P-MR-168 so all `5% 止蝕` / `MA10止蝕` / `+X% TP1` lines are caught:

```python
import re, json
from pathlib import Path
from fifo_pnl import fifo_open_positions

# 1. Per-line API view parser (P-MR-168) — catches 5% 止蝕 prefix that re.findall misses
api_positions = []
seen = set()
for line in stdout.splitlines():
    if not re.search(r'qty=[\d.]+\s+現價=\$[\d.]+', line): continue
    mm = re.search(r'(?:🟢|🔴|🟡)\s+(.+?)\s+([A-Z][A-Z0-9.]*)\s+qty=([\d.]+)\s+現價=\$([\d.]+)', line)
    if mm and mm.group(2) not in seen:
        api_positions.append({'symbol':mm.group(2), 'qty':float(mm.group(3)), 'price':float(mm.group(4))})
        seen.add(mm.group(2))
print(f"API view: {len(api_positions)} positions")
rebuild_n = int(re.search(r'\[rebuild\] API 持倉 (\d+) 隻', stdout).group(1))
assert len(api_positions) == rebuild_n, f"⚠️ P-MR-168 prefix regex dropped {rebuild_n - len(api_positions)} positions"

# 2. ⭐5 fallback (P-MR-169) — non-held Stage 2 candidates need this for current_price
prices = {p['symbol']:p['price'] for p in api_positions}
for line in stdout.splitlines():
    mm = re.search(r'⭐5\s+([A-Z][A-Z0-9.]*)\s+\$([\d.]+)', line)
    if mm: prices[mm.group(1)] = float(mm.group(2))

# 3. FIFO view (true current state from trades_log)
open_lots = fifo_open_positions(log)
print(f"FIFO view: {len(open_lots)} positions")

# 4. Diff → likely broker reconcile lag if diff > 0 and >1 cron old
api_syms = {p['symbol'] for p in api_positions}
fifo_syms = set(open_lots)
print(f"only_in_api: {api_syms - fifo_syms}")   # lag shell positions
print(f"only_in_fifo: {fifo_syms - api_syms}") # not yet broker-confirmed
```

If `only_in_api` non-empty, the table rows show stale qty (post-SL+rebuy lag). Note this in the cron report and use FIFO numbers for the user-facing summary.

---

## Historical pitfall log (P-MR-88 through P-MR-167)

For full historical session detail see `references/historical-pitfalls-pmr-88-through-pmr-167.md`. Recent canonical pitfalls P-MR-168 through P-MR-253 live in the section above.
- **P-MR-186 (2026-07-28 03:30)**: (1) Clean-clone at `/tmp/_vivian_notes_clean` itself accumulates corruption within ~24h; the recovery is **fresh-each-cron**, not slot-reuse. (2) Comma-formatted regex: `\$([\d.]+)` on `"$91,853.97"` drops leading "91"; always use `\$([\d,.]+)` and `s.replace(',','')`. (3) KTOS reached 5th lifecycle cycle (11 trade entries, multiple full-closure-then-reopen loops); FIFO recompute after a $49.32 micro-buy aggregates pre-buy `49.34`+post-buy `49.32` = avg `$49.33`, qty 1→2; API shows the lag shell at qty=1 (P-MR-180 fingerprint). (4) Cash-at-floor counter empirically observed: cash $117.23 → −$49.32 → post $67.88; counter went 0 → 1 per P-MR-182 (micro-buy under $1000 threshold does not reset). This is the third empirical observation of the P-MR-182 rule.
- **P-MR-187 (2026-07-28 22:00)**: (a) ALWAYS `python3 /tmp/ai_trader_scan.py 2>&1 | tee /tmp/_scan_stdout_$(date +%s).log` on the SAME line as the scan invocation. Re-runs overwrite terminal scrollback; only the tee'd file preserves the post-trade stdout for cron report recon. (b) Partial-saturation squeeze — when 2 SL+TP1 fire in the same scan, cash flushes from $70 to $6,804 and a NEW Stage 2 candidate can squeeze through with $6,734 deployment even when held symbol (ONDS) remains cap-blocked. Cash-at-floor counter STAYS elevated (4th P-MR-182 empirical observation) even though zero-trigger resets. Classify as "partial-saturation squeeze" (hybrid Type A+B, distinct from P-MR-127 cheapest-candidate pattern).
- **P-MR-187 (2026-07-28 22:00)**: **(a) ALWAYS tee scan stdout to a timestamped log on the SAME line as the scan invocation.** `python3 /tmp/ai_trader_scan.py 2>&1 | tee /tmp/_scan_stdout_$(date +%s).log`. Reason: any subsequent re-run (parser-test, manual retry, double-scan) will write to `trades_log` and `tp*_state.json` BUT the terminal scrollback may have already been overwritten — only the tee'd file preserves the post-trade stdout for the cron report's API↔FIFO recon. The first scan in this session produced 2 SL+1 TP1 sells (cash $6,804.25); the second produced the FUTU buy (cash $70.08). Without tee, the cron report would have used the first scan's pre-buy shell and miscomputed all counters. **Rule**: every cron invocation MUST tee stdout; if the cron report needs to re-derive values from stdout, it MUST read from the tee'd file, not from the terminal scrollback or a re-run. (b) **Partial-saturation squeeze-through variant — NEW type, distinct from P-MR-127 "cheapest candidate".** At deep saturation, when 2 SL+1 TP1 fire in the same scan, cash flushes from $70 to $6,804. A NEW Stage 2 candidate (FUTU, not previously held) can squeeze through with a $6,734 deployment even when the held symbol (ONDS) remains cap-blocked. Counter behavior: zero-trigger resets (P-MR-110) but cash-at-floor counter STAYS elevated because post-trade cash $70 < $100 (P-MR-182, 4th empirical observation). This is the "partial-saturation squeeze" — neither pure cap-block (Type B) nor pure cash-block (Type A), but a hybrid where sells unblock cash temporarily and a fresh candidate drains it back. **Diagnostic**: when cash-trajectory shows sudden spike-then-cliff (e.g. $70 → $6,804 → $70), classify as "partial-saturation squeeze" — note in cron report that zero-trigger reset but cash-at-floor counter did NOT.
- **P-MR-211 (2026-07-31 22:00)**: **Hybrid A+B+D saturation 0-trigger with cash-pool-split + cap-floor-collapse triple block — first empirical observation of Type D (queue exhaustion) interacting with Type B (cap-floor collapse) AND the cash-pool-split rule simultaneously.** The 22:00 BJT RTH-open-30min cron saw 5 ⭐5 candidates (CIFR/ORCL/SMCI/CEG/FCX) all blocked. CIFR Type B (cap-floor collapse: held $44.80 > $30 cash-floor cap per P-MR-144); ORCL/SMCI Type A (cash-block — ORCL outright $128.81 > $30, SMCI falls to cash-pool-split: `cash_per_stock = cash/n = $30/2 = $15/stock` → `qty = int($15/$28.17) = 0` shares); CEG/FCX Type D (MAX_STOCKS=2 queue truncation per P-MR-138/143 — never reached BUY loop, silent-skipped). **Key insight**: the cash-pool-split rule (`cash / MAX_STOCKS` for two-stock concurrent deployment) means that even when individual candidate unit-price × 1 ≤ cash (e.g. SMCI $28.17 < $30), the split can force qty=0 and block micro-squeeze. This breaks the P-MR-203/208 micro-squeeze pattern — when MAX_STOCKS=2 and cash is super-low, ANY deployment is split in half before sizing. **Diagnostic fingerprint**: `len(⭐5) = 5` + `len(explicit_blocks) ≤ len(⭐5) - 2` + `0 trades fired` + `cash < $50`. **Rule**: when Stage 2 has 5 ⭐5 candidates with cash < $50 AND 0 BUY fires, classify as "Hybrid A+B+D saturation" — DO NOT assume P-MR-208 micro-squeeze will fire, because the cash-pool-split rule (P-MR-145/186 fixed scan.py logic at L737-738) can block even the cheapest 1-share deploy when MAX_STOCKS=2 pools the cash. **Distinction matrix update (now 8 distinct Hybrid A+B sub-patterns)**: P-MR-187b partial-sat squeeze | P-MR-189 2-candidate hybrid | P-MR-194 4-type hybrid+X | P-MR-195 full-saturation-break | P-MR-203 1st-rank-RR micro-squeeze | P-MR-205 multi-cap collapse | P-MR-208 2nd-rank-RR micro-squeeze | **P-MR-211 hybrid A+B+D cash-pool-split triple block**.
- **P-MR-188 (2026-07-28 23:00)**: **NEVER use f-strings for cron-report templates — literal braces in markdown table cells trigger `NameError` on the symbol in braces.** P-MR-167 already documented that literal `{...}` braces inside f-strings get parsed as Python expressions. The trap is still easy to fall into when composing cron reports with symbol names in set-display brackets (e.g. `only_in_fifo: {FUTU}` in markdown). Even with NO Python `{var}` interpolation intended, `f"""... {FUTU} ..."""` evaluates `{FUTU}` as a name lookup and crashes. **Recipe**: ALWAYS use plain triple-quoted strings (`"""..."""`) for cron-report markdown templates. If interpolation is needed, use `.format(symbol=sym, ...)` with escaped braces `{{` and `}}` for literal braces, OR concatenate segments: `report = "... static text ... " + sym + " ... more static text ..."`. **Anti-pattern**: writing `report = f"## Cron ... only_in_fifo: {FUTU} ..."` — fails on first execution with `NameError: name 'FUTU' is not defined`. **Diagnostic**: if a cron report write throws `NameError: name '<SYM>' is not defined`, the f-string was used and the symbol is in literal braces; rewrite the report template as a plain string and re-execute.
- **P-MR-189 (2026-07-28 23:00)**: **Hybrid A+B block classification — when Stage 2 has 2 candidates of which ONE is held (cap-block) and ONE is new (cash-block), classify the scan as "Hybrid A+B" rather than picking one type.** At deep saturation, the typical pattern is: held symbol cap-blocked (Type B) + new candidate cash-blocked (Type A) → 0-trigger. The `Stage 2 候選: 2 only` line is the canonical signal: if the candidates include ≥1 held symbol AND ≥1 non-held symbol AND both blocks fire, it's a hybrid. This is distinct from P-MR-187(b) "partial-saturation squeeze" (where a candidate successfully deploys) and from P-MR-127 "cheapest-candidate squeeze-through" (Type A → micro-buy at saturation). **Recipe**: in the cron report's Block Classification block, list EACH candidate with its own type, then summarize as "Hybrid A+B saturation". Do NOT collapse to a single type. **Counter behavior**: zero-trigger counter increments (no buy fired), cash-at-floor counter increments if cash < $100. The 23:00 cron saw: ONDS = Type B (held, 10% cap $7746/$63), CSCO = Type A (cash $63 < $115). Hybrid A+B → 0-trigger → zt=1, cf=2.
- **P-MR-190 (2026-07-28 23:00)**: **Same-day consecutive crons validate counter carry-forward and P-MR-180 1h reconcile window.** The 22:00 → 23:00 crons are both 2026-07-28 BJT (same day), so P-MR-155 day-boundary reset does NOT apply. Counters carry forward: 22:00 (zt=0, cf=1 after FUTU buy) → 23:00 (zt=1, cf=2 after 0-trigger no-buy). Also empirically validated P-MR-180: at 22:00, FUTU was `only_in_fifo` (1h buy-side lag); at 23:00, FUTU is in API view at qty=67 matching FIFO — the broker reconcile window for fresh buys is ~1h (1 cron interval at this cadence). **Recipe**: when a cron notes `only_in_fifo: {X}` for a fresh buy, predict that the NEXT cron (~1h later) will have X in both API and FIFO. If it doesn't, escalate as broker-side lag persistence (multiple crons without reconcile is abnormal).
- **P-MR-191 (2026-07-28 23:00)**: **Inter-scan cash drift $6.74 with no trades — within P-MR-179 tolerance, log as watch footnote.** Observed: 22:00 post-cash $70.08 → 23:00 pre-cash $63.34 = −$6.74. No trades_log entry in between. This is the P-MR-179 broker-side adjustment pattern (realized P&L mark on stale lots, fee, settlement). $6.74 is well below the $10 single-event threshold and $10+ multi-cron escalation criterion. **Recipe**: log the delta in the cash-trajectory line with a footnote like "broker inter-scan adjustment (P-MR-179 watch footnote, no trades intervening)" — DO NOT flag as scan.py or cron failure. If consecutive crons show drifts > $10 each, escalate to broker inquiry (P-MR-179 diagnostic).
- **P-MR-192 (2026-07-29 01:00)**: **Day-boundary reset + micro-buy in SAME cron: counter arithmetic is non-trivial — work it step-by-step, NOT by feel.** Observed: 23:00 (2026-07-28 BJT) carried cf=2. This cron (2026-07-29 01:00 BJT) is a NEW BJT day → P-MR-155 day-boundary reset fires FIRST (cf: 2 → 0). Then 1 micro-buy (SMCI 1 @ $28.55 = $28.55, well under the $1000 P-MR-129/182 threshold) fires → P-MR-182 says micro-buy doesn't reset cf. Post-trade cash $34.79 < $100 → +1 → **cf=1 FINAL**, NOT 3 (no-carry arithmetic) and NOT 2 (no-reset arithmetic). **Recipe for future crons**: (a) Determine if `this_bjt_date != last_bjt_date` → reset both counters to base values (zt=1, cf=0). (b) Apply any rule effects from THIS cron's trades (buy → P-MR-110 zt reset; buy <$1000 → P-MR-182 cf NOT reset, only +1 if post-cash <$100). (c) The order is: **reset FIRST, then apply trade effects SECOND**. The trap is computing carry-forward from prior day's cf and missing the reset, OR forgetting the reset and applying trade effects to carried value (giving cf=2 or cf=3 instead of cf=1). **Worked example**: prior_cf=2, day_boundary=TRUE → cf=0; then 1 micro-buy under $1000 → cf stays 0 but post-cash $34.79 < $100 → +1 → cf=1. Same day zt: prior_zt=1, day_boundary=TRUE → zt=1 (base reset value); then 1 buy fired → reset to 0 (P-MR-110) → zt=0. **Anti-pattern**: writing the counter line as "carry-forward from 23:00 cron: zero-trigger=1, cash-at-floor=2" without recognizing day-boundary overrides that carry — the "carry-forward" phrasing is wrong on day-boundary crons. **Diagnostic**: when this cron is a different BJT day from the last, ALWAYS re-compute from base (zt=1, cf=0) and then layer trade effects on top; do NOT take the prior cron's counter as a starting point.

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你係 AI-Trader 模擬倉 cron（純 paper trading，冇 IB）。HermesV, Agent ID 6092.

# 任務
執行 /tmp/ai_trader_scan.py 北京時間 23:00 第二次 scan。

# 步驟
1. `python3 /tmp/ai_trader_scan.py`
2. 同 22:00 scan 結果對比，持倉有冇變動
3. 寫 `/tmp/vivian-notes/AI-Trader.md`（git push commit）
4. 寫 `/tmp/ai_trader_trades_log.json`（append）
5. 寫 `/tmp/ai_trader_tp1_state.json`
6. send_message target=telegram:8685773783 報告

# 時段
北京時間 23:00 = 美股 RTH 開市後 1.5 小時
22:00-23:00 期間信號通常有 follow-through，scan 確認

# 規則
- 純模擬倉
- Stage 2 突破回調
- 止蝕雙方法（β<1→5%, β≥1→1.5×ATR×max(β,0.5)）
- 評分含 RR，過濾顯不合格原因
- 報告用 code block + emoji 格式
- 每次 push GitHub

# Pitfalls
- 唔好 background
- 唔好 IB order
- append trades log

完成後報告。

## Response

All 5 deliverables complete + git push successful + Telegram delivered. Here's the final cron report:

---

# AI-Trader cron — 2026-08-19 23:00 BJT

**Result**: 0 trades fired (Hybrid A+B+D 0-trigger saturation, yfinance pool empty)

## Result block

- **Cash**: $207.40 (unchanged since 22:00 cron; no fills intervening)
- **持倉**: 32 只 | **FIFO Total**: **$100,441.88**
- **API↔FIFO identity**: EXACT (P-MR-214) — 32=32 perfect recon, qty match, no lag shells
- **Inter-scan FIFO drift** (22:00 → 23:00): **+$1,016.57** PURE RTH+1.5h price movement
- **Inter-scan cash drift**: $0.00 (P-MR-179 trivial)
- **`Stage 2 候選: 0 只` / `買入信號: 0 只`** — yfinance pool fetch returned 0 successful analyses (2nd consecutive after 22:00 cron)

## Counter arithmetic (P-MR-155/201 day-boundary + P-MR-110/125/182)

- Pre-cron: **zt=2, cf=0** (carried from 22:00 cron)
- BJT date: 2026-08-19 == 2026-08-19 → **NO day-boundary reset**
- 0 BUY → zt+1 → **zt=3**
- cash $207 > $100 → cf unchanged → **cf=0**

## Block classification: N/A

No Stage 2 candidates → no Type A/B/C/D/X. Cap-floor collapse (P-MR-144) is in **full active** state: every held symbol MV trivially > $207, all add-ons would Type B block; non-held ⭐5 would all hit P-MR-211/229 cash-pool-split ($207/MAX_STOCKS $2 = $103.70/stock). Pool-empty issue is upstream yfinance data fetch, not a scan.py block.

## Watch items (next cron priority)

| Symbol | Cost | Current | PnL% | Status |
|---|---|---|---|---|
| **COP** | $109.67 | $131.96 | **+20.33%** | TP1 threshold CROSSED — separate code path (scan.py is MA20-only, does not auto-fire TP1) |
| **T** | $21.53 | $25.41 | +18.02% | 1.98% to TP1 — CLOSE WATCH |
| **XOM** | $141.51 | $167.70 | +18.51% | 1.49% to TP1 — CLOSE WATCH |

**MA10 trail-edge SL candidates** (1 tick from break):
- VRT -6.99% (AT MA10 $262.93)
- INTC -6.20% (AT MA10 $93.39)
- KLAC -6.14% (AT MA10 $188.30)
- AVGO -5.97% (NEW on watch list — drifted during RTH open)

If 2-3 MA10 stops fire in next cron, expect P-MR-187(b)-style partial-saturation squeeze pattern.

## Drift decomposition (P-MR-200 0-trade variant + P-MR-214 identity shortcut)

1. API sum (per-line P-MR-168 parser): **$100,234.48** (32 positions)
2. FIFO MV: $100,234.48 = **IDENTITY HIT** (P-MR-214)
3. FIFO Total: $100,441.88 = cash $207.40 + FIFO MV $100,234.48
4. Notes Total: stale from 03:30 ($99,625.00); Notes-table not re-updated today → not usable as headline
5. Inter-scan drift = PURE price movement, ZERO buy-lag/SL-lag components

## Cash trajectory (last 7 crons, BJT)

```
08-18 22:00: pre=$3.28   → post=$19,891.34 (5 SL realization flush, P-MR-255)
08-18 23:02: pre=$19,870.11 → post=$460.98 (2 BUY MRVL/RKLB)
08-19 01:00: pre=$455.94 → post=$742.53 (1 SL CRWV)
08-19 03:00: pre=$742.24 → post=$207.94 (2 BUY TSLA/CRM)
08-19 03:30: pre=$207.40 (0 trades)
08-19 22:00: pre=$207.40 (0 trades, Stage 2 empty)
08-19 23:00: pre=$207.40 (0 trades, Stage 2 empty) ← this cron
```

**Cap-floor collapse full effect**: cash $207.40 < min(held MV); 22 hours at this floor (03:30 → 23:00). cf=0 because cash > $100.

## Deliverables

1. ✅ **Scan stdout tee'd** to `_scan_stdout_1787151666.log` (P-MR-187)
2. ✅ **AI-Trader.md**: appended `## ⏰ 2026-08-19 23:00 BJT` section (438,421 chars total, P-MR-202 verified)
3. ✅ **trades_log.json**: 286 entries (unchanged, 0 trades per P-MR-243 mutation guard)
4. ✅ **tp1_state.json / tp2_state.json**: 18 / 3 entries (unchanged)
5. ✅ **Git commit + push**: `e502eb4` pushed via P-MR-250 `--soft` rebase onto remote (`0f06d2e..e502eb4 main -> main`)
6. ✅ **Telegram**: message_id **7183** delivered to **Vivian Chan (chat_id 7290340299)** — P-MR-253 paired user_id substitution + P-MR-254 plain-text mode both validated

## Next cron: 01:00 BJT 2026-08-20

This will be a **day-boundary reset** (BJT date 2026-08-19 → 2026-08-20): zt→1, cf→0. Also re-tests whether yfinance pool fetch recovers. If still empty, escalate as pool-API issue. Watch for MA10 trail breaks on VRT/INTC/KLAC/AVGO and COP TP1 territory entry if scan.py picks up the SL/TP code path.

**Final counter state for next cron handoff**: zt=3, cf=0; if 2026-08-20 BJT then reset FIRST then trade effects SECOND per P-MR-192.
