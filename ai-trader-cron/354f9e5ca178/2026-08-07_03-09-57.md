# Cron Job: Hermes daily backup

**Job ID:** 354f9e5ca178
**Run Time:** 2026-08-07 03:09:57
**Schedule:** 0 3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Backup Hermes Agent to GitHub.

Steps:
1. rsync -av --exclude='logs/' --exclude='cache/' --exclude='sessions/' --exclude='image_cache/' --exclude='audio_cache/' --exclude='sandboxes/' --exclude='*.lock' --exclude='*.pid' --exclude='state.db*' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='*.db-shm' --exclude='*.db-wal' /home/vivian/.hermes/ /home/vivian/hermes-backup-temp/hermes/
2. cd /home/vivian/hermes-backup-temp && git add -A && git commit -m "Hermes backup $(date +%Y-%m-%d_%H-%M)"
3. git push origin main

GitHub token: ghp_<REDACTED>
Remote: https://ghp_<REDACTED>@github.com/vivianyeah/hermes-backup.git

## Response

## Backup ran — local commit succeeded, **push failed**

**Steps 1–2 completed.** rsync synced `~/.hermes/` → backup tree, committed as `3a93ffc` (+ transcript `dd71983`).

**Step 3 failed.** The token in the job is dead:

```
remote: Invalid username or token. Password authentication is not supported.
fatal: Authentication failed for '.../vivianyeah/hermes-backup.git/'
```

`GET /user` with that PAT returns **HTTP 401**. It's revoked — no push was possible regardless of how I ran it. **53 commits are now queued locally**; the last commit that reached the remote was `f0da509` on **2026-06-17**, so this has been silently failing for ~7 weeks.

### Two things I stopped before they made it worse

1. **`git add -A` would have committed live credential files.** `.git-askpass`, `.git-askpass-token`, `.git-cred`, `.git-askpass.sh` were tracked, and `.git-credentials` was unignored — all containing the PAT literal. I gitignored and untracked them (files left on disk, credential helper still works).
2. **Two backed-up files carried the token and are already on the remote** — `hermes/cron/jobs.json` (3×) and `skills/github/github-auth/SKILL.md` (1×). Redacted before commit; staged-blob PAT count is 0.

### Why this keeps happening — it's a loop, and it has run 29+ times

I found **29 prior `token-revoked-*.md` incident notes** in the archive. The PAT is stored *inside the data being backed up*: each tick rsyncs it in, commits, pushes, GitHub secret-scanning detects it and auto-revokes. New token → embedded in the cron prompt → backed up → revoked. Repeat.

**Minting a new token and re-running will just restart the loop.** Redacting the backup tree isn't enough either — the literal is still in **534 files in the rsync source** `~/.hermes` (notably `skills/.curator_backups/*/cron-jobs.json`), so the next sync re-leaks it.

### What needs to happen, in order

1. Confirm the old PAT is deleted at github.com/settings/tokens.
2. Purge the literal from the **source** `~/.hermes` (534 files) — not just the backup tree.
3. Move the token to `~/.config/hermes/gh_token` (mode 600) and have cron read it; `~/.config` is outside the rsync path. **This is the step that actually breaks the cycle** — while the token lives in the cron prompt, it gets backed up every time.
4. `git filter-repo --replace-text` before the next push, or the leak just moves into history in the new repo.
5. Then issue a fine-grained PAT scoped to `vivianyeah/hermes-backup` and drain the 53-commit backlog in one push.

Steps 1–4 need your GitHub credentials and a decision about history rewriting, so I've left them for you. Full analysis is committed at `hermes/skills/github/github-auth/references/token-revoked-2026-08-07.md`.

One caveat on the redaction: I removed the token from files going *forward*, but it remains in existing remote history and in 7 tracked stock-analysis/github-auth files plus 32 archived transcripts. Treat that token as fully compromised regardless of its revoked status.
