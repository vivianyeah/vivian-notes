# Cron Job: Hermes daily backup

**Job ID:** 354f9e5ca178
**Run Time:** 2026-08-08 03:09:33
**Schedule:** 0 3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Backup Hermes Agent to GitHub.

Steps:
1. rsync -av --exclude='logs/' --exclude='cache/' --exclude='sessions/' --exclude='image_cache/' --exclude='audio_cache/' --exclude='sandboxes/' --exclude='*.lock' --exclude='*.pid' --exclude='state.db*' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='*.db-shm' --exclude='*.db-wal' /home/vivian/.hermes/ /home/vivian/hermes-backup-temp/hermes/
2. cd /home/vivian/hermes-backup-temp && git add -A && git commit -m "Hermes backup $(date +%Y-%m-%d_%H-%M)"
3. git push origin main

GitHub token: ghp_<REDACTED>
Remote: https://ghp_<REDACTED>@github.com/vivianyeah/hermes-backup.git

## Response

# Hermes backup report — 2026-08-08 03:09 UTC

**Steps 1–2: OK.** Steps 3: **FAILED — PAT revoked.**

| Step | Status | Detail |
|---|---|---|
| 1. rsync | ✅ OK | 7.04 MB sent, 1.59 GB total backed up |
| 2. commit | ✅ OK | `5ff0823 Hermes backup 2026-08-08_03-09` — 7 files, +449 / −147 |
| 3. push | ❌ FAIL | `remote: Invalid username or token. Password authentication is not supported for Git operations.` |

## Root cause (already documented yesterday)

The PAT embedded in this cron prompt (`ghp_<REDACTED>`) is **the same one from yesterday's failure**. The incident note at `hermes/skills/github/github-auth/references/token-revoked-2026-08-07.md` explains the loop: GitHub secret scanning auto-revokes the PAT because it's stored inside the data being backed up (appears in 534 files under `~/.hermes`, including `cron/jobs.json` and `skills/github/github-auth/SKILL.md`). The cron prompt re-embeds it, the rsync backs it up, the commit pushes it, GitHub revokes it.29+ rotations since 2026-06-18.

Yesterday's note already redacted the live PAT to `ghp_<REDACTED>` in the staged blobs, so today's commit `5ff0823` does **not** contain the live token — pushing this commit is safe once a new PAT is issued.

## Backlog- **54 commits ahead of `origin/main`** locally (last successful push was `f0da509` on 2026-06-17).
- Today's commit is staged locally on `main` at `/home/vivian/hermes-backup-temp/`.
- Once Vivian rotates the PAT (a fresh one from https://github.com/settings/tokens), push with `git -C /home/vivian/hermes-backup-temp push https://<NEW_PAT>@github.com/vivianyeah/hermes-backup.git main` to flush all 54.

## Recommendation (same as yesterday, still open)

This keeps happening because the PAT lives in the data being backed up. The fix is one of:

1. **Move the PAT out of `cron/jobs.json`** — store it in a shell env var or systemd unit and have the cron command reference `$GITHUB_PAT`. Keeps the prompt token-free.
2. **Rotate to a fine-grained token** scoped only to `vivianyeah/hermes-backup` contents:write — reduces blast radius but doesn't break the loop, since secret scanning still flags it.
3. **Use SSH key auth** — `git@github.com:vivianyeah/hermes-backup.git` with a deploy key. No PAT in the tree, no auto-revoke. Cleanest fix.

Ping Vivian; the backup pipeline will keep cycling the same failure pattern otherwise.
