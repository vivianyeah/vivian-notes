# Cron Job: Hermes daily backup

**Job ID:** 354f9e5ca178
**Run Time:** 2026-09-22 03:01:00
**Schedule:** 0 3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Backup Hermes Agent to GitHub.

Steps:
1. rsync -av --exclude='logs/' --exclude='cache/' --exclude='sessions/' --exclude='image_cache/' --exclude='audio_cache/' --exclude='sandboxes/' --exclude='*.lock' --exclude='*.pid' --exclude='state.db*' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='*.db-shm' --exclude='*.db-wal' /home/vivian/.hermes/ /home/vivian/hermes-backup-temp/hermes/
2. cd /home/vivian/hermes-backup-temp && git add -A && git commit -m "Hermes backup $(date +%Y-%m-%d_%H-%M)"
3. git push origin main

GitHub token: $(cat $HOME/.config/hermes/backup_pat)
Remote: https://$(cat $HOME/.config/hermes/backup_pat)@github.com/vivianyeah/hermes-backup.git

## Response

Backup complete. All three steps executed cleanly:

1. **rsync** — 8.2 MB transferred (1.6 GB total directory size after excludes); logs/cache/sessions/audio_cache/sandboxes/state DBs/lock files excluded as specified.
2. **git commit** — `cd30ddf` on `main`: "Hermes backup 2026-09-22_03-00" (6 files changed, +587/−31).
3. **git push** — `09b828c..cd30ddf main -> main` to `github.com/vivianyeah/hermes-backup.git`.

Notable sync delta: 4 newly-tracked files under `skills/ai-trader-cron/references/` (cron artifacts from Sep 21–22); no other directory churn.
