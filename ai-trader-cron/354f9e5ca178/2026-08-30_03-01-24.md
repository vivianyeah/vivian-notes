# Cron Job: Hermes daily backup

**Job ID:** 354f9e5ca178
**Run Time:** 2026-08-30 03:01:24
**Schedule:** 0 3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Backup Hermes Agent to GitHub.

Steps:
1. rsync -av --exclude='logs/' --exclude='cache/' --exclude='sessions/' --exclude='image_cache/' --exclude='audio_cache/' --exclude='sandboxes/' --exclude='*.lock' --exclude='*.pid' --exclude='state.db*' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='*.db-shm' --exclude='*.db-wal' /home/vivian/.hermes/ /home/vivian/hermes-backup-temp/hermes/
2. cd /home/vivian/hermes-backup-temp && git add -A && git commit -m "Hermes backup $(date +%Y-%m-%d_%H-%M)"
3. git push origin main

GitHub token: $(cat $HOME/.config/hermes/backup_pat)
Remote: https://$(cat $HOME/.config/hermes/backup_pat)@github.com/vivianyeah/hermes-backup.git

## Response

Done. Backup complete:

- **Rsync**: transferred ~7.25 MB across 1 changed file; backup size ~1.6 GB
- **Commit**: `df71295` "Hermes backup 2026-08-30_03-01" — 1 file changed (cron/jobs.json)
- **Push**: `bd63cc4..df71295 main -> main` to vivianyeah/hermes-backup

Minor note: the `.git-cred` file in the backup repo was empty (no token cached), so push used the inline token from `$HOME/.config/hermes/backup_pat`. If you want subsequent pushes to work without the inline token, consider re-seeding `.git-cred` (or relying on store/git credential helpers) in the backup repo.
