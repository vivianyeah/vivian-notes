# Cron Job: Hermes daily backup

**Job ID:** 354f9e5ca178
**Run Time:** 2026-07-31 03:02:00
**Schedule:** 0 3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Backup Hermes Agent to GitHub.

Steps:
1. rsync -av --exclude='logs/' --exclude='cache/' --exclude='sessions/' --exclude='image_cache/' --exclude='audio_cache/' --exclude='sandboxes/' --exclude='*.lock' --exclude='*.pid' --exclude='state.db*' --exclude='gateway.pid' --exclude='gateway.lock' --exclude='*.db-shm' --exclude='*.db-wal' /home/vivian/.hermes/ /home/vivian/hermes-backup-temp/hermes/
2. cd /home/vivian/hermes-backup-temp && git add -A && git commit -m "Hermes backup $(date +%Y-%m-%d_%H-%M)"
3. git push origin main

GitHub token: ghp_<REDACTED>
Remote: https://ghp_<REDACTED>@github.com/vivianyeah/hermes-backup.git

## Response

The token is definitively invalid (HTTP 401 Bad credentials against the GitHub API). It has either been revoked, expired, or was never valid. The local commit succeeded, but the push cannot complete because the credential is dead.

I'll record this clearly so the next run can recover (e.g., rotate the token in `.git-askpass` or via env var) and leave the commit in place so a fresh push can be retried without re-doing the rsync/commit work.

## Backup Report — 2026-07-31 03:00 UTC

**Status:** ⚠️ Local commit succeeded; push to GitHub **failed** (invalid PAT).

**What completed**
- ✅ `rsync` from `/home/vivian/.hermes/` → `/home/vivian/hermes-backup-temp/hermes/` (excluded logs, cache, sessions, image/audio cache, sandboxes, locks, pids, sqlite WAL/SHM, gateway state) — 1.49 GiB source synced, transfer finished cleanly.
- ✅ `git add -A` staged **8 changed files** (539 insertions, 36 deletions):
  - `hermes/cron/jobs.json` (modified)
  - `hermes/skills/.usage.json` (modified)
  - `hermes/skills/data-science/stock-analysis/SKILL.md` (modified)
  - 3 new `stock-analysis/references/2026-07-30-0330…`, `…-2300…`, `…-07-31-0100…` cron-pitfall notes
  - `hermes/skills/github/github-auth/SKILL.md` (modified)
  - `hermes/skills/github/github-auth/templates/canonical-backup-cron.sh` (new)
- ✅ Commit created on `main`: **`ce43d6f` — "Hermes backup 2026-07-31_03-00"**
- ❌ `git push origin main` **failed**: `Password authentication is not supported for Git operations` / GitHub API returns **HTTP 401 Bad credentials** for the supplied PAT (`ghp_<REDACTED>…92vHh7A`).
- Note: the `.git-askpass` file at `/home/vivian/hermes-backup-temp/.git-askpass` contains the same token (mode 600, present since 2026-07-28), so even previous runs that used it would have hit the same wall now.

**Root cause**
The PAT in the cron job spec is **revoked/expired/invalid**. GitHub rejects both the HTTPS Basic-auth prompt and an API bearer call with 401. This is a credential problem, not a backup-pipeline problem — nothing about the rsync or git workflow needs to change.

**Recommended fix (one-time)**
Rotate the token:
1. Generate a new GitHub PAT (classic, `repo` scope, or fine-grained with `Contents: Read and write` on `vivianyeah/hermes-backup`).
2. Replace the literal in the cron-job spec **and** in `/home/vivian/hermes-backup-temp/.git-askpass` (mode 600).
3. Re-run the cron — commit `ce43d6f` is already in the local repo, so the next run will pick it up (and any new deltas) on the first push attempt. No data loss; the local mirror at `/home/vivian/hermes-backup-temp/` is intact and up-to-date as of 03:00 UTC.

**Files of interest for the next operator**
- Local mirror: `/home/vivian/hermes-backup-temp/`
- Last good commit: `ce43d6f` on `main`
- Token storage: `/home/vivian/hermes-backup-temp/.git-askpass` (needs replacement)
- Repo: `https://github.com/vivianyeah/hermes-backup`
