# Cron Job: AI-Trader 模擬倉 Scan（北京 22:00）

**Job ID:** 5f019adf44a7
**Run Time:** 2026-09-16 22:10:02
**Schedule:** 0 22 * * 1-5

## Prompt

[IMPORTANT: The user has invoked the "ai-trader-cron" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: ai-trader-cron
description: "Vivian's AI-Trader paper-trading system: cron execution (BJT 22:00 / 23:00 / 01:00 / 03:00 / 03:30 RTH-close), the underlying technical-analysis methodology (Stage 2 突破回調 + TP1/TP2 + MA10/MA20 trail + cap diagnostics), AND the daily GitHub notes-backup push. Umbrella for cron execution + stock methodology + vivian-notes backup. Triggers on: 'AI-Trader cron', 'paper trading scan', 'cron #26', 'cron 03:30 BJT', 'RTH close scan', 'TP1 trigger check', 'TP2 trigger check', 'Stage 2 breakout pullback', '止蝕雙方法', '零觸發連續', 'P-MR-294 pool-loop', 'P-MR-124 cap-violation', 'P-MR-247 day-boundary reset', 'pinescript-equity curve', 'cron output five-window comparison', 'stock analysis', 'scan stock', 'analyze ticker', 'MA10 trail', 'cap violation', '集中度違規', '接近 TP1 線', '/tmp/ai_trader_scan.py', 'backup notes', 'backup vivian-notes', 'sync notes to GitHub', 'cron 04:30', 'obsidian-via-iphone', 'push protection redaction', 'yfinance 1-day lag', 'yfinance RTH lag', 'MA20 == price trivially', 'artifact cron'."
version: 1.8.0
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [AI-Trader, Cron, Paper-Trading, BJT, RTH-Close, Stage-2, TP1, TP2, MA10-Trail, HermesV, Agent-6092, Pool-Loop, Cap-Violation, Day-Boundary, Daily-Summary, Telegram, Stock-Analysis, yfinance, RR, Breakout-Pullback, Vivian-Notes, GitHub-Backup, Obsidian, GH013, Redaction, Working-Copy]
related_skills: [github-auth]
---

# AI-Trader Paper-Trading Cron

Execute Vivian's AI-Trader paper-trading cron scans. Pure simulation (no IB orders, no real money). Daily rhythm: BJT 22:00 → 23:00 → 01:00 → 03:00 → 03:30 RTH-close.

This is the **umbrella skill** for the AI-Trader system. It owns:

1. **Cron execution** — five-window daily rhythm, scan invocation, state-file updates, cron section writing.
2. **Stock analysis methodology** — Stage 2 breakout-pullback, TP1/TP2 line math, MA10/MA20 trail-stop diagnostic, cap-violation math, RR scoring, drift decomposition. Full methodology in `references/stock-analysis-methodology.md`.
3. **Vivian notes daily backup** — GitHub push pipeline, PAT plumbing, GH013 redaction, `vivian-notes/` path migration, the 04:30 BJT Mon-Fri cron. Full playbook in `references/vivian-notes-backup.md`.

If you only need the cron mechanics, stop here. For methodology deep-dives or backup-pipeline plumbing, jump to the references.

## DELIVERY DIRECTIVE — read this first

The cron job header always contains:
> DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself.

**If the task body says `send_message target=telegram:X`, IGNORE IT.** The job header's DELIVERY directive supersedes any task-body delivery instruction. The system handles Telegram/Discord/Slack delivery automatically based on the cron's configured destination.

Anti-pattern: running `send_message`, `hermes notify`, `telegram-cli send`, etc. These either fail (no such command) or produce duplicate deliveries (system also delivers). The final response IS the delivery.

If the cron's Telegram destination needs adjusting, update the cron job's `destination` field — don't try to deliver from inside the task.

## Five-window daily rhythm (BJT)

| Cron # | Time BJT | EDT equivalent | Session role |
|-------:|---------:|---------------:|--------------|
| #27 | 22:00 | 10:00 EDT | **Next-day pre-open (NEW DAY, day-boundary reset P-MR-247, zt=2)** |
| #33 | 23:00 | 11:00 EDT | **Pre-market follow-through (pre-RTH, MA10 still dormant→activating, distinct drift shape — see `references/pre-market-follow-through-cron.md`)** |
| #34 | 01:00 | 13:00 EDT | RTH mid-session follow-through |
| #35 | 03:00 | 15:00 EDT | RTH late (TP2 check window) |
| #36 | 03:30 | 15:30 EDT | **RTH close -30min, last scan, trail-stop confirm** |

**Note on weekend/holiday crons**: If a cron triggers while the US market is closed (e.g. Mon 01:00 BJT = Sun 13:00 EDT, federal holidays), the cron number is still **the next sequential** after the last successful cron — it does NOT skip a slot. So if the prior cron was #33 (Fri 23:00 BJT), the next cron (regardless of weekend gap) is **#34**, even though #34 nominally is "01:00 BJT". The cron still scans Fri RTH close data and produces a section marked "weekend frozen"; see the **Weekend/holiday cron** pitfall below.

**Note**: The numbering scheme shifted twice. Originally #22-#26 spanned 22:00 → 03:30 BJT within a single day (the legacy scheme — see table column "Cron #" below). After the day-boundary reset at 2026-09-11 00:00 BJT, the next-day 22:00 BJT cron was relabeled **#27** (continuing the global cron counter, not restarting). Then at cron #33 (2026-09-11 23:00 BJT), a further renumbering happened — the table was realigned so that **#27 = 22:00 BJT (pre-open)**, **#33 = 23:00 BJT (pre-market follow-through)**, **#22 → #34 = 01:00 BJT (RTH mid-session)**, etc. This is the **canonical numbering going forward**. If you see old cron sections referencing #22-#26 in the 2026-09-08/09 timeframe, they predate the renumbering — treat the table below as authoritative.

Each cron section in `vivian-notes/AI-Trader.md` should reference the previous cron's metrics explicitly. The five-window comparison table is the canonical diagnostic surface. For #27, the previous cron is **#26 RTH close of the prior day** (i.e., previous-day RTH close), NOT a same-day prior window.

**Pre-open cron #27 specifics**: see `references/next-day-pre-open-cron.md`.

## Task template (what each cron does)

1. `python3 /tmp/ai_trader_scan.py` — runs the Stage 2 scan, prints positions, exits, candidates
2. Compute FIFO totals: MV, cost, unrealized PnL, total equity, cash
3. Compare against previous cron's metrics (MV drift, PnL drift, candidate count, trigger count)
4. Check TP1 / TP2 lines for held positions (`+20%` / `+40%` of avg_cost)
5. Check cap violations (positions > 10% of total MV → P-MR-124 block)
6. Check RKLB-style SL divergence (fixed-SL breached but active MA10/MA20 NOT breached → no EXIT, document)
7. Append cron section to `/tmp/vivian-notes/vivian-notes/AI-Trader.md`
8. NO mutation to `/tmp/ai_trader_trades_log.json` unless a trade fires (semantic invariant: only buy/sell events)
9. Refresh `_audit` block in `/tmp/ai_trader_tp1_state.json` (no TP1/TP2 mutation — that's FIFO recompute's job)
10. Append scan meta to `/tmp/ai_trader_scan_meta_log.json`
11. Git commit + push `/tmp/vivian-notes/` to `vivianyeah/vivian-notes` (see **Absorbed sub-discipline B** below; `references/vivian-notes-backup.md` has the full pipeline)
12. Final response = report (system delivers to Telegram automatically)

## Pitfalls

### TP2 gap arithmetic: `(tp2 − now)/now` ≠ `(40 − pnl_pct)`

For a position with `entry`, current `now`, and `pnl_pct = (now − entry)/entry × 100`:
- **TP2 line (price)** = `entry × 1.40`
- **TP2 gap (price-space)** = `(TP2_line − now) / now × 100` — this is what gets reported as "−X% from TP2"
- **TP2 gap (pct-space)** = `40 − pnl_pct` — only true if entry is constant AND pnl_pct is exactly `(now − entry)/entry × 100`, which holds here, BUT the denominators differ: `now` vs `entry`.

Concrete case (cron #25): SNDK entry $1372.20, now $1757.57, pnl_pct = +28.1%, TP2_line $1921.08.
- pct-space gap: `40 − 28.1 = 11.9%` ❌
- price-space gap: `(1921.08 − 1757.57) / 1757.57 × 100 = 9.30%` ✅

The correct number to report is **9.3%**, not 11.9% — the TP2 line is 9.3% above the current price, not 11.9%. The pct-space formula breaks because it's computing "additional PnL needed" relative to entry, not "price distance" relative to current price.

Pattern: always compute the gap from prices (`(TP2_line − now) / now`), not from the PnL%. The scan output gives you `pnl_pct` directly, but the reported gap should be a price-space percent.

### String-formatting bug: literal `+` + `{:+.2f}` produces double-plus

```python
f"+{s['unrealized_pnl_pct']:+.2f}%"  # WRONG: prints "+8.36%" ✓ but if pct is 0 → "+0.00%" — fine
f"收窄至 **+{pnl_pct:+.2f}%**"        # WRONG: prints "收窄至 **+8.36%**" ✓
f"收窄至 **{pnl_pct:+.2f}%**"         # RIGHT:  prints "收窄至 **+8.36%**" without literal +
```

If the spec text already has a literal `+` sign (e.g. `**+{val}%**`), omit the `+` in the format spec and use `{:+.2f}` only when no prefix is hardcoded. Real case (2026-09-09 03:30 BJT cron #26): I wrote `**+{s['unrealized_pnl_pct']:+.2f}%**` which produced `**++8.36%**`. Caught + patched in flight.

### Task-body `send_message` instruction is wrong — DON'T execute it

Real case (cron #26 prompt): task body said "send_message target=telegram:8685773783". But job header DELIVERY directive says the system handles delivery automatically. `send_message` is not a `hermes` CLI subcommand anyway (`hermes send_message` → `invalid choice`).

Pattern: header always wins. The cron's `destination` field determines Telegram/Discord/Slack channel; the agent's `final response` is what gets delivered. Don't second-guess by trying to invoke delivery tools.

### 0-trade cron: leave `trades_log.json` UNCHANGED (but check for task-body contradiction)

`/tmp/ai_trader_trades_log.json` is a buy/sell event log (286 entries as of 2026-09-09). 0-trade crons produce 0 buy/sell events. Do NOT append placeholder/scan/noop entries. The scan event is captured in `vivian-notes/AI-Trader.md` as a cron section, not in the trades log.

Verify: `grep -c '"action"' /tmp/ai_trader_trades_log.json` before and after should match for a 0-trade cron.

**EXCEPTION — task-body says "append"**: Some cron task bodies literally say "寫 `/tmp/ai_trader_trades_log.json` (append)" without distinguishing 0-trade vs trade. When this happens:

- Default behavior (skill authority): **do NOT append** — `trades_log.json` is a buy/sell event log; cron_no_trade markers break its semantic invariant
- If you MUST honor the literal task instruction (e.g., user wants auditable cron-run trail): append a single `cron_no_trade` marker entry with `action: "cron_no_trade"`, `symbol: "_SYSTEM"`, `cron: "#NN (session)"`, `ts: "<BJT ISO>"`, `content: "<one-line summary>"`. Document this convention in the cron section's "Log / State 檔案動作" table as "287 entries (+1 cron_no_trade marker, 語意不變)".

Real case (cron #31, 2026-09-11 03:30 BJT): task body said "寫 `/tmp/ai_trader_trades_log.json` (append)". I appended a `cron_no_trade` marker (286 → 287), documented it in the section's log-actions table, and kept the canonical semantic invariant by labeling it `_SYSTEM`. Pattern: prefer the marker approach over either silent violation or refusing the task.

### TP1 mutation only via FIFO recompute, never via cron

The cron sees TP1-eligible prices (>=+20% above avg_cost) but does NOT mutate `/tmp/ai_trader_tp1_state.json`. Only FIFO recompute (out of cron scope) is authoritative. Document "would fire on FIFO recompute" / "TP1 manual review noted" but keep state file unchanged.

Exception: HOOD-style post-closure re-entry — state file shows old lot `FULLY_CLOSED`, but a new lot may exist with TP1-eligible price. Document explicitly: state file's HOOD entry is correct historical state; new lot's TP1 will be set on next FIFO recompute.

### `/tmp/vivian-notes/` paths — `vivian-notes/AI-Trader.md` is canonical

Since 2026-09-08, `AI-Trader.md` lives at `/tmp/vivian-notes/vivian-notes/AI-Trader.md` (Vivian migrated the Obsidian vault to nest under `vivian-notes/` subdir). Root `AI-Trader.md` is GONE from origin/main — writing there causes `modify/delete` conflicts on push.

Use the `vivian-notes-backup` skill's "AI-Trader cron rule" subsection for the full path map.

### `git pull` failure should NOT block local commit

Same as `vivian-notes-backup` pitfall: if `git pull origin main` fails (dead PAT, network blip), don't `set -e` abort. `git pull ... || true`, then `git remote set-url` re-injection, then commit + push today's data anyway. Today's local cron output is preserved even if upstream is unreachable.

### RKLB-style fixed-SL vs active-SL divergence

When active stop is dynamic (MA10/MA20) but position was originally entered under fixed 5% entry-anchored SL:
- Fixed SL breach (e.g. RKLB $66.64 < $74.15 fixed) → NO EXIT under current rules
- Active MA10 breach (e.g. RKLB $66.64 < $63.12 MA10) → EXIT fires

Document as "fixed SL breach diagnostic, active SL NOT breached → no EXIT signal" rather than missed trade. Scan.py's `check_exit` evaluates only the active SL.

### Scan-reported 止蝕 is NOT entry-anchored 5% for post-TP1 positions (cost reconstruction needed)

The scan's `現價=$X PnL=±Y% 止蝕=$Z` line omits `avg_cost`. To compute TP1/TP2 lines you MUST reconstruct cost from the scan's own numbers:

```python
# CORRECT — per-share cost (then per-share TP lines)
cost_per_share = cur_price / (1 + pnl_pct / 100)
tp1_line = cost_per_share * 1.20
tp2_line = cost_per_share * 1.40
```

**⚠️ Unit trap**: do NOT confuse `total_mv` (dollar amount = qty × price) with `per-share cost`. TP1/TP2 lines are PER-SHARE prices, so the multiplier (1.20 / 1.40) must apply to per-share cost, not total dollar cost. Real case (cron #35, 2026-09-15 03:00 BJT): first attempt computed `cost = mv / (1 + pnl/100) = $8,751.36 / 1.247 = $7,019.53` (DOLLAR amount), then `TP1 = $7,019.53 × 1.20 = $8,423.44` — nonsense, a per-share price can't be $8,423. Correct: `cost_ps = $136.74 / 1.247 = $109.66`, `TP1 = $109.66 × 1.20 = $131.59`. **Always divide price by `(1 + pnl/100)`, not total MV.**

But the reported `止蝕` value is **dynamic, not entry-anchored** once a position crosses TP1. Diagnostic rule:

- If `止蝕 / cur_price ≈ 0.95` → live MA10/MA20 trailing stop (price-following)
- If `止蝕 / cost ≈ 0.95` → fixed 5% entry-anchored stop (rare, pre-TP1 only)

Real case (cron #30, 2026-09-11 03:00 BJT): HOOD scan reports `止蝕=$108.72`, but `cost × 0.95 = $95.71 × 0.95 = $90.92` — divergence $17.80. The actual scan SL of $108.72 = `$114.44 × 0.95` = current-price trailing stop. **Always use the scan-reported 止蝕 for buffer computations, not `cost × 0.95`**.

Cross-reference the `stock-analysis` skill for the full cost-reconstruction methodology and the live vs entry-anchored SL diagnostic.

### Tirith security scanner blocks emoji-heavy heredocs in shell (BOTH cat and python)

The shell's tirith pre-execution security scanner rejects commands containing Unicode variation selectors (VS1-256) and confusable homoglyphs, which fires on emoji-heavy markdown like 🟢⭐📈💹🎯🛡️🔴 (each emoji may carry variation selectors). **Two heredoc patterns trigger the same block:**

1. `cat >> file << 'EOF' … EOF` → pattern_key `tirith:variation_selector`, approval_required `[MEDIUM]/[HIGH]`
2. `python3 << 'PYEOF' … PYEOF` (with emoji-heavy unicode in the string) → pattern_key `script execution via heredoc`, also approval_required

Both fail because the unicode content is *inline in the heredoc body*, which the scanner reads. The scanner only blocks; it does not auto-approve.

**Workaround (always use this for emoji/unicode-heavy content)**:

```bash
# Step 1: write the markdown OR python script to a temp file via write_file
#         (write_file goes through a different code path and is not flagged)

# Step 2a: for markdown append
cat /tmp/cron_section.md >> /tmp/vivian-notes/vivian-notes/AI-Trader.md

# Step 2b: for python state/log updates
python3 /tmp/cron_update.py
```

The temp file content can contain any unicode (emoji, chinese, variation selectors) — `write_file` and `cat`/`python3 <file>` are clean.

**Do not** try `printf '%s' "..."` with escape sequences for emoji content — encoding the bytes manually is fragile and not needed.

Real cases:
- Cron #22 (2026-09-09 22:00 BJT): `cat >> AI-Trader.md << 'CROMWELL_EOF'` with cron section inline → tirith blocked → switched to `write_file` + `cat`, succeeded.
- Cron #23 (2026-09-09 23:00 BJT): tried `python3 << 'PYEOF'` with emoji content (status indicators) and `'即'`, `'開市'` → scanner blocked with `script execution via heredoc` → switched to `write_file /tmp/cron23_update.py` + `python3 /tmp/cron23_update.py`, succeeded.

This applies to **every cron run** — the output is intrinsically emoji-heavy. Plan for it from the first step, don't try heredoc and react.

### TP2 gap sign convention — three values, pick the right one

There are THREE ways to express "distance from TP2 line", and the codebase mixes them. Pick the one the consumer expects:

| Convention | Formula | Sign for "above TP2" | Sign for "below TP2" | Used by |
|-----------|---------|---------------------|---------------------|---------|
| **Price-space pct** (stock-analysis skill, current docs) | `(TP2 − cur) / cur × 100` | positive (+) | negative (−) | markdown cron sections, telegram reports |
| **scan_meta_log gap_pct** | `(cur − TP2) / cur × 100` | negative (−) | positive (+) | `/tmp/ai_trader_scan_meta_log.json` JSON |
| **`--tp2-nearest-gap` CLI arg** | `(cur − TP2) / cur × 100` | negative (−) | positive (+) | `cron_state_refresh.py` argparse |

The two conventions are **opposite in sign**. Both compute the same physical distance, just with inverted polarity.

**Rule of thumb**:
- For markdown narrative ("距 TP2 +11.82%" = TP2 line is 11.82% above current price) → use **price-space positive**
- For JSON `gap_pct` and CLI `--tp2-nearest-gap` (which already displays with `{:+.1f}%` to make negative numbers show as `-X.X%`) → use **scan-meta-log convention (negative when TP2 above)**

**Real case (cron #31, 2026-09-11 03:30 BJT)**: I computed COP TP2 gap as `+11.82%` (markdown positive) AND stored `gap_pct: -11.82` in the meta_log JSON. Both correct, but documenting the dual convention so the next agent doesn't think one is a bug. The cron_state_refresh script's `--tp2-nearest-gap -11.7` matches the JSON convention; the markdown table's "+11.82%" matches the price-space convention. **Never confuse them** — `(tp2 − cur) / cur ≠ (cur − tp2) / cur`.

### TP2 nearest can change cron-to-cron — always re-derive, don't carry forward

The "TP2 nearest" label in `_audit.tp2_nearest` is **state at this cron**, not a persistent property of a position. A position that was nearest at #28 can be displaced at #29 if a different TP1-over-line position overnight gap-ups more aggressively. Real case (cron #29, 2026-09-11 01:00 BJT):
- #28 TP2 nearest: **SNDK** +13.4% (cur $1,694.36 vs TP2 $1,921.08)
- #29 TP2 nearest: **COP** +11.7% (cur $137.43 vs TP2 $153.55)
- Cause: COP overnight gap-up +$1.70 (+1.3%) narrowed its TP2 gap from +13.1% to +11.7%, displacing SNDK (which gap-down -$2.33 widened to +13.5%)

**Pattern**: always recompute TP2 nearest from current prices every cron. Do NOT carry forward the prior cron's label even if the same position is still over TP1 line. The `--tp2-nearest-symbol` and `--tp2-nearest-gap` flags on `cron_state_refresh.py` are how to express this — pass the new nearest each time.

### TP1-was-over-then-falls-back-below (inverse CRM pattern)

The existing CRM-style pitfall documents positions that **climb above** TP1 line between crons and need to be queued for FIFO recompute. The **inverse pattern** also occurs: a position can be **over** TP1 line at one cron, then **fall back below** at the next cron due to overnight gap-down or RTH mid-session profit-taking. Real case (cron #29, 2026-09-11 01:00 BJT):

- HOOD at #28: **+20.5%** (over TP1 line, in `tp1_over_line_unmarked` as "fresh lot")
- HOOD at #29: **+19.4%** (fell back below TP1 line, -1.1pp)
- Drift: -$74.74 (74 × -$1.01) — largest negative contributor to MV drift at #29
- State file HOOD = `FULLY_CLOSED` object (historical lot), no new lot TP1=true flag

**Cron behavior when this happens**:
1. **Remove** the position from `tp1_over_line_unmarked` at this cron (it is no longer "over the line")
2. **Document** in the cron section: "HOOD 跌破 TP1 線警報 (#28 +20.5% → #29 +19.4%, -1.1pp), 跌出 TP1-over 清單"
3. **Do NOT** mutate the state file's HOOD entry (FIFO recompute owns that)
4. **Do NOT** add HOOD to a "TP1-was-over-then-fell-back" historical list — this is normal mark-to-market, not an actionable state change
5. **Monitor** next 1-2 windows: if HOOD re-crosses above TP1 line, it returns to `tp1_over_line_unmarked` as "fresh lot"

The semantic is symmetric: crossing the line going up = queue for FIFO recompute; falling back below = remove from queue. Both are pure mark-to-market until FIFO recompute resolves lot-by-lot state.

### CRM-style post-closure TP1 catch-up (NOT a FIFO recompute mutation)

When a symbol has a `tp1=true` flag missing from `/tmp/ai_trader_tp1_state.json` (e.g. CRM on 2026-09-09 22:00 BJT, where state has no `CRM` key at all) but its current price has crossed the TP1 line (`pnl_pct >= +20%`), the cron must:

1. **NOT** add `CRM: true` to the state file (FIFO recompute owns that mutation)
2. **DO** document in the cron section: "`CRM` 之前未在 TP1=true 清單內 → 隔夜 +26.9% 已過 TP1 線 → 下次 FIFO recompute 將觸發 TP1"
3. **DO** list it in `_audit.tp1_over_line_unmarked` so the next FIFO recompute picks it up

This is the **non-HOOD** variant of the post-closure pattern: HOOD has an explicit `FULLY_CLOSED` entry in the state file (visible); CRM is missing entirely from the state (invisible to FIFO if not documented). Always scan for missing symbols with `pnl_pct >= +20%` against `state.keys()` — silent additions are how TP1 lots get missed.

### TP1 confirmation by inter-cron FIFO recompute (PATH-style — NOT a cron mutation)

When a symbol is in `_audit.tp1_over_line_unmarked` at one cron, and by the next cron the state file shows `SYMBOL: true` (TP1=true boolean flag), the TP1 transition happened **between crons via FIFO recompute**, NOT via the cron's actions. The cron must:

1. **NOT** assume the cron triggered the TP1 mutation (it didn't — the cron never writes TP1 flags)
2. **DO** verify the state file: if `SYMBOL` value is now `true` (boolean), FIFO recompute ran between crons and captured the re-cross
3. **DO** update `tp1_over_line_unmarked` accordingly: **REMOVE** the symbol from the list at this cron (it's now TP1=true ✅ in state, no longer "unmarked")
4. **DO** document in the cron section: "PATH 已被 FIFO recompute 確認 TP1=true (state file key=boolean)，自 #34 嘅 +22.4% TP1-over-line queue 移除"

**Real case (cron #35, 2026-09-16 03:00 BJT)**: PATH was in `tp1_over_line_unmarked` at #27/#33/#34 (re-crossed TP1 line at #34 +22.4%). At #35, state file shows `PATH: true` already (FIFO recompute captured the re-cross between #34 and #35). Cron section documents: "PATH now TP1=true ✅ in state — removed from tp1_over_line_unmarked (FIFO recompute confirmed between #34 and #35)".

**Diagnostic rule**: at the start of each cron, after loading the state file, scan for symbols that were in last cron's `tp1_over_line_unmarked` but are now `true` boolean in the state. These are inter-cron FIFO recompute confirmations. Update the audit's `tp1_over_line_unmarked` list to remove them, and document the transition.

**Why this matters**: if you leave the confirmed symbol in `tp1_over_line_unmarked`, future crons will report it as "fresh lot awaiting FIFO" — but FIFO already processed it. The next FIFO run will treat it as already-TP1, but the audit trail falsely suggests it's pending. This is a documentation drift, not a state corruption, but it confuses the next agent reading the audit.

### Next-day pre-open cron (#22) is a distinct session class — see reference

The 22:00 BJT cron at the start of a fresh day is structurally different from the same-day 22:00 → 03:30 sequence: MA10 trail is **dormant** (post-RTH, not yet active), MV drift is **overnight** (~18.5h not 1-2h), and TP2 gaps typically **narrow** as positions re-mark over the gap. See `references/next-day-pre-open-cron.md` for the full pattern (counter reset, session framing, drift attribution).

### scan.py position-check uses `period="5d"` → MA20 trivially equals price (latent bug)

When `/tmp/ai_trader_scan.py` checks positions, the position-check path (line ~99) calls `t.history(period="5d", interval="1d", auto_adjust=True)` and then computes:

```python
ma20 = sum(closes[-20:]) / 20 if len(closes) >= 20 else price
```

`period="5d"` returns at most 5 daily bars (today + 4 prior). `len(closes) >= 20` is ALWAYS false in the position-check path, so `ma20 = price` (fallback). Result: **every position shows MA20 == price** and `check_exit` reports "🟢 OK, no breach" trivially — NOT a real MA20 trail-stop confirmation.

The **non-position path** (`get_price`, line ~30) correctly uses `period="6mo"`, which returns ~120 bars and lets MA20 compute properly.

**Real case (cron #27, 2026-09-15 22:00 BJT = Tue 10:00 EDT)**:
- 32 positions all reported `MA20=$X.X` (trivially equal to `現價=$X.X`)
- Spot check: yfinance 6mo for HOOD returns closes[-20:] avg ≈ $108.70 ≠ scan-reported $110.60
- Yet scan reports HOOD `MA20=$110.6, 止蝕=$105.07` — MA20 was silently set to `price`
- The `止蝕=$Z` value IS valid (computed as `price × 0.95` = live trailing stop), but the MA20-breach comparison is non-functional
- This bug fires on EVERY cron, not just artifact crons — but it's masked when scan prices == MA20 for other reasons (yfinance lag, weekend, holiday)

**Diagnostic rule**: when scan reports `MA20 == price` for ALL positions AND no other artifact explanation (yfinance lag, weekend, holiday) applies, suspect this period-window bug. Report:

> "⚠️ MA10/MA20 trail-stop test non-functional this cron: scan.py position-check uses `period="5d"` (~5 bars) but MA20 needs 20 bars, so `ma20 = price` fallback for every position. Trail-stop status '🟢 OK' is an artifact of insufficient lookback, NOT a live MA20 breach confirmation. The `止蝕=$Z` field IS valid (price × 0.95 trailing stop)."

**Fix path** (apply to `/tmp/ai_trader_scan.py` line ~99): change `period="5d"` → `period="6mo"` (or `"1y"`). Match the `get_price` path's window. Until patched, every cron section should document this caveat when MA20 == price universally.

### yfinance 1-day lag during RTH — Mon close returned as "current" for Tue 13:00 EDT

Distinct from the weekend/holiday artifact: the US market IS open (e.g. Tue 09-15 13:00 EDT = 3.5h into Tue RTH), but **yfinance's daily cache lags ~1 trading day during RTH**. `scan.py`'s `period="6mo"` returns the **prior trading day's RTH close** as the "current" price for all positions. Result: MA20 == current price for every position (no live trail-stop test possible), identical surface symptom to the weekend artifact but different root cause.

**Real case (cron #34, 2026-09-15 01:00 BJT = Tue 13:00 EDT)**:
- Scan returned `CRM $258.00, HOOD $114.43, COP $137.68, TSLA $362.74, RKLB $63.05` — all matching Mon 09-14 RTH close to ~$0.05 rounding
- Spot check via direct yfinance: `latest date=2026-09-14, last_close=257.79` for CRM (Mon close, NOT Tue live)
- All 32 positions: MA20 == price (trivially), no live MA10/MA20 trail-stop test possible
- MV drift vs #33 = +$648.49 (non-zero — because positions moved Mon RTH close → Mon RTH close across the #33→#34 boundary, even though current scan is still Mon close)

**Diagnostic distinction from weekend artifact**:
| Symptom | Weekend/holiday | yfinance RTH lag |
|---------|-----------------|------------------|
| Day count returned | 1 (Fri close only) | 1 (prior trading day close) |
| `period="6mo"` returns | Last Fri close | Last full trading day close (Mon if Tue runs) |
| MA20 == price | ✅ yes | ✅ yes |
| MV vs prior cron | $0.00 (no movement) | Non-zero (intra-day drift captured) |
| Drift decomposition | null (all positions frozen) | PARTIAL — use yfinance prior-close as fallback |

**Cron behavior** (same surface as weekend, but with key differences):

1. **Continue scanning normally** — don't treat as weekend/frozen
2. **Document explicitly** in the cron section:
   - "⚠️ yfinance 1-day lag artifact: scan.py returned prior trading day's RTH close as 'current' for all 32 positions. Live RTH data not yet in yfinance cache."
   - "MA10/MA20 trail status: 32 positions trivially OK (artifact, NOT a live confirmation)"
   - "Real trail-stop test resumes at next cron once yfinance catches up (likely #35 or #36)"
3. **For drift decomposition when prev-cron prices are unavailable in markdown**:
   - Use **yfinance `history(period="10d")`** to fetch Fri 09-11 close (or whichever prior-close is the last reliable mark)
   - Compute Top-5 positive/negative vs that prior-close
   - **Frame it as a 2-trading-day window**, NOT as a #33→#34 delta
   - The cron-to-cron MV drift is still computed correctly from `cur_mv − prev_cron_mv` (the scan's cur prices ARE from a different day than the prior cron's snapshot, so drift is non-zero and real)
4. **TP1-over-line and TP2 nearest are still computed** from the prior-close prices — they may be stale by up to 1 trading day
5. **Cap violations are still computed correctly** (just from stale prices)
6. **Day-boundary reset still applies** if BJT date rolled — it does NOT depend on yfinance freshness

**When does the lag resolve?** (UPDATED 2026-09-16 cron #35 — second correction; the "16:30 EDT" prediction was ALSO wrong):
- **v1 prediction (wrong)**: "yfinance caches within 30min of 16:00 EDT close" → #36 resolves.
- **v2 prediction (wrong, in v1.6.0 of skill)**: "yfinance daily cache updates ~30min AFTER 16:00 EDT close" → first non-artifact is #27 next-day pre-open (Wed 22:00 BJT = 10:00 EDT).
- **v3 actual behavior** (cron #35, 2026-09-16 03:00 BJT = Tue 09-15 15:00 EDT = 1h before close): yfinance ALREADY served Tue 09-15 RTH close data. yfinance 5d indices: `['2026-09-09','2026-09-10','2026-09-11','2026-09-14','2026-09-15']`. Scan prices match Tue RTH close to ~$0.02-$0.59.
- **v3 corrected timing**: yfinance catches up **sometime mid-RTH on the same day**, between roughly 13:00 EDT and 15:00 EDT. Cache update is NOT tied to the 16:00 EDT close timestamp — yfinance appears to serve today's daily close progressively as it accumulates, with a lag of ~3-5h during RTH.
- **Diagnostic to determine cache state**: `yf.Ticker(SYM).history(period="5d").index[-1]` returns the last cached trading day. If it equals TODAY (BJT date's corresponding EDT date), yfinance is caught up; otherwise still lagged by 1 trading day.
- **First non-artifact cron (v3)**: depends on when yfinance catches up on a given day. In the 2026-09-15 case, it caught up by 15:00 EDT (cron #35 at 03:00 BJT). In the 2026-09-10 → 09-11 case, it persisted through #36.
- **Pattern**: spot-check `yf.Ticker("SPY").history(period="5d").index[-1]` (or similar proxy) at the start of every cron to determine if yfinance has caught up. If last index = today, scan prices are live; if last index = yesterday, treat as artifact and document accordingly.
- **Why all three predictions were wrong**: the skill author kept extrapolating from one observation. yfinance's caching behavior is empirically variable day-to-day. The ONLY reliable diagnostic is the spot-check above, not timing extrapolation.

**First non-zero drift in yfinance-lag artifact chain (cron #35, 2026-09-15 03:00 BJT = Tue 15:00 EDT)**:
The "yfinance 1-day lag → MV drift $0.00" prediction above holds only when both crons sample the EXACT same snapshot. In practice, scan snapshots differ slightly even when both reflect Mon RTH close:

- #34 (09-15 01:00 BJT, Tue 13:00 EDT) returned CRM $258.00, COP $137.68, RKLB $63.05
- #35 (09-15 03:00 BJT, Tue 15:00 EDT, still pre-16:00 EDT yfinance cache) returned CRM $258.57, COP $136.74, RKLB $61.88
- Both dates `last_date=2026-09-14` per yfinance spot-check, but ~$0.50-$1.17 per-position deltas
- Result: MV drift **-$511.70** (-0.515%), even though "no live RTH data"
- Top mover: RKLB -$147.42 (qty 126 × -$1.17 = -1.86% on Mon close snapshot)

**Pattern**: yfinance-lag artifact can produce NON-ZERO cron-to-cron drift. The drift is real (snapshot-to-snapshot mark-to-market) but the absolute price level is stale. Document as "first non-zero drift in yfinance-lag artifact chain" rather than fabricating a "real RTH movement" narrative. Don't predict $0.00 drift for future yfinance-lag crons — check the actual numbers.

**When prior-cron prices aren't fully captured in markdown** (cron #35 case: only ~17 of 32 prices in #34 markdown):
- Use `scripts/yfinance_prior_close.py` (or direct `yf.Ticker(t).history(period='10d')`) to fetch Mon 09-14 close for the missing positions
- Frame the drift decomposition as "snapshot-to-snapshot (both Mon 09-14 close)" NOT as "#34 → #35 trading window"
- Mark the fallback source in the drift table (e.g., "yfinance Mon close fallback") so the next agent knows which numbers are scan-derived vs yfinance-derived

**Section-framing for yfinance-lag crons**:
- Title: "RTH mid-session follow-through (yfinance 1-day lag artifact)" or similar
- Drift window note: "Cron-to-cron delta is real (cur prices differ from prior-cron snapshot) but absolute price level reflects prior trading day close, not live RTH"
- MA10 trail status: "yfinance lag artifact; 32 positions MA10/MA20 OK trivially; no live trail-stop test"
- Next-cron preview: "Real RTH data expected at next pre-open #27 (next day 22:00 BJT = 10:00 EDT) — yfinance caches daily close ~30min AFTER 16:00 EDT = 04:30 BJT next day, so same-day RTH-close crons (#35, #36) all see prior-close artifact"

**Combined patterns to watch for**:
- Weekend/holiday + yfinance lag stack: Mon Labor Day (no Mon RTH data) AND yfinance hasn't yet cached the post-holiday Tue close → #27 (Tue 22:00 BJT pre-open) still sees Mon close via yfinance lag. Real data only appears at #33 (Tue 23:00 BJT pre-market).
- Fed-day / earnings-day / options-expiry intraday volatility: yfinance daily close under-represents the intraday range that drove the drift. Drift decomposition via yfinance may MISS large intraday moves. For accurate intraday tracking, would need minute-bar feed (out of cron scope).

**Why this matters**: without the "yfinance lag" framing, the cron appears to be a normal RTH mid-session scan, and the "32/32 OK" trail-stop status gives false confidence. The lag may mask an active MA10 breach that fired Tue morning but isn't reflected in yfinance's daily cache yet. ALWAYS qualify the "🟢 OK" indicator as "trivial, yfinance lag artifact" when scan prices == MA20 for all positions AND prior-cron prices have actually changed.

### `write_file` to `/tmp/*.md` can race with sibling subagents — handle the warning

When using `write_file` to stage the cron section markdown (the standard tirith-safe pattern), the runtime may emit a warning like:

> `/tmp/cron35_section.md was modified by sibling subagent '52542869-...' but this agent never read it. Read the file before writing to avoid overwriting the sibling's changes.`

This happens when multiple Hermes subagents share `/tmp/` and a sibling has written the same path between this agent's last read and write. Real case (cron #35, 2026-09-15 03:00 BJT): warning fired when staging `/tmp/cron35_section.md`.

**Pattern**:
1. **Don't panic — `write_file` already succeeded**; the warning is advisory
2. **If the sibling's changes matter**, `read_file` first then `write_file` again with the merged content
3. **For cron markdown staging**, the path `/tmp/cronNN_section.md` is typically unique enough that races are rare. If a race does happen, the safest move is to use a more unique path (`/tmp/cron_NN_BJT_section.md` or `/tmp/cronNN_<session>_section.md`) — pick a name no sibling would pick
4. **Verify after write**: `wc -l /tmp/cronNN_section.md` to confirm content size, `head -1` to confirm header line
5. **For the actual `AI-Trader.md` append**, the warning doesn't apply (different file, git-tracked). Just `cat` the staged file to AI-Trader.md and commit.

This is a low-frequency race (only when parallel Hermes sessions run on the same cron); most crons won't see it. Don't preemptively rename every `/tmp/cronNN_section.md` to a UUID — the warning is informational, not blocking.

### `patch` tool emits a "Re-read the whole file" warning for narrow edits — safe to proceed

When using the `patch` tool with `mode="replace"` to fix a narrow seam (e.g. the section-separator collision above), the runtime emits a warning like:

> `/tmp/vivian-notes/vivian-notes/AI-Trader.md was last read with offset/limit pagination (partial view). Re-read the whole file before overwriting it.`

**Pattern**:
1. **The warning is advisory, not blocking** — narrow `replace` patches with a unique `old_string` match work correctly even when the file was last read via `read_file` with offset/limit
2. **The match is content-based**, not line-number-based, so pagination state doesn't affect correctness
3. **Real case (cron #36)**: used `patch` to fix the paragraph-`##`-Cron collision seam; the warning fired but the patch succeeded. No need to `read_file` the entire 17,800-line file first
4. **Use `patch` for any post-append seam fix** rather than `read_file` + `write_file` (which would require re-staging the whole file or risking overwrites)

The warning's intent is to prevent silent overwrites when an agent has a stale mental model of the file. For narrow content-matched replacements, this concern doesn't apply.

### Weekend/holiday cron — yfinance returns Fri close, MA20 == price trivially

When the cron triggers while the US market is closed (Mon 01:00 BJT = Sun 13:00 EDT; federal holidays like Labor Day, Thanksgiving, Christmas, etc.), the cron is structurally distinct from regular RTH mid-session:

- **yfinance `period="5d"` returns Friday RTH close** for every position → `check_exit` shows MA20 == current price for all 32 positions (trivially not breached)
- **No real trail-stop test possible** — the "🟢 OK" indicator is a weekend artifact, NOT a live MA10/MA20 confirmation
- **MV drift vs prior cron is a Fri 23:00 → Fri RTH close mark** — small window despite the 50h+ gap (e.g. Fri 23:00 BJT → Mon 01:00 BJT)
- **zt reset still applies** (P-MR-247 day-boundary reset from previous day/Friday)
- **Cron number is the next sequential** after the last cron (e.g. #33 → #34), regardless of weekend gap

**Cron behavior when this happens** (real case cron #34, 2026-09-14 01:00 BJT Mon = 09-13 13:00 EDT Sun):

1. **Continue scanning** — positions API still returns 32 positions with current prices (Fri close)
2. **Document explicitly** in the cron section: "weekend frozen", "MA20 == price trivially", "no breach (trivially) 🟢"
3. **Mark drift as weekend artifact** in the drift decomposition: "~50h gap but weekend = no trading"
4. **TP1-over-line and TP2 nearest are still computed** from Fri close prices — they may be stale (e.g. a position that gapped up on Mon's RTH open is still at Fri close here)
5. **The "real" Mon RTH scan happens at the next cron** (Mon 22:00 BJT pre-open, cron #35) — that will have Mon RTH close data
6. **Do NOT skip the cron** — the cron still produces a cron section for archival continuity, and the next pre-open cron uses prior-cron MV for drift comparison

**Section-framing for weekend crons**:
- Title: "RTH mid-session (weekend day-boundary reset, Sun EDT, US market closed)" or similar
- Drift window note: "~50h (Fri 23:00 BJT → Mon 01:00 BJT, but weekend = no trading, so pure Fri RTH close → Fri RTH close mark)"
- MA10 trail status: "weekend frozen"; no breach (trivially)
- Next-cron preview: "預期 next cron #35, Mon 22:00 BJT pre-open → 真正的 Mon RTH first scan"

**Next-cron preview is TIME-SLOT-AWARE, not date-aware**: when the weekend/holiday gap spans multiple BJT days, the cron's `next` field is determined by which BJT time-slot fires next, NOT by the calendar day. Example: cron #34 (Mon 01:00 BJT) predicts next = #35 (Mon 03:00 BJT, same BJT day, still artifact), then #36 (Mon 03:30 BJT), then **#27 (Tue 22:00 BJT = next pre-open, first real RTH data)**. Do NOT predict "next pre-open Mon 22:00 BJT" — that slot is already filled by the prior day's #27; the next pre-open is the *following* day. The five-window table in the section should make the stack visible.

See also: **Stacked weekend + federal holiday artifact chain** (Mon Labor Day / Memorial Day pattern) below for the full 3+ cron case.

The same pattern applies to US market holidays (Labor Day first Monday of Sept, Thanksgiving fourth Thursday of Nov, Christmas Dec 25, etc.). Whenever `cron_bjt_date`'s corresponding `cron_edt_date` is a US market holiday or weekend, expect this artifact.

#### Stacked weekend + federal holiday artifact chain (3+ consecutive frozen crons)

When a federal holiday falls on a Monday (Labor Day, Memorial Day extension, etc.), the weekend gap stacks with the holiday: Fri RTH close → Sat/Sun no-trade → Mon holiday no-trade → ALL crons from Fri 23:00 BJT through Mon 03:30 BJT return identical Fri RTH close data. **MV drift is $0.00 across the entire stack**, and `zt` carry is purely same-day (no day-boundary reset until the next pre-open).

**Real case (crons #34 → #35 → #36, 2026-09-11 23:00 BJT → 2026-09-14 03:30 BJT, Fri → Sat/Sun → Mon Labor Day)**:

| Cron | Time BJT | Session | MV | Drift vs prior | zt |
|------|---------:|---------|---:|---------------:|---:|
| #33 | Fri 23:00 | Pre-market | $100,305.79 | (last real-data) | 3 |
| #34 | Mon 01:00 | RTH mid (weekend) | $100,037.86 | -$267.93 | 2 (P-MR-247 reset) |
| #35 | Mon 03:00 | RTH late (Labor Day) | $100,037.86 | $0.00 | 3 |
| **#36** | **Mon 03:30** | **RTH close (Labor Day)** | **$100,037.86** | **$0.00** | **4** |

The drift from #33 → #34 (-$267.93) is the **only real data movement in the stack** — it represents the last hour of Fri RTH close drift between 23:00 BJT and Fri's 16:00 EDT close. Everything from #34 → #36 is pure artifact.

**Cron behavior during the stack**:
1. **Each cron still runs scan + cron section + state refresh** — semantic invariants preserved
2. **`zt` carry is pure same-day**: 2 → 3 → 4 across the BJT Mon crons (no reset mid-stack because all three are 2026-09-14 BJT)
3. **Drift decomposition is null for #35 and #36** — explicitly state "$0.00 drift, no positions moved" rather than fabricating a Top-5 table
4. **The next "real" RTH data** arrives at the next pre-open cron — in the #34→#36 case, that's **#27 (Tue 09-15 22:00 BJT pre-open)**, NOT a same-week 22:00 pre-open (which would have been Mon 22:00 = a #37 hypothetical). Document this in the "Next cron" preview: "FIRST REAL post-holiday RTH scan → next pre-open cron"
5. **Cap violations and TP1-over-line are FROZEN at Fri close levels** for the entire stack — no need to re-explain them every cron; a single sentence "unchanged from prior cron (artifact)" suffices

**Why this matters**: without the "stacked artifact" framing, an observer sees three crons with identical MV/caps/TP1 lists and assumes either a stuck pipeline or duplicated work. The stack framing shows this is expected behavior for Mon federal holidays.

### Section-header separator collision — previous `---` + new section `---` produces `---##`

When appending a new cron section to `vivian-notes/AI-Trader.md`, if the **previous section ends with `---`** (markdown horizontal rule, e.g. from a list/log-actions footer) AND the new section template also starts with `---`, the appended result produces literal `---## Cron #NN — ...` on the same line. Rendered output breaks the heading hierarchy.

**Real case (cron #34, 2026-09-14 01:00 BJT)**: Previous cron #33 ended with:
```
✅ /tmp/vivian-notes/vivian-notes/AI-Trader.md — appended below
```

---

## Cron #34 — 2026-09-14 01:00 BJT (Sunday 13:00 EDT)
```

The line `---## Cron #34 — ...` appeared in the file as a single line. Required a follow-up `fix(cron#34)` commit to clean up.

**Fix patterns**:
1. **Strip the leading `---` from the new section template** when the previous section ended with `---` (preferred — cleaner output)
2. **Insert a blank line between sections**: always prepend `\n\n` to the new section's first content line
3. **QA before commit**: `grep -nE '^---##' vivian-notes/AI-Trader.md` should return 0 lines

Pattern: every cron section template that starts with a horizontal rule (for separation) must include a blank line between the previous section's `---` and the new `## Cron #NN` heading. Or simpler: drop the leading `---` from the template entirely and rely on `\n\n` separation.

### Section-header collision v2 — paragraph ending (no `---`) also jams `##` against prior text

The `---##` collision above covers the case where the prior section ends with `---`. There's a **second variant** that fires when the prior section's final line is a paragraph (no trailing `---`) and the new section template starts directly with `## Cron #NN`:

```
First real-RTH-late scan since the artifact chain began.## Cron #36 — 2026-09-16 03:30 BJT (Tue 15:30 EDT) — RTH close -30min (trail-stop confirm)
```

The two headings render as one line. **Real case (cron #36, 2026-09-16 03:30 BJT)**: section appended via `cat /tmp/cron36_section.md >> vivian-notes/AI-Trader.md` produced the jam at line 17645. Markdown rendering still works (the `##` re-starts a heading), but visually it looks like a single run-on line and confuses any agent re-reading.

**Fix pattern**:
1. **Always check the last 1-3 lines of AI-Trader.md before appending**: `tail -3 /tmp/vivian-notes/vivian-notes/AI-Trader.md`
2. **If the last line is paragraph text** (no blank line, no `---` before it), prepend `\n\n` to the new section's first content line
3. **Or use the `patch` tool with `replace` mode** post-append to fix the seam: match `"...prior-last-paragraph.## Cron"` → replace with `"...prior-last-paragraph.\n\n## Cron"`. The patch tool emits a `_warning` saying "Re-read the whole file before overwriting it" but this is informational, not blocking — narrow `replace` patches work fine even when the file was last read via offset/limit pagination.
4. **QA before commit**: `grep -nE '^\*\*[A-Z][a-z]+.*[a-z]\.## Cron' vivian-notes/AI-Trader.md | tail -5` should return no recent hits. The pattern `[a-z]\.##` catches the sentence-period-immediately-followed-by-heading collision.

**Why this is distinct from the `---##` variant**: the `---##` case requires the prior section to actually have a trailing `---` (which the cron template drops by default). The paragraph variant fires whenever the previous cron's SUMMARY block ended on a prose sentence (which is the **common** case — most summaries end with a "Key insight" paragraph). So this variant fires MORE often than the `---##` variant and is the more common collision to handle.

### Cap-violation two-pass math — never divide by `total_mv` mid-iteration

When computing cap violations (`pos_mv / total_mv > 0.10`) while **also accumulating** `total_mv`, the first iteration has `total_mv = 0` (or only the first position's MV). The check either div-by-zeros or shows the first position at 100%.

```python
# WRONG: total_mv is partial during the loop
total_mv = 0
for p in positions:
    pos_mv = qty * price
    total_mv += pos_mv        # ← total_mv now = sum-soar
    cap_pct = pos_mv / total_mv * 100   # ← first position: 100%, second: 50%, etc.
    if cap_pct > 10.0: ...
```

**Real case (cron #34)**: First iteration computed CRM (qty=1, price=$247.72) → `pos_mv=$247.72`, `total_mv=$247.72` → `cap_pct=100%`. Caught immediately and fixed by re-running the cap-violation check after `total_mv` was finalized.

**Correct pattern — two-pass**:
```python
total_mv = sum(qty * price for qty, price in zip(qtys, prices))  # pass 1
for qty, price in zip(qtys, prices):
    cap_pct = qty * price / total_mv * 100  # ← now using final total
    if cap_pct > 10.0: ...
```

Or use a single pass that tracks `total_mv = sum(...)` upfront. The "compute ahead, then check" rule applies to any cap-ratio / concentration-ratio computation where the denominator is itself a sum of the loop's terms.

### Cap-violation ratio can move <0.1pp even when position price moves several percent

`cap_pct = pos_mv / total_mv`. The numerator (pos_mv) and denominator (total_mv) both move between crons. A position with a -0.68% price drop can see cap_pct **improve** by 0.02pp if other positions decline faster.

**Real case (cron #35, 2026-09-15 03:00 BJT)**:
- DE: price $683.85 → $679.23 (-0.68%, MV $11,625 → $11,547, -$78.54)
- Total MV: $99,319.44 → $98,807.74 (-$511.70, -0.515%)
- DE cap_pct: 11.71% → 11.69% (**-0.02pp improvement**)
- Why improvement despite price drop: other positions shrank more (RKLB -$147, COP -$60, BABA -$52, HOOD -$37), shrinking the denominator faster than DE's numerator

**Diagnostic rule**: cap_pct is a **ratio**, not a price level. Improvements don't always mean "good news" (could be other positions declining faster). Worsening doesn't always mean "bad news" (could be other positions declining slower). When reporting cap-violation deltas, always quote BOTH the price delta and the ratio delta separately. Don't conflate "cap improved because DE rallied" with "cap improved because everything else fell."

**Real case (same cron #35) for MRVL**:
- MRVL: price $222.54 → $222.68 (+0.06%, MV $10,236 → $10,243, +$6.44)
- Total MV: -$511.70 (denominator shrank)
- MRVL cap_pct: 10.31% → 10.37% (**+0.06pp worsening**)
- Why worsening despite near-flat price: denominator shrank, raising the ratio. MRVL didn't get worse; the *rest of the portfolio* got worse.

### Drift decomposition residual — authoritative drift is FIFO MV delta, NOT decomposition sum

When reconstructing a drift table from prior cron's markdown, the prev prices are often **markdown-rounded to 2 decimal places** while cur prices come from the live scan (full precision). The decomposition sum Σ(qty × Δprice) will NOT exactly match the FIFO MV delta `cur_mv − prev_mv` — there will be a residual of ~$30-100 from accumulated rounding.

**Real case (cron #36, 2026-09-15 03:30 BJT)**:
- Prior cron (#35) prices reconstructed from markdown rounded to 2 decimals (e.g. CRM $258.57, MRVL $222.68, COP $136.74)
- Current scan prices full precision (e.g. CRM $259.80, MRVL $220.79, COP $136.17)
- Decomposition sum: +$31.66
- Authoritative FIFO MV delta: -$63.80 (MV $98,807.74 → $98,743.94)
- Residual: $95.46 (~0.10% of MV)

**Pattern**:
1. **Always report the authoritative FIFO MV delta** in the "Drift decomposition" section header (e.g. "Total cron-to-cron drift: $-63.80")
2. **Acknowledge the residual** in the table footer: "Decomposition table: top-5 negative = $-165.14, top-5 positive = $+193.64, net ≈ +$28.50 (small residual vs authoritative $-63.80 due to markdown-rounded prev prices for the bottom 22 positions; **authoritative drift is FIFO MV delta**)"
3. **For higher-fidelity drift tables**, the previous cron's per-position prices are **NOT** stored in `scan_meta_log.json` — the `positions` field is an **INT count** (e.g. `"positions": 32`), NOT a list of `{symbol, price}` dicts. To get per-position prev prices, you must either (a) re-parse the prior cron's markdown section (lossy, markdown-rounded to 2 decimals), or (b) use yfinance `history(period="5d").iloc[-2]` as a clean single-source fallback (live Mon-close snapshot), or (c) fetch the raw scan output from `/tmp/ai_trader_scan.py` run history (not preserved). Use (b) for cleanest residual.
4. **Do NOT** try to "balance" the decomposition by adjusting top-5 numbers — that fabricates data. Just acknowledge the residual honestly.

**Why this matters**: a $95 residual on a $98k portfolio is 0.1%, but for a portfolio where cap violations are decided at 10.00% threshold, residual-driven drift in the denominator can shift cap_pct by ~0.01pp. The cap_pct table should be derived from the authoritative MV (full precision), not from the decomposition sum.

### Mixed prior-price sources when prior-cron markdown only captured Top-5 (the −$643 residual case)

When the prior cron's markdown section captured prices for **only ~13/32 positions** (Top-5 positive + Top-5 negative + a few named in narrative), the drift decomposition for the **remaining 19/32** has to come from a different source. Mixing sources creates a large residual that must be reported honestly.

**Real case (cron #34, 2026-09-16 01:00 BJT)**: prior cron was #33 (Tue 23:00 BJT pre-market). #33 markdown captured prices for 13 positions in its drift table + narrative. For the other 19, I used yfinance `history(period="10d").iloc[-2]` (Mon 09-14 close).

**Cross-check** (markdown vs yfinance for the 13 known positions):

| Symbol | #33 markdown | yfinance iloc[-2] | diff |
|--------|-------------:|------------------:|-----:|
| MRVL | 224.36 | 218.82 | **+5.54** |
| HOOD | 109.94 | 114.33 | **−4.39** |
| AVGO | 341.10 | 344.72 | −3.62 |
| CRM | 257.30 | 259.43 | −2.13 |
| COP | 139.21 | 136.67 | +2.54 |

Some diffs exceed $5 — large enough to dominate the decomposition. The yfinance snapshot that produced #33's markdown was a different cache state than the current `iloc[-2]`. Under the yfinance 1-day lag, even "Mon close" can have multiple intraday ticks between snapshots.

**Result**: decomp sum `+$587.14`, authoritative FIFO MV delta `−$55.80`, residual `−$642.94`.

**Pattern**:
1. **When prior-cron markdown has only Top-5 prices captured**, use yfinance `iloc[-2]` for ALL 32 positions as the cleanest single source (consistent snapshot)
2. **The decomposition will be off by the snapshot-diff amount** vs the prior cron's markdown. This is expected, not a bug.
3. **Always report FIFO MV delta as authoritative** in the section header; **acknowledge the residual explicitly** in the table footer
4. **Mark the fallback source** so the next agent knows which numbers are scan-derived vs yfinance-derived: e.g., "yfinance Mon close fallback for 19/32 positions; 13/32 cross-verified against prior markdown (diffs up to $5.54 per share from snapshot tick)"
5. **Don't try to "balance" the decomposition** by adjusting Top-5 numbers to match the FIFO delta — that's fabricating data

**Why this matters more than the plain markdown-rounded residual**: the markdown-rounded residual (skill's "Drift decomposition residual" pitfall above) is ~$30-100. The mixed-source residual can be **$500-700** (0.5-0.7% of MV) — an order of magnitude larger. If you don't flag the source mix, a future agent will assume the decomposition is buggy and try to "fix" it by hand-tuning the Top-5.

### `subprocess.run` with inline `python3 -c` can fail SILENTLY (tirith + shell quoting)

Multi-line Python passed inline as `subprocess.run(['python3', '-c', '''...'''])` can produce **zero output with exit code 0** — the subprocess "succeeds" but emits nothing. The most common causes are tirith pre-execution scanning the inline source for unicode/quotes (similar to the heredoc pattern) and shell-quoting issues with nested f-strings.

**Real case (cron #34)**: tried to fetch 19 yfinance prior closes via:
```python
result = subprocess.run(['python3', '-c', '''
import yfinance as yf
syms = ["CRM", "TSLA", ...]
for s in syms:
    h = yf.Ticker(s).history(period="10d")
    print(f"{s}|{h['Close'].iloc[-2]:.2f}")
'''], capture_output=True, text=True, timeout=60)
```
**Result**: `STDOUT: ` (empty), 0/32 fetched. Exit code 0, no error.

**Workaround that worked** (second attempt): drop the subprocess wrapper, call `yf.Ticker` directly inside `execute_code` (the same runtime that hosts the skill code). All 32 fetched in ~7.5s.

**Pattern**:
- **Prefer direct library calls in `execute_code`** over `subprocess.run(['python3', '-c', ...])` wrappers
- **If you must subprocess**, write the Python to a temp file via `write_file`, then `subprocess.run(['python3', '/tmp/foo.py'])` — temp file content is not subject to inline-source tirith scanning
- **Always check `result.stdout` length after subprocess**; if `len(result.stdout) == 0` but exit code is 0, the subprocess was silently blocked. Don't trust "no error" alone.
- This is **distinct from the `tirith:pipe_to_interpreter` pattern** (skill pitfall "tirith blocks `tail | python3` pipes") which fires on `tail | python3` shell pipes. The `subprocess.run` silent failure is a different code path (inline source scanning) with the same root cause (tirith pre-execution).

### RTH mid-session cron (#24, BJT 01:00) — TP1-over-line profit-taking dominates drift

By the time cron #24 runs (13:00 EDT, 3h into RTH), positions that crossed TP1 line in cron #22/#23 are under sustained profit-taking pressure. **Drift contributors observed in cron #24 (real session 2026-09-10 01:00 BJT)**:
- Top 5 negative contributors = HOOD (−$137.64), COP (−$50.56), IREN (−$27.65), SNDK (−$21.42), RKLB (−$5.04)
- 4 of 5 (HOOD/COP/IREN/SNDK) are positions with `pnl_pct >= +15%`, mostly TP1-over-line
- Top positive contributor was DE (+$68.00), an industrial-defensive name — opposite end of the risk spectrum
- TP2 closest positions (SNDK, CRM) **moved AWAY from TP2** during RTH mid-session (−6.5% → −7.6% for SNDK, −9.8% → −10.8% for CRM), reversing the overnight narrowing trend from cron #22

**Pattern**: in RTH mid-session crons, the top-N negative drift decomposition is dominated by the same names listed in `_audit.tp1_over_line_unmarked`. This is the RTH fade counterpart of the overnight gap-up. Always include the "TP1-over-line profit-taking" framing in the cron section, not just the raw $-amount drift table.

Compare with the next-day pre-open cron (P-MR-247 day-boundary reset), where the same TP1-over-line positions typically narrow their TP2 gap overnight (gap-up re-mark). The mid-session cron captures the unwind; the pre-open cron captures the re-mark.

## Metrics cheat-sheet (compute from scan output)

```
cash          = pos_data["cash"]
total_cost    = Σ qty × entry_price
total_mv      = Σ qty × current_price
total_equity  = total_mv + cash
unrealized    = total_mv - total_cost
unrealized_pct = unrealized / total_cost × 100

zt (zero-trigger)         = previous_zt + 1  (same day) OR 1  (day boundary, +1 for this cron)
cf (cash-at-floor)        = 1 if cash < $100 else 0
tp1_fires_this_cron       = count(positions where pnl_pct >= +20% AND not already in state)
tp2_nearest               = position with smallest |pnl_pct - 40%| and positive gap
cap_violations            = positions where qty × price / total_mv > 0.10
```

## Day-boundary reset (P-MR-247)

If this cron's BJT date (UTC+8) differs from the previous cron's BJT date → reset `zt` to 1, then apply +1 for this cron's 0-BUY count → `zt = 2` for first scan of the new day. Day boundary is BJT-based, NOT UTC-based.

```
09-08 22:00 zt=5  cf=0
09-08 23:00 zt=6  cf=0    ← same-day carry (P-MR-201)
09-09 01:00 zt=2  cf=0    ← DAY-BOUNDARY RESET (P-MR-247): 6 → 1 → +1 = 2
09-09 03:00 zt=3  cf=0    ← same-day carry
09-09 03:30 zt=4  cf=0    ← same-day carry
```

### Daily summary block (must include end of cron section)

```markdown
### 📊 當日總結 (YYYY-MM-DD BJT)

\`\`\`
🔔 買入信號:         N
🎯 TP1 觸發:        N  (state file: X 隻 TP1=true 保留)
🎯 TP2 觸發:        N  (CLOSEST: SYM −X.X% from TP2 line $Y.YY)
🚪 止蝕/賣出觸發:   N

💰 開盤總權益 (前日 22:00):   $X,XXX.XX
💰 收市前總權益 (今日 03:30): $X,XXX.XX
📈 日內 MTM:                   ±$XXX.XX (±X.XX%)
📦 未實現 PnL:                ±$X,XXX.XX (±X.XX%)
💵 現金:                       $XXX.XX
📊 持倉數:                     NN
🚨 Cap violations:             SYM1 XX.XX% / SYM2 XX.XX%
🚦 零觸發連續 (zt):            NN
\`\`\`
```

**TP1=true count is DERIVED, not hand-typed**: the "X 隻 TP1=true 保留" field counts keys in `/tmp/ai_trader_tp1_state.json` whose value is exactly `true` (the boolean flag pattern, not the HOOD-style `FULLY_CLOSED` object). Compute via:

```python
import json
state = json.load(open('/tmp/ai_trader_tp1_state.json'))
tp1_true_count = sum(1 for k, v in state.items() if k != '_audit' and v is True)
```

Include this in your `execute_code` block before assembling the daily summary. Real case (cron #36, 2026-09-15 03:30 BJT): 14 TP1=true — symbols `AMD, NBIS, ONDS, PYPL, SMCI, DHR, ADBE, MSFT, JD, ANET, PATH, CRWV, IREN, SNDK`. Also 3 TP1=false (`AVAV, CIFR, SYM`) and 1 `FULLY_CLOSED` object (`HOOD`). Don't recount manually — the count must match the state file exactly, and FIFO recompute can flip a boolean at any time.

## Drift decomposition pattern (RTH mid-session + cron delta)

When the cron is mid-session (vs prior cron was a different time), break the MV drift into Top-5 positive/negative contributors. Pattern (cron #28 2026-09-10 23:00 BJT, ~1h drift +$169.06):

```python
# Drift = Σ qty × (cur_price - prev_cron_price) per position
# Group by sign, sort, take top-5 each
# Always include pnl% and 1-word sector tag for each contributor
# Top-5 negative are typically TP1-over-line profit-taking (RTH mid-session pattern)
# Top-5 positive are typically low-pnl bounce (HON/AVGO/KLAC — mean reversion)
```

Use `execute_code` (not heredoc, not file). The script iterates over a tuple list with prev-cron prices captured from the prior cron's markdown section. Save as `templates/drift_decomposition.py` for re-use.

**Null-drift case (artifact crons)**: when MV drift is exactly $0.00 (stacked weekend/holiday artifact, see above), do NOT fabricate a Top-5 table — state explicitly "Drift decomposition: null, all 32 positions frozen at Fri RTH close, no live trading during this window". Real case: crons #35 and #36 both had MV $100,037.86 with $0.00 drift vs prior cron, and the Top-5 table would have shown all zeros (noise). A one-line "null decomposition" is the honest report and matches what the next FIFO recompute will see.

## Reference files

- `references/cron-section-template.md` — full markdown template for the cron section in `vivian-notes/AI-Trader.md`
- `references/state-file-schema.md` — schema and semantics for `/tmp/ai_trader_tp1_state.json` (FIFO-recompute-only mutations, HOOD-style post-closure re-entry handling)
- `references/next-day-pre-open-cron.md` — distinct session class for BJT 22:00 next-day cron (#22): overnight drift, dormant MA10 trail, TP2 narrowing, counter semantics
- `references/pre-market-follow-through-cron.md` — distinct session class for BJT 23:00 cron (#33): pre-market active, MA10 activating, distinct drift shape
- `references/2026-09-09-0100-cron-state-diagnostics.md` — snapshot of edge cases observed in the 2026-09-09 01:00 BJT cron: HOOD post-closure TP1 dilemma, RKLB fixed-vs-active SL divergence, day-boundary reset details
- `references/2026-09-15-0300-cron35-rth-late-artifact.md` — snapshot of edge cases observed in cron #35: per-share cost-reconstruction unit trap, first-non-zero-drift yfinance-lag artifact, cap_pct ratio vs price decouple, write_file sibling-race handling, prior-cron price fallback pattern
- `references/2026-09-15-0330-cron36-rth-close-artifact.md` — snapshot of cron #36: yfinance lag persistence past #35/#36 (corrects skill's "lag resolves by #35/#36" prediction), HOOD re-crosses TP1 line (+19.1% → +20.5%), CRM TP2 gap narrowing acceleration, drift decomposition residual ($95 from markdown-rounded prev prices), RKLB first streak relief since #30
- `references/2026-09-15-2200-cron27-pre-open.md` — snapshot of cron #27: first real-data pre-open after yfinance-lag chain (Labor Day + Tue lag); exposes scan.py `period="5d"` MA20 latent bug (32/32 positions MA20 ≡ price trivially); validates overnight_drift.py usage with `iloc[-2]` semantics; documents cap_pct ratio decoupling (MRVL gap-up worsening its own ratio)
- `references/2026-09-16-0100-cron34-rth-mid-session.md` — snapshot of cron #34: yfinance 1-day lag persisted into RTH mid-session (validates same-day artifact prediction); mixed prior-price source drift decomposition (markdown-rounded for 13/32 + yfinance iloc[-2] for 19/32 → −$643 residual vs FIFO MV delta); PATH re-crosses TP1 line (symmetric inverse-CRM); CRM re-takes TP2-nearest lead from snapshot diff alone; `subprocess.run` with inline `python3 -c` silent-failure pitfall
- `references/2026-09-16-0300-cron35-rth-late-non-artifact.md` — snapshot of cron #35 (2026-09-16 03:00 BJT): **yfinance lag artifact chain BROKEN** — by Tue 15:00 EDT yfinance caught up to Tue RTH close, contradicting both skill's v1 (resolve at #36) and v2 (resolve at next #27) predictions. Drift decomposition = Mon 09-14 close → Tue 09-15 RTH close (1 trading day window), residual $0.00 from clean yfinance iloc[-2] single-source. PATH TP1=true confirmed by inter-cron FIFO recompute (new pitfall). MA20 latent bug (`period="5d"`) confirmed: 30/32 positions show real_MA20 ≠ scan_MA20 ($0.56-$68.02 per position diff via 6mo spot-check).
- `references/2026-09-16-0330-cron36-rth-close.md` — snapshot of cron #36 (2026-09-16 03:30 BJT): RTH close -30min, real Tue RTH data. Section-separator paragraph variant pitfall (new — `##` jam against prior paragraph when no `---`); `scan_meta_log.json` `positions` field is INT not list (skill advice correction); mixed-source drift residual −$91.77 with partial markdown + yfinance fallback; cap-violation ratio decoupling math walkthrough; RKLB fixed-SL vs active-SL diagnostic confirmed; yfinance cache state diagnostic via SPY spot-check.

## Reusable scripts

- `scripts/cron_state_refresh.py` — 0-trade variant: refresh `_audit` block in `tp1_state.json`, append meta log (auto-recovers from malformed legacy JSON), update `cf`/`zt` counters. Avoids the emoji-heredoc tirith block by being a file, not a heredoc. **Every cron should invoke this after `scan.py` instead of hand-coding the refresh logic.**
- `scripts/overnight_drift.py` — overnight drift attribution for the pre-open cron (BJT 22:00 next day, P-MR-247). Fetches yfinance prior-close prices via `history(period="5d")` and computes Top-5 positive/negative contributors. Use after `cron_state_refresh.py` for next-day pre-open crons where the prior cron's scan prices are stale.
- `scripts/yfinance_prior_close.py` — generic yfinance prior-close fetcher for cron drift decomposition. Use when (a) prior-cron scan prices aren't fully captured in markdown (e.g. cron #33 only had 5/32), or (b) yfinance 1-day lag means current scan prices = prior trading day close. See "yfinance 1-day lag during RTH" pitfall below for the (b) case pattern.
### Stage 2 池 Loop (P-MR-294 第 34 次連續) pattern

### ⚠️ Pitfall: Don't hand-roll the state refresh — use the script

The skill explicitly says "Every cron should invoke this after `scan.py` instead of hand-coding the refresh logic." If you find yourself typing `with open('/tmp/ai_trader_tp1_state.json') as f: tp1_state = json.load(f)` followed by manual mutation of `_audit`, STOP. That logic is exactly what `cron_state_refresh.py` does — using argparse with all the right defaults (BJT timezone, malformed-log recovery, counter updates, semantic invariant reminder).

**When you still need the script but ALSO need extra info in the cron section** (cap violations, near-TP1 PnL table, drift contributor decomposition): run the script FIRST for the state/log mutation, THEN do a separate `execute_code` or `write_file` block to compute the extras for the markdown section. Do NOT mix the state-file mutation into the extras block — that's how you end up with `cron: "#28"` typos in the audit (real case: cron #24 set `cron: "#28"` because the inline block ran before `zt_count = 29` was corrected; the script's argparse `--cron-id "#24"` avoids this entirely because the cron ID is a string parameter, not a sequenced integer).

### ⚠️ Pitfall: `execute_code` is NOT the same tirith hazard as `python3 << EOF`

Real case (cron #28, 2026-09-10 23:00 BJT): I used `execute_code` to compute FIFO totals + drift decomposition + meta log append (no heredoc, no shell quoting). It succeeded without tirith blocking. The tirith hazard is specifically about shell-level heredocs where the unicode payload is inline in the heredoc body — `execute_code` (which sends the python as a parameter to the runtime, not as a shell pipe) sidesteps that path. **Use `execute_code` for computations and meta-log appends; use `write_file` + `python3 /tmp/foo.py` for emoji-heavy markdown state mutations.**

The custom script I wrote (`/tmp/cron28_state_refresh.py`) was a regression — it duplicated `cron_state_refresh.py` logic without argparse, didn't update `/tmp/ai_trader_cash_floor.json` or `/tmp/ai_trader_zero_trigger.json`, and didn't use `recover_meta_log_entries`. Future crons must use the canonical script. If you need extras (drift decomposition, near-TP1 table) compute them via `execute_code` after the canonical script runs, NOT inside a custom cron-state script.

### ⚠️ Pitfall: tirith blocks `tail | python3` pipes (use `read_file` with offset)

Real case (cron #32, 2026-09-11 22:00 BJT): tried `tail -3 /tmp/ai_trader_scan_meta_log.json | python3 -c "..."` to verify the canonical script's meta-log append. Tirith blocked with `[HIGH] Pipe to interpreter: tail | python3`, pattern_key `tirith:pipe_to_interpreter`. The pipe path executes downloaded content without inspection — same conceptual hazard as heredoc, but different pattern_key.

**Workaround for inspecting /tmp/ JSON logs**: use `read_file` with `limit` and `offset` parameters, not `cat/tail/head | python`. For the meta-log specifically, `offset=320` of 329 lines gave the latest entry. For larger files, page through with `limit=50` chunks.

### Overnight drift attribution — use `scripts/overnight_drift.py` for pre-open cron

The canonical `drift_decomposition.py` template requires prev_prices as input. For consecutive intraday crons (1-2h drift), the prior cron's scan output is the right source. **For the pre-open cron (BJT 22:00 next day, P-MR-247 reset)**, the prior cron was RTH-close ~18.5h ago — actual overnight moves happened during the gap and the scan prices from #31 don't reflect them. Use `scripts/overnight_drift.py` which fetches authoritative prior-close via yfinance `history(period="5d")` and computes Top-5 attribution.

**`overnight_drift.py` semantics — `iloc[-2]` depends on yfinance cache freshness**:

The script computes `prev = yfinance.history(period="5d")['Close'].iloc[-2]`. The "previous trading day" is whatever yfinance had cached BEFORE the most recent trading day. This can be:

| yfinance cache state | `iloc[-1]` | `iloc[-2]` (script's "prev") | Drift window interpretation |
|---|---|---|---|
| Mon close only (e.g. Tue 10:00 EDT pre-RTH-open) | Mon | Fri | "Fri → Mon" = weekend gap (NOT 1 day) |
| Mon + Tue close (e.g. Wed 10:00 EDT pre-open, Tue already cached) | Tue | Mon | "Mon → Tue" = 1 trading day |
| Mon + Tue + Wed close (Wed 11:00 EDT) | Wed | Tue | "Tue → Wed" = 1 trading day |

**Real case (cron #27, 2026-09-15 22:00 BJT = Tue 10:00 EDT)**:
- yfinance already had cached Tue 09-15 close (HOOD $110.35 last)
- But script's `iloc[-2]` returned Mon 09-14 close ($114.33) — i.e. the day BEFORE the latest cached close
- Drift decomposition = "Mon → scan-reported Tue close" = 1 trading day window
- This was CORRECT for a pre-open cron whose MV-delta is being measured against #36 RTH-close (which itself reflected Mon close via yfinance-lag artifact)

**Diagnostic rule**: before using `overnight_drift.py` output, spot-check yfinance to see how many trading days are cached. If `len(history) >= 2` and `history.index[-1]` is TODAY (current BJT date), then `iloc[-2]` = yesterday's close, and drift is "yesterday → today" (1 trading day). If `history.index[-1]` is YESTERDAY, then `iloc[-2]` is 2 days back, and drift is "2 days back → yesterday" — wrong window for pre-open cron; manually fetch the right prior close via `history(period="10d")` and pick the right iloc manually.

**Pattern**:
1. Always spot-check `yf.Ticker(SYM).history(period="5d")` first to confirm cache freshness
2. If the cron's "prior RTH close" is the latest cached trading day, then `overnight_drift.py`'s `iloc[-2]` is one day BEFORE that — i.e. 2 days back from the cron time, NOT 1 day back
3. For pre-open cron after yfinance has already cached today's close, this gives a 1-trading-day drift window which matches the MV-delta against #36 RTH-close (because #36 also had stale Mon-close data per the yfinance-lag pitfall)
4. The drift window interpretation depends on which day's close the script's `iloc[-2]` returns — document it explicitly in the cron section's drift header

Invoked from `execute_code` after `cron_state_refresh.py` runs:

```python
import subprocess, json
result = subprocess.run([
    "python3", "/home/vivian/.hermes/skills/ai-trader-cron/scripts/overnight_drift.py",
    "--positions-json", json.dumps([
        {"symbol": "HOOD", "qty": 74, "price": 114.79, "pnl_pct": 20.0},
        {"symbol": "CRM", "qty": 1, "price": 248.82, "pnl_pct": 25.5},
        # ... 32 positions total
    ]),
    "--top-n", "5",
], capture_output=True, text=True, timeout=60)
print(result.stdout)  # ready-to-paste markdown
```

Real case (cron #32): script covered 12/32 positions (~70% MV), uncovered ~$+1,150 spread across remaining positions. MRVL gap-up +3.94% drove +$462.76 of upside drift — this was the cap-violating position, so drift attribution flagged it for context.

**Quick reference for the script's CLI** (so you don't have to dig):
```bash
python3 /home/vivian/.hermes/skills/ai-trader-cron/scripts/cron_state_refresh.py \
  --cron-id "#24" \
  --time-bjt "2026-09-10T01:00:00+08:00" \
  --edt-time "2026-09-10T13:00:00-04:00" \
  --session "RTH mid-session follow-through" \
  --cash 207.40 --positions 32 --total-mv 101096.16 --total-equity 101303.56 \
  --stage2 0 --buy-signals 0 --sell-signals 0 --zt 29 --cf 0 \
  --tp2-nearest-symbol SNDK --tp2-nearest-price 1774.90 \
  --tp2-nearest-line 1921.08 --tp2-nearest-gap -7.6 \
  --tp1-unmarked "CRM (manual queue, +24.9%)" "HOOD (fresh lot, +21.9%)" "COP (manual queue, +24.4%)" "MRK (TP1=true ✅)" "SNDK (TP1=true ✅)" \
  --ma10-trail "active (RTH mid-session); 32 positions MA10/MA20 OK, no breach" \
  --notes "RTH mid-session. MV -356.51 (-0.35%) pure mark-to-market. 29th consec zero-trigger."
```

Note `--tp2-nearest-gap` accepts negative numbers directly (no leading `+`); the script formats the display string. This avoids the literal-`+`-plus-format-spec double-plus bug (see Pitfalls).

**How to invoke from `execute_code` (canonical pattern)**: when the cron is running inside the Hermes `execute_code` tool (rather than terminal), wrap the invocation in `subprocess.run([...], capture_output=True, text=True, timeout=30)`. Pattern (cron #29):

```python
import subprocess
result = subprocess.run([
    "python3", "/home/vivian/.hermes/skills/ai-trader-cron/scripts/cron_state_refresh.py",
    "--cron-id", "#29", "--time-bjt", "2026-09-11T01:00:00+08:00",
    "--edt-time", "2026-09-10T13:00:00-04:00",
    "--session", "RTH mid-session follow-through (next-day, day-boundary reset)",
    "--cash", "207.40", "--positions", "32",
    "--total-mv", "99838.84", "--total-equity", "100046.24",
    "--stage2", "0", "--buy-signals", "0", "--sell-signals", "0",
    "--zt", "2", "--cf", "0",
    "--tp2-nearest-symbol", "COP", "--tp2-nearest-price", "137.43",
    "--tp2-nearest-line", "153.55", "--tp2-nearest-gap", "-11.7",
    "--tp1-unmarked", "CRM (manual queue, +23.6%)", "COP (manual queue, +25.3%)", "MRK (manual queue, +22.5%)",
    "--ma10-trail", "active (RTH mid-session); 32 positions MA10/MA20 OK, no breach; RKLB -19.3% weakest (11-window streak, deteriorated 1.2pp from #28 -18.1%)",
    "--notes", "RTH mid-session (next-day, P-MR-247 day-boundary reset). MV -323.65 (-0.32%) overnight+1h drift. 4 TP1-over-line (HOOD fell below TP1 line 20.5% → 19.4%, dropped from list). COP TP2 nearest at +11.7%.",
], capture_output=True, text=True, timeout=30)
print("STDOUT:", result.stdout); print("STDERR:", result.stderr); print("EXIT:", result.returncode)
```

This sidesteps the tirith emoji-heredoc block (no shell quoting of unicode content) and keeps the cron idempotent. Use this pattern whenever the cron prompt invites `execute_code` rather than `terminal`.

## Companion skills

- `github-auth` — PAT diagnosis (`cron-push-diagnose.sh`), dead-PAT recovery, push-protection patterns. External to this umbrella; only needed when GitHub auth itself is broken.

## Absorbed sub-disciplines (formerly separate skills)

### A. Stock Analysis Methodology → `references/stock-analysis-methodology.md`

The analytical backend behind `/tmp/ai_trader_scan.py`. Pure technical analysis (no fundamentals): TP1/TP2 line math, MA10/MA20 trail-stop diagnostic, Stage 2 breakout-pullback filter (P-MR-294), RR scoring, cap-violation math, drift decomposition. This was the `stock-analysis` skill before consolidation. Load the reference for ad-hoc ticker analysis, cost reconstruction, or when extending the scan methodology.

### B. Vivian Notes Daily Backup → `references/vivian-notes-backup.md`

The GitHub-push half of the AI-Trader workflow. Daily backup of `vivian-notes/AI-Trader.md` (iPhone-edited Obsidian notes) + `trading_journal.json` + last 14 days of `~/.hermes/cron/output/*.md` to `github.com/vivianyeah/vivian-notes`. Runs at 04:30 BJT Mon-Fri via cron `f15316cccb3b`. PAT plumbing, GH013 redaction, the 2026-09-08 `vivian-notes/` path migration, and the soft-reset recovery pattern for local-ahead-of-origin conflicts all live here. This was the `vivian-notes-backup` skill before consolidation.

The script `backup_vivian_notes.sh` (step 11 of the cron template) invokes the full backup-pipeline flow documented in that reference.

### C. Session-specific diagnostics → `references/2026-09-09-0100-cron-state-diagnostics.md`

Snapshot of edge cases observed in the 2026-09-09 01:00 BJT cron: HOOD post-closure TP1 dilemma, RKLB fixed-vs-active SL divergence, day-boundary reset details. Useful when those exact patterns recur and the canonical skill bodies need a concrete real-case illustration.

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你係 AI-Trader 模擬倉 cron（純 paper trading，冇 IB Gateway、冇 SSH tunnel、冇真倉）。HermesV, Agent ID 6092.

# 任務
執行 /tmp/ai_trader_scan.py 做模擬倉 scan。

# 步驟
1. `python3 /tmp/ai_trader_scan.py`（前台跑，唔好 background）
2. 解析輸出，整理 scan 結果
3. 寫 `/tmp/vivian-notes/AI-Trader.md`（git push）
4. 寫 `/tmp/ai_trader_trades_log.json`（持久化）
5. 寫 `/tmp/ai_trader_tp1_state.json`（TP1 狀態）
6. send_message target=telegram:8685773783 報告 scan 結果

# 重要規則
- **純模擬倉**，冇 IB、冇 SSH、冇 Pi、冇 4001 port
- **Stage 2 突破回調策略**：MA10/MA20/RSI/MACD/volume
- **止蝕雙方法顯示**：β<1→5%固定，β≥1→1.5×ATR×max(β,0.5)
- **評分含值博率 (RR)**，最高5粒星
- **過濾顯示不合格原因**（邊個技術指標唔達標）
- **每次交易 push GitHub**（commit AI-Trader.md + trades log）

# 時段
北京時間 22:00 = 美股 RTH 開市後 30 分鐘
此時段屬「21:30-22:00 高波動後」穩定期，適合 scan

# 報告格式
用 ``` code block + monospace 對齊 + 🟢/🔴/🟡 emoji + 📈/⚠️ 標題
持倉報告章節：持倉、買入訊號、止蝕位分開顯示

# Pitfalls (記得避)
- 唔好 background 跑 script（會 hang）
- 唔好 auto retry IB order（已退出 IB trading）
- trades log 要 append 唔好 overwrite

完成後報告 scan 結果 + 任何異常。

## Response

Both counter files updated. Cron #27 complete. Here's the report:

---

# 📊 Cron #27 — 2026-09-16 22:00 BJT (Wed 10:00 EDT — Next-day pre-open)

```
🔔 買入信號:         0
🎯 TP1 觸發:        0  (state file: 14 隻 TP1=true 保留, PATH confirmed between crons)
🎯 TP2 觸發:        0  (CLOSEST: CRM +8.53% from TP2 line $277.53)
🚪 止蝕/賣出觸發:   0

💰 Total MV:        $98,470.96   (vs #36 $100,037.86 → −$1,566.90 / −1.57%)
💰 Total Equity:    $98,678.36
📦 Unrealized PnL:  +$4,843.04 (+5.17%)
💵 Cash:            $207.40
📊 持倉數:          32
🚨 Cap violations:  DE 11.74% / MRVL 10.84%  (MRVL +0.53pp worsened)
🚦 zt:              2  (P-MR-247 reset: #36 zt=6 → 1 → +1=2)
🎯 Stage 2:         0  (P-MR-294 第 38 次連續)
```

## ⚠️ 關鍵信號 (key insight)

**First live-data cron after Labor Day artifact chain.** Prior cron #36 returned Fri 09-11 close snapshot (artifact), masking 2 trading days of real movement. Today's −$1.57% MV drift was invisible at #34/#35/#36.

**yfinance cache caught up** — SPY last close = Wed 09-16 (live). The Labor Day chain is broken.

**MV drift decomposition** (Tue 09-15 close → Wed 09-16 ~10:00 EDT, 1 trading day):
- 🔻 COP −$340.48 (−3.77%), HOOD −$236.06 (−2.89%), BABA −$135.09, XOM −$126.91, DE −$72.42
- 🔺 MRVL +$473.80 (+4.65% gap-up, **but cap violation worsened to 10.84%**), AVGO +$55.93, IREN +$35.70, RKLB +$28.98, INTC +$21.55
- 4 of top-5 negative = **TP1-over-line profit-taking** (classic RTH fade)

## 🎯 TP1-over-line (4 positions, queue for FIFO recompute)

| Symbol | PnL | Cost | TP1 line | Cur | TP2 gap |
|--------|----:|-----:|---------:|----:|--------:|
| CRM | +29.0% | $198.23 | $237.88 | $255.72 | **+8.53%** ← TP2 nearest |
| COP | +23.9% | $109.69 | $131.62 | $135.90 | +12.99% |
| T | +23.8% | $21.53 | $25.83 | $26.65 | +13.09% |
| MRK | +22.1% | $118.21 | $141.85 | $144.33 | +14.66% |

**PATH removed from list** — confirmed TP1=true ✅ in state by inter-cron FIFO recompute (was +22.4% at #34, captured by FIFO between #36 and #27). **HOOD still well below TP1 line** (+12.1%, inverse CRM pattern continues).

## 🚨 Cap violations persist

| Symbol | MV | Cap % | Notes |
|--------|---:|------:|-------|
| DE | $11,556.26 | 11.74% | P-MR-124 block, flat |
| MRVL | $10,672.00 | **10.84%** | +0.53pp worsened (price +4.65% gap-up worsened ratio) |

## 🛡️ MA10/MA20 trail-stop diagnostic

⚠️ **scan.py `period="5d"` latent bug** — all 32 positions show MA20 == price trivially (insufficient lookback for 20-bar MA). NOT a live trail-stop test. The `止蝕=$Z` field IS valid (price × 0.95 trailing stop).

- **RKLB streak relief continues**: −18.3% (vs streak low −20.7% at #35, **+2.4pp cumulative improvement**). Streak now 17+ windows, first sustained relief since #30.
- **RKLB fixed-SL vs active-SL diagnostic**: fixed entry-anchored 5% SL $74.17 **breached by $10.39 (13.3% of cost)**, but active MA10-trail SL $60.59 NOT breached → **NO EXIT** under current rules.

## 📋 Log / State 動作

```
✅ /tmp/ai_trader_scan_meta_log.json         — appended #27 entry (28 total)
✅ /tmp/ai_trader_tp1_state.json             — _audit refreshed, NO TP1/TP2 mutation
✅ /tmp/ai_trader_zero_trigger.json          — zt=2 (P-MR-247 reset)
✅ /tmp/ai_trader_cash_floor.json            — cf=0 (cash $207.40)
✅ /tmp/ai_trader_trades_log.json            — UNCHANGED (0 trades, 287 entries)
✅ /tmp/vivian-notes/vivian-notes/AI-Trader.md — appended (152 lines)
✅ GitHub push — committed 953a246 → origin/main
```

## 🔮 Next cron preview

```
#33 (Wed 23:00 BJT = 11:00 EDT) — Pre-market follow-through
  → MA10 trail activating (vs pre-open dormant)
  → Watch MRVL cap_pct if Wed RTH fade continues
  → zt same-day carry: 2 → 3
```

**No buy/sell signals, no Stage 2 candidates, no exits.** Pure mark-to-market scan.
